---
description: Generate or refresh the business data map (entities, flows, state machines, automatic processes).
argument-hint: [project-path-or-module]
---

Invoke skill `project-context` in mode `data`.
Forward `$ARGUMENTS` unchanged.
