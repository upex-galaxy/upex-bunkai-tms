---
description: Repoint the repo at a new Atlassian instance and regenerate the Jira catalogs after explicit approval.
argument-hint: [source-host] [target-host]
---

Invoke skill `jira-administration` in mode `instance-migration`.
Forward `$ARGUMENTS` unchanged.
