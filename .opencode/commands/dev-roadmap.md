---
description: Generate or refresh the ticket-level dependency execution roadmap (which story unblocks which).
argument-hint: [master-sprint-or-epic-key]
---

Invoke skill `project-context` in mode `dev-roadmap`.
Forward `$ARGUMENTS` unchanged.
