---
description: Plan and reconcile Jira Components against the app's real modules after explicit approval.
argument-hint: [project-key] [target-source]
---

Invoke skill `jira-administration` in mode `components`.
Forward `$ARGUMENTS` unchanged.
