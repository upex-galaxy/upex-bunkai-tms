# BK-87 — Code Review Adjudication

Dev-authored (non-Jira). Independent adversarial review per `/sprint-development` Stage 3; findings adjudicated by the orchestrator against the actual diff + AC before any fix was applied.

## PR1 — `feat/BK-87-settings-hub-shell` (#63, merged)

Reviewer verdict: 0 BLOCKER, 0 MAJOR, 2 MINOR, 1 NIT — mergeable as-is. No findings required a fix; merged unchanged.

## PR2 — `feat/BK-87-settings-workspace-list`

| # | Severity | Finding | Adjudication | Action |
| - | -------- | ------- | ------------- | ------ |
| 1 | BLOCKER | `app/(app)/settings/account/page.tsx`: the per-workspace active-member count ran through the RLS-bound client, so `workspace_members` RLS (`workspace_members_select_self_or_admin`) silently restricted the count query to the caller's own row for any workspace where the caller is `member`/`viewer`. Reviewer live-verified: fixture account (`member` in `bunkai-smoke-qa`) showed "1 member" when the workspace actually has 2+. | **Legitimate — CONFIRMED.** Verified directly by reading `page.tsx:126` (used `supabase`, not `createAdminClient()`, unlike the identity lookup two sections up) and re-testing live post-fix. | **Fixed** — routed the count aggregate through `createAdminClient()`. Re-verified live: `bunkai-smoke-qa` now reports `memberCount: 2`. Commit `adf0862`. |
| 2 | MAJOR | `WorkspacesSection` (the actual async data-fetching component, where the BLOCKER lived) has zero test coverage — only the extracted pure functions (`buildWorkspaceRows`, etc.) are unit-tested. | **Legitimate observation, not actionable now.** This repo has no component/integration test anywhere (confirmed during PR1's review too) — adding one here would introduce a new testing pattern unilaterally, out of scope for a single story. | **Deferred as tech-debt.** Not fixed in this PR. Flagging for a future tech-story: this repo's Server Components that do multi-query I/O have no integration-test coverage anywhere, which is exactly how the BLOCKER above shipped unnoticed. |
| 3 | MINOR | `WorkspacesSection`'s `createClient()` call sat outside the `try` block — if it throws, the rejection bypasses the local catch and would propagate past the component, violating TD7's guarantee in the edge case. | **Legitimate, cheap to fix.** Matches an existing pattern elsewhere in the same file (line 21, from PR1) so not a new regression, but directly touches this story's binding TD7 decision. | **Fixed** — moved `createClient()` inside the `try`. Commit `adf0862`. |
| 4 | MINOR | `components/settings/WorkspacesList.tsx`: the replaced placeholder's `aria-labelledby` landmark association was dropped; the `<h2 id="settings-workspaces-heading">` became orphaned (nothing references it). Mockup uses full ARIA table roles, not adopted here. | **Legitimate regression on the landmark; ARIA-table-roles gap is a separate, larger fidelity question not raised as a Technical Decision.** | **Partially fixed** — restored `role="region" aria-labelledby="settings-workspaces-heading"` on the list's root `<Card>` (commit `adf0862`), reinstating the landmark a screen-reader user had before. Full ARIA table semantics (`role="table"/"row"/"cell"`) NOT adopted — that's a materially larger structural change beyond this MINOR's fix-cost; noted as a future fidelity candidate, not blocking. |
| 5 | NIT | Silent `catch` on the workspace-count query has no logging — a real prod failure would only be visible via the user-facing retry message. | **Legitimate, matches an existing convention already in the same file (identity lookup, lines 61-70).** | **Not fixed** — consistent with established codebase convention; would need an observability decision (logging/monitoring) that's out of this story's scope. |

**Post-fix verification**: `tsc --noEmit` clean, `eslint .` clean, `bun test lib/account/ lib/settings/ middleware.test.ts` 50/50 pass. Live-verified against the `QA_E2E_USER_EMAIL` fixture (no admin impersonation): both workspace rows render, `bunkai-smoke-qa`'s member count corrected from 1 to 2, zero console errors.
