# BK-27 — Mockup

> Jira field: `customfield_10120` · [View in Jira](https://jira.upexgalaxy.com/browse/BK-27)

# Acceptance Test Plan (ATP) — Shift-Left DRAFT

## Coverage Summary

- ***Total Scenarios:*** 25
- ***Positive:**** 5 | ****Negative:**** 6 | ****Boundary:**** 7 | ****Integration:*** 7

## Positive Scenarios (5)

- Should create a Test with a single ATC and preserve it in the chain
- Should create a Test with three ATCs in exact order
- Should create a Test with ten ATCs in exact order
- Should display newly created Test immediately in the Test list
- Should preserve Test title exactly as entered (including spaces, punctuation, mixed case)

## Negative Scenarios (6)

- Should block Test creation when no ATC is selected
- Should display error message "A Test must include at least one ATC" when save fails
- Should reject Test creation from viewer role with HTTP 403
- Should reject Test creation when ATC belongs to foreign workspace
- Should not reveal whether foreign ATC exists in error message
- Should block Test creation with whitespace-only title

## Boundary Scenarios (7)

- Should accept a 200-character title (exact limit)
- Should reject a 201-character title (over limit)
- Should accept special characters in title (emoji, accented chars)
- Should accept a Test title with leading/trailing spaces after trim
- Should accept a Test with the same ATC twice in the chain
- Should accept a Test with max chain length (assume: 100 ATCs)
- Should reject a Test chain with 101 ATCs (if limit enforced)

## Integration Scenarios (7)

- Should write activity log entry when Test is created
- Should link activity log entry to the correct workspace
- Should link activity log entry to the correct user
- Should link activity log entry to the correct Test
- Should return Test ID in creation response (for API clients)
- Should idempotently return same Test ID when request is retried with same Idempotency-Key
- Should accept Test creation from API client with Bearer PAT (headless)

## Refined Acceptance Criteria (All 4 ACs remain valid)

### AC 1: Elena assembles a Test from three ATCs

Given Elena is signed in to workspace "Acme QA" with role "member"
When Elena assembles a Test with title "Add to Cart from Empty State" from three ATCs [open-cart, click-add, verify-count]
Then the Test is created, appears in Test list, preserves ATC order, and activity log records Test.create event

### AC 2: Saving a Test without any ATC is blocked

Given Elena is on the "New Test" form with title but no ATCs
When she clicks "Save"
Then save is blocked (400/422), error message shows "A Test must include at least one ATC", no Test created, no activity log entry

### AC 3: Double-click on Save button within idempotency window

Given Elena fills the form with valid data and clicks "Save"
When she clicks "Save" again within 1 second (before response arrives)
Then exactly one Test is created, activity log records one event, second request returns same Test ID

### AC 4: Elena cannot use ATCs from a workspace she does not belong to

Given Elena belongs to "Acme QA" but NOT to "Other Co"
When Elena attempts to create a Test in "Acme QA" with a foreign ATC from "Other Co"
Then creation is rejected (403/400), error says "You do not have permission to use this ATC" (no leakage), no Test created

## Open Questions for PO/Dev

### PO Scope Clarifications (3)

1. Idempotency window: suggest 24h
2. Max chain length: suggest no hard limit in MVP; soft UI limit (e.g., 100)
3. Test description field: confirm scope (title only vs. title+description)

### Dev Technical Clarifications (5)

1. Idempotency-Key scope: recommend (user_id, endpoint, key) for race-condition safety
2. ATC validation timing: recommend validate ALL ATCs before insert (atomic failure)
3. RLS policy for foreign ATC: explicit check: ATC.workspace*id = Test.workspace*id
4. Error message for foreign ATC: generic "You do not have permission to use this ATC" (no leakage)
5. Activity log actor field: recommend store user_id (immutable FK reference)

## Shift-Left Status

- 4 ACs refined (all valid, no blockers)
- 25 ATP outlines (Positive, Negative, Boundary, Integration)
- 8 edge cases mapped + mitigations
- 8 open questions (3 PO, 5 Dev) — none blocking estimation
- Coverage is comprehensive across permission, validation, audit, and E2E flows

---

Generated by Shift-Left Testing Phase 2 — 2026-06-06

---
_Synced from Jira by sync-jira-issues_
