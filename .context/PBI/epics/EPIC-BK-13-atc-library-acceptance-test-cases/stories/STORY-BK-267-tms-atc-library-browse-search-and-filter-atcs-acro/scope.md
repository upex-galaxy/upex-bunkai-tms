# BK-267 — Scope

> Jira field: `customfield_10055` · [View in Jira](https://jira.upexgalaxy.com/browse/BK-267)

- Browse every ATC across the whole workspace in one dense, cross-project list, not scoped to a single Project
- Search ATCs by name with fast, incremental narrowing as I type (`/` or Cmd+K focuses the search field, Esc clears it)
- Filter the list by Project, Module, layer (UI / API / Unit), and the User Story / Acceptance Criterion an ATC is anchored to — filters combine together (AND), not mutually exclusive
- See, for each ATC row, its mono ATC id, its name, its owning Project, its Module, its layer (paired with a text label, never color alone), its anchored User Story / Acceptance Criterion, and how many Tests reference it ("used in N tests")
- Clear every active search term and filter in one gesture and return to the full unfiltered list
- Open an ATC row and land inside its owning Project's context — this screen is a lookup-and-launch surface, not an editor
- Reach "ATC Library" as a live sidebar destination instead of the current disabled "Coming soon" placeholder

---
_Synced from Jira by sync-jira-issues_
