# BK-439 — Scope

> Jira field: `customfield_10055` · [View in Jira](https://jira.upexgalaxy.com/browse/BK-439)

- Browse every ATC across the whole workspace in one dense, cross-project list, not scoped to a single Project
- Reach "ATC Library" as a live sidebar destination instead of the current disabled "Coming soon" placeholder, carrying an unfiltered count badge of every ATC the caller can read
- See, for each ATC row, its mono ATC id, its name, its owning Project, its Module, its layer (paired with a text label, never color alone), its anchored User Story / Acceptance Criterion, and how many Tests reference it ("used in N tests")
- Open an ATC row and land inside its owning Project's context — this screen is a lookup-and-launch surface, not an editor
- Default, loading, empty (including empty-workspace), and named-error-with-retry states for the list

---
_Synced from Jira by sync-jira-issues_
