# BK-398 — Scope

> Jira field: `customfield_10055` · [View in Jira](https://jira.upexgalaxy.com/browse/BK-398)

- Open the command palette with ⌘K (or Ctrl+K) from any screen inside the app shell
- Open the same command palette from the sidebar's search control
- Type a query and see results grouped under a heading per entity type: ATC, Test, Project, Module, Bug, Run
- Select a result and land directly on that entity's own screen
- Operate the palette entirely from the keyboard: arrow keys to move between results, Enter to select, Esc to close
- See a guiding empty state before typing anything
- See an explicit "no results" state when a query matches nothing
- See only entities that belong to my current workspace, never another workspace's

---
_Synced from Jira by sync-jira-issues_
