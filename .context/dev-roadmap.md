# Dev Roadmap — Bunkai TMS (ticket-level dependency plan)

> **What this is**: the single source of truth for the **execution order of the dev backlog, driven by dependencies** — at Jira-ticket granularity (BK-NN), across every epic.
> **Last sync**: 2026-08-29 (autonomous-delivery `story`, session `209a7d28` — **EMPTY STORY RUN: no story claimed, and that is the correct outcome. The eleventh consecutive one.** Audited against `origin/staging` tip **`9b9fe8f`**, which this run itself created by merging PR #213; the tip at run start was `83e226e` (PR #216). Four independent verifiers (git ancestry · open PRs and branches · live tracker · staging code surface) were reconciled against each other rather than collapsed into one narrative. **(1) The rescue this document announced on 2026-08-28 had NOT actually landed, and now has.** The prior header says ADR-0015 was "rescued here" — it was authored, but its PR sat OPEN, so `origin/staging` still carried `ADR-0013 … Superseded by: —` while four runs cited the supersession as settled. **PR #213 merged this run (`9b9fe8f`); ADR-0015 is on `staging`; ADR-0013 now names its successor.** PR #210, which carried a byte-identical ADR body under the already-taken number ADR-0014, was **closed as superseded** — the two files were diffed first and differ only in the heading's number, so nothing was lost. **The generalisable rule, and the prior run's own advice unheeded by itself: an open PR is not a landed decision. Either merge the docs PR before the run ends or say plainly in the handoff that its content is NOT on `staging`.** **(2) The live `Ready For Dev` pool is still TWO — BK-219 and BK-512 — and BOTH were dropped, for genuinely different reasons.** **BK-219** fails on a real dependency, which is the one blocker class that legitimately survives Critical Rule #18: its epic root **BK-215 has zero application code on `staging`** — verified this run by inventorying the tree, not by reading a status: no `app/` chat route, no `lib/` chat module, no chat migration, only mockups under `.context/designs/bunkai-test-management-tool/bk-210-team-chat/`. Its comment `11433` (2026-07-16) also declared four critical blockers and said *"Story NOT transitioned"*; the only later comment (`11431`) attaches a mockup link and clears nothing. **A second, previously unrecorded defect on BK-219: its two synced fields contradict each other.** `business-rules.md` ratifies (comment `11432`) that a moderator's tombstone is **indistinguishable** from an author's — *"readers cannot tell whether the author or a moderator removed it"* — while `acceptance-criteria.md` Scenario 5 says the tombstone **indicates removal by a moderator** and the author can see it. Neither was reconciled in any comment. That is a shift-left authoring gap, not a question this run may answer for it. **BK-512** is dropped on **scope-growth**, not on refinement: 8 SP (largest on the board), an irreversible cascading migration across 14 direct cascade edges off `workspaces`, and a destructive-action UI surface that does not exist. Its 🔒 mockup gate also stands — settled by the 2026-08-27 ruling and NOT waived here (A16: a settled question is followed, not re-asked). **(3) BK-512's blocker count nevertheless drops from three to one.** The ADR contradiction is now genuinely closed (item 1). Its four unfilled `<NEEDS PO/DEV CONFIRMATION>` clauses in shift-left scenarios N1/N3/N4/N5 were settled this run by a scored AI PO + AI Tech Lead decision published to the ticket and written into the acceptance-criteria FIELD — a comment alone does not clear it, which comment `12658` proved by recording the same finding a day earlier and leaving the placeholders in place. **What remains is exactly one gate: the mockup.** That is a `/design-system` screen-phase job, and clearing it makes BK-512 the board's next claimable story. **(4) The §8-row coverage gap recurred a FOURTH time, and the count in §5 was internally inconsistent.** **BK-666** and **BK-667** have `master-design-plan.md` §8 rows and **zero mentions in this document** (`grep -c` = 0 for both) — exactly as BK-600/601 did on 08-24, BK-595/596 on 08-23 and BK-593/594 on 08-22. BK-666 is 🔒 mockup-gated (no mockup anywhere draws a Tests list, and `project.jsx` is explicitly NOT its host) and therefore belongs in §5's registry; BK-667 is API-only with no gate. Both added below. Separately, the §5 🔒 row said **"Twelve"** in its heading and **"all thirteen stay gated"** in its tail while listing twelve — corrected to **thirteen** with BK-666 appended. **The mechanism is unchanged and the fix is still not where the cleanup is: `discovery` writes the §8 design row and never writes `dev-roadmap.md`.** Four consecutive runs have patched the symptom here. It belongs in the `discovery` contract. **(5) Two stale claims in this file's own prior headers, corrected — and the BK-215 correction is itself corrected.** This file has said in several places that BK-215 has *"no branch and no merge commit in existence"*, and a peer run's escalation entry pushed back that BK-215 *"satisfies `--is-ancestor`"* through a docs-only merge (Hazard H1). **Measured directly this run, BOTH are imprecise.** `git ls-remote --heads origin | grep BK-215` is **empty** — no branch, confirmed. But `git log origin/staging --grep="BK-215"` returns **four commits**, all of them `docs(...)` commits belonging to *other* tickets that merely NAME BK-215 in a cache-sync or roadmap-reconcile message (e.g. `0740420 docs(BK-262): sync PBI cache for BK-262, BK-215, BK-219, BK-467`). **A `--grep` on a ticket key does not find that ticket's work; it finds every commit that typed the key.** That is the trap underneath both prior claims. The accurate, verifiable statement is **BK-215 has zero implementation commits, zero branches, and zero application code**: `git ls-tree -r origin/staging` matching chat or message returns only `.context/designs/bunkai-test-management-tool/bk-210-team-chat/` mockups and two unrelated skill reference files — no `app/` route, no `lib/` module, no migration. The 2026-08-24 header also cites BK-512's gate at `master-design-plan.md:647`; the row is now at `:675`, a 28-line drift in five days. `master-design-plan.md:13` already forbids exactly this — *cite a §8 row by its ROW TEXT, never by its line number* — and this document still cites by line in several places. Treat every `master-design-plan.md:NNN` citation here as approximate and re-locate by row text.)

> **Prior sync**: 2026-08-28 (autonomous-delivery `story`, session `2967a01a` — **EMPTY STORY RUN: no story claimed, and that is the correct outcome. The tenth consecutive one. The corrected planning surface plus a rescued ADR IS this run's deliverable.** Audited against `origin/staging` tip **`c768542`** (PR #209, merged 2026-08-28T08:14Z), by four independent verifiers reconciled against each other rather than by one narrative. **(1) The pool this document has described since 2026-08-24 no longer exists. Three of its six members SHIPPED**: BK-203 (`c768542`, PR #209, 26 app files), BK-230 (`404cd40`, PR #208, 23 app files) and BK-315 (`d3182c0`, PR #207, 12 app files), each `--is-ancestor origin/staging` exit 0 and each verified to carry application code, not a docs commit wearing its key. BK-269 (`bd3922d`) shipped earlier and is `QA Approved`. **The live `Ready For Dev` pool is now TWO: BK-219 and BK-512.** **(2) An ADR number collided, and that collision is why the 2026-08-27 run's PR sat unmerged and conflicting.** That run authored `ADR-0014-workspace-deletion-revised.md` at 15:51Z on a branch that never merged; BK-230 landed a *different* `ADR-0014-stripe-checkout-billing-upgrade.md` on `staging` at 20:06Z the same day. Two ADRs claimed one number and `.context/ADR/README.md` conflicted, so **the decision that settles BK-512's four-day hard-vs-soft-delete contradiction never reached `staging` at all** — it lived only on a dead branch while three runs in a row cited it as settled. Rescued here: renumbered **ADR-0015**, with ADR-0013's supersession pointer and `master-design-plan.md:675` repointed. **The generalisable trap: an ADR number is a shared resource on a shared branch, and reserving one on an unmerged branch reserves nothing.** Take the number against `origin/staging` at commit time, exactly as migration numbers are taken from the live ledger and never from `ls`. **(3) Neither remaining pool member is claimable, and neither fails for a reason a delivery run can fix.** BK-219 is blocked by **BK-215, which has no commit and no branch anywhere in repo history** — a genuine dependency, the only legitimate blocker type. BK-512 fails on three independent counts: its 🔒 mockup gate stands *and its spec is now wrong* post-ADR-0015 (the ticket's own newest comment says "Do not draw it"), its own newest comment states plainly *"this ticket is NOT claimable yet"*, and AC scenarios N1/N3/N4/N5 still carry the literal string `<NEEDS PO/DEV CONFIRMATION: ...>` in the Gherkin body — answered in prose in comment `12541` and never written back into the field. Scope-growth defers it regardless: 8 SP, the product's first tenant-root soft-delete, a `pg_cron` purge, and a read-path fix touching four helper functions that gate 166 call sites. **(4) "Existing branch" is not evidence of in-flight work in this repo, and treating it as such would have dropped a candidate wrongly.** Merged branches are never auto-deleted here — 75 remote refs carry a `BK-` key, most of them long merged. Eligibility must cross-check the branch against the merged-PR head refs and the live tracker, not stop at `git ls-remote`. Conversely BK-214 *is* genuinely peer-active, proved not by its branch but by the live migration ledger: `0078_notification_digest_log` plus a consolidate-fix applied 2026-08-28T08:15Z. **(5) The streak's cause is unchanged across ten runs and it is an intake defect, not a delivery-side one.** Both pool members reached `Ready For Dev` with their own refinement unresolved and no comment recording who moved them or why. That is a shift-left QA-authoring gap; it is recorded for assignment and is deliberately NOT invented here. **(6) One correction this run did NOT make, on purpose.** The home checkout carries an uncommitted 52.7 KB edit to this file — the 2026-08-27 refresh that was written and never committed. It belongs to another session and Critical Rule #13 / A20 forbid touching it, so this sync block was **prepended** rather than merged into it. Expect a conflict in this header when that work is committed; the body corrections above it are disjoint.)
>
> **Prior sync**: 2026-08-24 (autonomous-delivery `story` — **EMPTY STORY RUN: no story claimed, and that is the correct outcome. The corrected planning surface IS this run's deliverable.** Audited against `origin/staging` tip **`20930ab`** (PR #204). **(1) The live `Ready For Dev` pool is SIX, not the five this document has reported for three runs: BK-214, BK-219, BK-230, BK-269, BK-315 and — newly — BK-512** (8 SP, joined the pool 2026-08-23). **ZERO were eligible for an unattended run.** The per-ticket drop reasons are not restated here; the run report carries them, and the five carried-over rulings still stand. **(2) BK-512 is the strongest next pick on this board and the pool's most tractable story — and it is blocked by ONE thing only: its 🔒 mockup gate at `master-design-plan.md:647`. It is NOT blocked by refinement.** That inverts the pool's usual failure mode and is worth stating plainly, because every other candidate fails on refinement provenance: BK-512's refinement trail is **the only clean one in the pool**, and its semantics are already ratified in **`ADR-0013-workspace-deletion-semantics.md`**, which shipped *ahead* of the mockup. A ratified decision waiting on a screen is a different, and much cheaper, problem than an unrefined idea waiting on a human — clearing one mockup unblocks it. **(3) The third-consecutive-sync coverage gap is now a named mechanism, not a recurring surprise.** BK-600 and BK-601 were created by the `discovery` routine on 2026-08-24 and given §8 rows the same day via PR #204, and appeared **nowhere in this file** — exactly as BK-593/594 did on 08-22 and BK-595/596 on 08-23. Fixed here (§6 edge-mapping entry, §3 edge, §5 registry now **thirteen** with BK-601 appended). The mechanism: **`discovery` writes the §8 design row and never writes `dev-roadmap.md`** — the fix belongs in the `discovery` contract, not in the next run's cleanup. **(4) A ratification that existed was nearly re-invented, and the trap is recorded so it is not repeated.** BK-600's §8 row cites `D-UI-1` (the flat-checklist-vs-wizard adaptation on BK-35). An earlier pass this run concluded `D-UI-1` did not exist because a repo-wide grep missed it — it lives in BK-35's implementation plan under `.context/PBI/`, **a gitignored Jira cache that was in cold-clone state**. `D-UI-1` is real and was ratified per Critical Rule #15. The genuine, narrower defect was that it had never been transcribed into `master-design-plan.md` §5, which Rule #15 names as the registry — done here as **D35**, explicitly a transcription and not a new ratification, with no ADR and no change to BK-600's `✅ covered` verdict. **A repo-wide grep is not evidence of absence while `.context/PBI/` is un-hydrated.** **(5) Two structural holes closed that neither §3 nor §4 had ever carried**: the `BK-571 -> BK-601` and `BK-499 -> BK-595` edges lived only in the design plan and are now in the §3 flat edge list; and the **Team Chat (BK-210)** and **Billing (BK-224)** clusters — both carrying §3.1 edges since 2026-07-11 — got their first §4 execution-sprint rows, the same omission class ES-PAT and ES-ATC-LIB were caught in on 2026-08-18. Team Chat is blocked at its root (BK-215 is live `Estimation` with zero commits and zero branches), which is the real reason BK-219 keeps failing the pool. **(6) Design-plan line citations across this file were stale by one to five lines** because §8 rows keep being inserted above the cited targets while the quoted text stays correct; all of them were re-verified and renumbered against the post-D35 file. Expect this to recur — cite text, and treat every line number here as needing a re-read.)
>
> **Prior sync**: 2026-08-23 (autonomous-delivery `story`, session `3ee584e1` — **EMPTY STORY RUN: no story claimed, and that is the correct outcome. The deliverable is this reconciliation.** Audited against `origin/staging` tip `3548d13`; `origin/main` (`e88512e`) verified an ancestor of `staging`, release invariant holds — **staging is now 832 commits ahead of main** (820 on 08-22, 796 on 08-21) **and still no `staging -> main` release PR exists; the last one was #6, merged 2026-06-03, eighty-one days ago.** The BK-400 operator escalation that depends on a release is now **ten days** open. **(1) This document had a coverage gap it created for itself.** BK-595 and BK-596 were created by the `discovery` routine on 2026-08-23 and given `master-design-plan.md` §8 rows the same day via PR #202 — and appeared **nowhere in this file**, not in §3, not in §3.1, not in §5, not in the edge-mapping TODO. The design plan and the roadmap disagreed about which stories exist. **Fixed here**: an edge-mapping TODO entry for both, and BK-596 appended to the §5 🔒 registry, which now reads **twelve** gated stories rather than eleven. BK-595 is deliberately absent from §5 — its §8 row reads *"n/a — API-only, no mockup applicable"*, so it has no gate. **This is the second consecutive sync where the newest stories were missing** (BK-593/594 on 08-22, BK-595/596 today), which makes it a pattern, not an accident: `discovery` creates a story and writes its §8 row in the same run, and nothing in that run's contract writes the roadmap. **(2) Live Ready-For-Dev pool was 5 — BK-214, BK-219, BK-230, BK-269, BK-315 — identical to the prior run, and ZERO were eligible.** Nothing was in flight anywhere: **zero open PRs, zero `In Progress`, zero `In Review` project-wide**, and all four unmerged remote branches are non-ticket or stale (`chore/sync-boilerplate-cc65f61` is PR #154 closed-unmerged; `claude/gifted-visvesvaraya-422167` never had a PR; `feature/BK-50-...` is a docs-only dangling tip whose real commits shipped via PR #145; `pr/61` is a fetched PR ref, not a branch, and will reappear in every sweep). **(3) Not one candidate failed on git — the seventh consecutive run with zero hard tracker-vs-git contradictions among candidates.** Every ancestry check that mattered returned exit 0. The pool fails entirely on refinement provenance and design gates. **(4) All five drops were governed by BINDING rulings from the 2026-08-22 run and were followed, not re-derived** — but the two whose basis was a *factual* claim were re-verified rather than inherited, because a ruling premised on a fact stops binding when the fact changes. **BK-269**: its ratified answers commit to `pg_cron`, a Supabase Edge Function, a `runs.last_step_activity_at` column and an admin sweep route — all four re-checked today (`supabase/migrations/`, `supabase/functions/`, `app/api/**/admin`), all four **still absent**. **BK-315**: `master-design-plan.md:604` re-read today, still *"export control mockup-gated, unratified … Do not invent the control"*. Both premises hold; both rulings stand. **(5) BK-230 is dropped by §7 scope-growth, applied UPSTREAM of the decision protocol** per the 08-22 process ruling — 13 SP against a pool median of 3, committing to a Stripe integration that exists nowhere in this repo, i.e. a new external service plus a new webhook principal class. Worth recording precisely because its refinement provenance is the pool's **cleanest**: comment `12454` is a genuine third-party ratification by Carlos C, not the assignee, answering all eight questions with no `NEEDS PO/DEV` marker left. Clean refinement does not make an oversized, novel ticket claimable unattended. **(6) BK-214 now carries this routine's own one-day-old refusal as Jira comment `12524`** — an `AI Tech Lead` finding from the 08-22 run naming four defects — and the ticket **still sits at `Ready For Dev`, unchanged**. The routine's memory of a ticket it declined lives only in a comment, so the same ticket resurfaces in every pool and is re-adjudicated from scratch each time. **(7) BK-219 remains the only genuinely git-blocked candidate**, unchanged for seven runs: BK-215 is live `Estimation` with zero commits and zero branches anywhere, while its own refinement comment `11433` reads *"NEEDS PO RESPONSE … Story NOT transitioned"* — and it was transitioned anyway, with no comment explaining it. **(8) The two newest stories are not pickable for a different reason than the other five, and it is the diagnostic one: BK-595 and BK-596 are live `Backlog`, unpointed, never through shift-left.** BK-595 is otherwise the most claimable thing on the board — no mockup gate, a published contract at `api-contracts.yaml:314`, an existing backing RPC (`lib/supabase/rpc.ts:113` wraps `bunkai_get_atc`, already called on the PATCH empty-body path), a route file that already exists and exports only `PATCH`, and its declared sequence gate satisfied (BK-499's merge `f7d3bbc`, ancestry exit 0). It is blocked by nothing except never having been refined. **(9) The lever is unchanged and this is the seventh run to name it: this is an INTAKE problem, not a delivery one.** `discovery` is producing well-specified stories and `story` cannot claim them, because the two routines are separated by a shift-left step that no routine owns. Until a `/product-management` pass refines what `discovery` creates, `story` runs will keep re-adjudicating the same five-ticket residue. **(10) Operator items carried forward, deliberately NOT re-escalated to avoid diluting the escalation channel**: the missing `staging -> main` release (BK-400, ten days), the `0068_story_traceability_report_v2` migration-ledger orphan (nine days — live ledger tip is `0074_test_plan_nbsp_whitespace_class`, **next free number `0075`**), the two zero-code mis-clones sitting in `Ready For QA` (BK-467 and BK-509), the BK-315/BK-467 duplicate-summary pair, and Critical Rule #18 attribution being violated on all five pool stories.)
>
> **Prior sync**: 2026-08-22 (autonomous-delivery `story`, session `0cab73f1` — **EMPTY STORY RUN: no story claimed, and that is the correct outcome. The deliverable is this reconciliation plus an attributed readiness finding published to BK-214.** Audited against `origin/staging` tip `733a584`; `origin/main` (`e88512e`) verified an ancestor of `staging`, release invariant holds — **staging is now 820 commits ahead of main** (796 on 08-21, 780 on 08-20) **and still no `staging -> main` release PR exists; the last one was #6, merged 2026-06-03, eighty days ago.** The BK-400 operator escalation that depends on a release is now **nine days** open. Live Ready-For-Dev pool was **5** — BK-214, BK-219, BK-230, BK-269, BK-315 — and **ZERO were eligible.** **(1) Not one candidate failed on git.** Every ancestry check that mattered returned exit 0; the pool fails entirely on refinement provenance and design gates. Sixth consecutive run with zero hard tracker-vs-git contradictions among candidates. Nothing was in flight anywhere: **zero open PRs, zero `In Progress`, zero `In Review`**, and all three unmerged branches abandoned. **(2) BK-214 was the one genuinely arguable candidate and it was DEFERRED by a scored three-lens panel** (AI Product Owner / AI Tech Lead / refute-by-default skeptic; `model: opus`, non-overlapping briefs). Two lenses opposed claiming it; the third proposed a reduced scope the skeptic refuted, and **the disputed point was re-verified by the orchestrator against `customfield_10097` rather than settled by vote**: all five AC scenarios are framed on the cycle (*"When the daily digest is sent"* x2, *"When the digest cycle runs"* x2, *"when the daily digest time arrives"*), so a manually-triggered build satisfies none of them. **(3) The blocking defect is that AC4 has no target.** Scenario 4 requires landing *"in his Bunkai notification inbox"* and **there is no `/notifications` route** — the inbox is a bell + `NotificationsPanel` overlay in `components/layout/AppSidebar.tsx`, whose absence of a route is the RATIFIED D17 divergence, not an oversight. The refinement's `?next=` answer names a real mechanism with **no legal target**. **(4) Three artifacts state three different send times with no §5 divergence row** — the mockup says *"Daily at 17:00 workspace time"* (`master-design-plan.md:393`), the refinement says *"08:00 UTC"*, `workflow.md` narrates *"At 8:00"* local. Rule #15 requires ratification FIRST. **(5) The refinement is factually wrong about the shipped code in four places** — the `/notifications` target, an invented `bug.commented` event type (the live vocabulary has ten types and that is not one of them, while six real ones are omitted), the send time, and Resend's scope. Answers written without reading the code are a stronger provenance signal than their signatures. **(6) `CRON_SECRET` does not exist in `.env`**, so the scheduler cannot be authenticated without improvising a credential (A13), and `POST /api/v1/admin/send-digest` would be a **third principal class** — neither a user session nor a PAT — amending ADR-0001 rather than applying it, while pairing a privileged cross-user read with **egress to an external mailbox** where a scoping bug is neither revocable nor observable. New security posture; not adopted unattended. **(7) §7's scope-growth check is a selection gate UPSTREAM of the decision protocol.** `decision_authority.product: decide` governs whether an open question *stops* a run — not whether a candidate is *claimable unattended at all*. Conflating the two is how a run talks itself into a ticket it should not take. **(8) The pipeline has NOT stalled — this is an INTAKE problem.** The last two story runs both shipped (BK-202 on 08-20, BK-499 on 08-21). What remains is a five-story residue, each parked for its own documented reason. **The lever is a `discovery` run or a `/product-management` pass, not more story runs.** **(9) NEW, never recorded by any prior run: BK-509** reads `Ready For QA` with **zero commits and zero PRs of any kind** — the same 🚀-prefix mis-clone shape as the known BK-467, this time cloned from BK-8. Two mis-clones now sit in `Ready For QA` carrying no code. **Operator item.** **(10) Critical Rule #18 is violated on ALL FIVE pool stories** — not one refinement is attributed to an AI role profile. This is no longer a per-ticket slip, it is the house style of the refinement process. **Operator item.** **(11) The migration ledger orphan is now EIGHT days unacted-on** — `0068_story_traceability_report_v2` applied live with no file on disk (75 ledger rows vs 74 `.sql` files). Live tip is `0074_test_plan_nbsp_whitespace_class`, **next free number `0075`**, and ledger order still does not match filename order — `0058`, `0046` and `0060` are applied out of sequence, so disk and ledger agree on the tip today **by coincidence, not by construction**. **Operator item.** **(12) `acli` tooling facts, personally executed**: `workitem search --json` returns a **bare JSON array** (`.[]`), not `{issues:[...]}` as the skill body says; `--fields customfield_10036` is REJECTED outright (the allowed set is fixed at `issuetype,key,assignee,priority,status,summary`, with no widening flag), so story points and epic parent are obtainable **only** through `bun run jira:sync-issues get <KEY>`; inward issue links render as `outwardIssueKey: null`. **(13) The §7 write-the-roadmap-commit-LAST protocol was violated for the FIFTH time** by the 08-21 run (roadmap PR #192 merged 18:25Z, its story PR #193 at 19:03Z) — which is precisely why items 5-7 of this run's correction list existed at all. Full evidence in the 2026-08-22 escalation-log entry.)
>
> **Prior sync**: 2026-08-21 (autonomous-delivery `story`, session `0575a2be` — **BK-499 CLAIMED and implemented, closing the BK-262 split; the BK-201 Test Plans cluster had no §4 row at all and BK-202's ship was recorded nowhere structural.** Audited against `origin/staging` tip `3fd06c8`; `origin/main` (`e88512e`) verified an ancestor of `staging`, release invariant holds — **staging is now 796 commits ahead of main** (780 on 08-20, 755 on 08-18) **and still no `staging -> main` release PR exists**; the BK-400 operator escalation that depends on a release has now been open eight days. Live Ready-For-Dev pool was **6**: BK-499 (claimed), BK-269 and BK-315 (dependency-clear but not ready — see below), BK-214 / BK-219 / BK-230 (blocked, contradictory, or at the SP ceiling). **(1) Tracker-vs-git contradictions found: ZERO, for the fifth consecutive run.** Every actionable discrepancy was document-vs-document. The lean two-agent audit (~325k subagent tokens, down from 550k at four agents) was retained on the 08-18/08-20 recommendation and still surfaced two unrecorded ships, two wrongly-blocked dependents, a missing §4 cluster row, twelve unregistered mockup gates and nine uncovered tickets. **(2) The 2026-08-20 nomination of BK-269 is PARTLY REFUTED and it was deferred.** Its refinement answers are concrete, but they commit the story to infrastructure that exists nowhere in this repo — a `pg_cron` job, a Supabase Edge Function, a new service-role `admin` route, a new column with a write path. Verified absent: `grep -rl "pg_cron|cron.schedule" supabase/ lib/ app/` no hits, `supabase/functions` no such directory, `vercel.json` does not exist. **3 SP on paper, greenfield in practice** — and note the scope-growth check fired on the SMALLEST candidate on the board, not the largest. **(3) BK-269's decisions are published as human PO/Dev sign-off and that violates Critical Rule #18.** Its answer comments are headed `## PO Responses` / `## Dev Responses` and signed `Confirmed by: PO (Ely)` / `Confirmed by: Dev`, posted by a different account; Ely authored neither. Contrast BK-499, whose ruling is correctly headed *AI Product Owner & AI Tech Lead Ruling*. Recorded for correction, not corrected here — rewriting another actor's ticket comments is not a story run's call. **Operator item.** **(4) `bypass_actors` is NOT returned by `GET repos/../rules/branches/{b}`** — only by `GET repos/../rulesets/{id}`. Any prior run that "checked bypass" via the `rules/branches` endpoint checked nothing. Live value re-read this run: `[{actor_type: OrganizationAdmin, bypass_mode: always}]`, ruleset `16809486`, unchanged since 2026-08-06. `saiotest` is org admin, so a direct push to `staging` or `main` WOULD succeed — Critical Rules #4/#5 are the only guard. `require_extra_approval_for_unattributed_changes: true` is also live and `CLAUDE.md` §11's table, which presents itself as complete, still omits it. **(5) This project links dependencies with the `Dependencies` type (`depends on` / `is dependency for`), NOT `Blocks`/`is blocked by`** — a `blocks`-shaped JQL finds nothing here — and **`Ready For QA` carries `statusCategory = To Do` on this instance**, so reading the category as "unstarted" inverts the meaning of every dev-done ticket. **(6) `pr/61` is not a branch.** It is an orphaned remote-tracking ref from a deleted `pr` remote (`git remote -v` lists only `origin`) and it will keep appearing in every `git branch -r --no-merged` sweep. The three real unmerged branches are all abandoned: `feature/BK-50-traceability-export-snapshot` (a post-merge cache resync never PR'd), `chore/sync-boilerplate-cc65f61` (superseded by PRs #178/#179/#180), `claude/gifted-visvesvaraya-422167` (a merge of staging into itself, zero unique content). **(7) The migration ledger orphan is now SEVEN DAYS unacted-on** — `0068_story_traceability_report_v2` is applied live with no file on disk (74 ledger rows vs 73 `.sql` files); first recorded 2026-08-14, re-recorded 08-18 and 08-20. Live tip is `0073_test_plans` (`20260820182200`), next free number `0074`, and ledger order does **not** match filename order — five separate inversions. **Operator item.** Full evidence in the 2026-08-21 escalation-log entry.)
>
> **Prior sync**: 2026-08-20 (autonomous-delivery `story`, session `1d38e9c8` — **BK-202 CLAIMED and implemented; the ES-PAT and Billing clusters were both under-reporting their own shipped work.** Audited against `origin/staging` tip `67f76b3`; `origin/main` (`e88512e`) verified an ancestor of `staging`, release invariant holds — **staging is now 780 commits ahead of main and still no `staging -> main` release PR exists.** Live Ready-For-Dev pool was **6**: BK-202 (claimed), BK-269 and BK-315 (dependency-clear), BK-214 / BK-219 / BK-230 (blocked or unrefined). **(1) The previous run crashed after shipping.** It merged BK-498 (PR #186) and its own roadmap PR (#185), then died before writing another progress line, leaving a 24-hour-stale lock and its session records stranded inside a worktree. No work was lost; the records were rescued and archived. **(2) This project's post-dev Jira transition is driven by an automation rule keyed on the GitHub PR, not by the delivery run.** BK-498's `In Review -> Ready For QA` was performed by `Automation for Jira` six seconds after the merge. **A correct-looking status here proves only that some PR merged.** The steps that genuinely evidence an agent close-out are the assignee change and the handoff comment. **(3) BK-202's epic gate was stale in the BLOCKED direction** — the inverse of the usual failure. Epic BK-24 reads `Planning` while all four of its children are merged and ancestor-verified and `public.tests` has been live since `0024_tests`. A run trusting the status field would have deferred an eligible story indefinitely. **(4) Three shipped stories carried no ✅ on their §3.1 nodes** — BK-497, BK-229 and BK-205 (the last shipped 15 days ago) — so **BK-499, BK-230 and BK-232 all read as blocked while their blockers were merged.** Corrected. **(5) `acli jira workitem search --json` returns only 5 fields** (`assignee`, `issuetype`, `priority`, `status`, `summary`) with no flag to widen it, so epic, story points, links and `updated` come back BLANK and look like absent data rather than unqueried data. Story points live in `customfield_10036`; the two similarly-named fields are null on every story. **(6) The ambient shell exports a stale `ATLASSIAN_URL`** (`upexgalaxy69`) against the yaml's `upexgalaxy71`; the sync script warns and correctly prefers the yaml. Note this is the INVERSE of what a previous run was told — that run was told `.env` held the stale value, and `.env` does not contain the key at all. **(7) The `jira.upexgalaxy.com` vanity domain in generated links is ALIVE and correct** — it 302s to `upexgalaxy71`. A verifier reported it dead; one `curl` disproved it. Fourth consecutive run in which a subagent's tooling claim was false and checking cost one command. Full evidence in the 2026-08-20 escalation-log entry.)
>
> **Prior sync**: 2026-08-18 (autonomous-delivery `story`, session `dd573cf7` — **BK-497 CLAIMED and implemented; §4 gained the two execution-sprint rows it had been missing since 2026-08-13.** Audited against `origin/staging` tip `9f034700`; `origin/main` (`e88512ed`) verified an ancestor of `staging`, release invariant holds — **but staging is now 755 commits ahead of main and no `staging -> main` release PR appears in the last 40 merges.** Live Ready-For-Dev pool was **3**: BK-219 (blocked), **BK-497** (claimed), BK-498 (blocked on BK-497 by sequence). **(1) The prior run's prediction held** — splitting BK-262 produced a genuinely pickable story, and BK-497 was it: 5 SP, no incoming edge, refinement RESOLVED via BK-262's chain, no migration, no mockup gate. **(2) §4 was regenerated for the first time since the two post-MVP clusters were seeded.** ES-PAT (BK-497/498/499) and ES-ATC-LIB (BK-439/440/441) had full §3.1 edge detail but **no §4 row at all**, while §4's own header claims it is *"regenerated from the live dependency graph each run."* It was not. Both rows now exist. **(3) BK-219's deferral is upgraded from one blocker to two.** Its BK-215 dependency is unchanged (live `Estimation`, zero branches, zero commits, git-verified), but its refinement is ALSO genuinely unresolved: the QA comment of 2026-07-16 ends *"NEEDS PO RESPONSE before sprint planning. Story NOT transitioned — blockers must be resolved first."* and the only PO ratification comment **predates that blocker by five days**, answering a different set of points. The story was transitioned to `Ready For Dev` anyway. **(4) Six live tickets had zero roadmap coverage** — BK-442, BK-443, BK-465, BK-467, BK-500, BK-502 — plus **BK-492 "ELIMINAR ESTA USER STORY"**, a live parentless Story that will keep surfacing in every §6 pool query until a human deletes it. All now recorded in the edge-mapping TODO. **(5) `epic-tree.md` still listed BK-262 as `Ready For Dev` 21 pts with zero mentions of its three successors** — a `[SYNC]`-owned file, so corrected by re-running the sync, never by hand. **(6) A stale duplicate of BK-262 exists in the PBI cache** under `EPIC-BK-183-qa-defect-management/` alongside the live copy under `EPIC-BK-1-tenancy-identity/`; the sync writes a fresh folder on re-parent and never removes the old path, so globbing `epics/*/stories/STORY-BK-262-*` returns two contradictory answers. **(7) Migration ledger: live and disk agree at `0072_workspace_billing_overview`, but the live ledger still carries `0068_story_traceability_report_v2` with no file on disk** — the same drift recorded 2026-08-14 and still unacted-on. Full evidence in the 2026-08-18 escalation-log entry (§6 carries no 2026-08-18 conclusion).)
>
> **Prior sync**: 2026-08-17 (autonomous-delivery `story`, session `5fd9c9d7` — **BK-262 SPLIT by attributed ruling into BK-497 / BK-498 / BK-499; BK-262 is now `ABORTED`. No story implemented, and this is NOT an empty run.** Audited against `origin/staging` tip `18662df`. Live Ready-For-Dev pool at the start of this run was **2**: BK-219 (blocked — genuine dependency, unchanged) and BK-262 (21 SP). **(1) BK-262's four-run deferral is RESOLVED, and the fix was executing a remedy a human had already named.** Its 2026-08-14 estimation comment reads *"This Story is NOT auto-split by this session — the number is recorded as-is, and the split-or-not call is left to PO."* Verified live: **no attributed ruling on the split question existed**; the two AI rulings on the ticket cover capability vocabulary and enforcement shape only. Under Critical Rule #18 a reservation naming the *PO role* — not a specific human — belongs to the AI Product Owner profile, so it was decided rather than waited on. A joint AI PO + AI Tech Lead panel (`model: opus`, non-overlapping questions) ruled **split into three** (5 + 8 + 8 = 21, no estimate inflation), scored against five alternatives: **BK-497** (Require every API route to declare its capability posture, 5 SP, `Ready For Dev`, no incoming dependency), **BK-498** (Enforce capability scopes on the authoring domain, 8 SP, `Ready For Dev`, `depends on` BK-497), **BK-499** (Enforce capability scopes on read, identity and notification routes, 8 SP, `Backlog`, `depends on` BK-497). Rulings published as comments `12441` / `12442`, both REST-verified with headings and tables intact. **(2) The seam is the DOMAIN, not the verb** — six route files hold their GET and their write in the same file, so an "authoring writes" cut would have had the second PR re-editing lines the first finalized. **(3) NEW scope-enforcement gap found: `GET /api/v1/tokens` (`app/api/v1/tokens/route.ts:111`) has no capability check and no bearer guard**, while its two siblings hand-roll one — a PAT can enumerate its owner's other PATs but not mint or revoke one. RLS-scoped to the caller's own tokens, so it is not cross-tenant exposure; it is what BK-497 fixes. **(4) The unguarded-handler count is GROWING: 48 (08-06) -> 49 (08-11) -> 50 today**, re-measured with a parser matching every export form: 68 route files, 87 handlers, 8 public, 27 declaring a capability, 50 unguarded, 2 bypassing the gateway. **(5) BK-467 reads `Ready For QA` with zero code and is NOT the automation trap** — its QA owner posted that they created it by accident, auto-cloning the real backlog story (probably BK-315, though Jira records no link) instead of transitioning it. Recorded, deliberately not corrected. Full evidence in the 2026-08-17 escalation-log entry.)
>
> **Prior sync**: 2026-08-16 (sprint-development, BK-229 — **BK-229 CLAIMED, and two standing blockers this document carried forward from 2026-08-14/15 are CORRECTED as false.** Audited against `origin/staging` tip `f565a5a`. Live Ready-For-Dev pool at the start of this run was **3**: BK-219 (blocked, see below), **BK-229** (8 SP, epic BK-224 Billing — CLAIMED and implemented this run), and BK-262 (21 SP, still contradicting the promotion ruling — see D2 below). **(1) BK-229's "no comment trail at all / shift-left never ran" claim (2026-08-14 §6, carried forward 2026-08-15) is REFUTED.** BK-229 has a complete Shift-Left ATP dated 2026-08-13 — 17 AC scenarios, 18 ATP outlines, zero open questions, QA owner `pinto.lucas.nahuel` — visible in the ticket's comments. The two runs that reported "no comment trail" both verified with `acli jira workitem view BK-229`, which does **not** return comments — the exact trap this document's own §1 warns about (`acli view` returns null for custom fields; comments need the sync script's `--include-comments` flag). `bun run jira:sync-issues get BK-229 --include-comments` surfaces the ATP immediately. **(2) Discrepancy D2 is CLOSED as a mis-diagnosis, not left open.** No comment on BK-262 forbids its own promotion — the quoted "ruling" text (2026-08-13, requoted 2026-08-14/15) was a discovery run's record of its OWN decision not to promote BK-262 itself that day, not a standing prohibition binding every later run. The live changelog shows a human, Luis Eduardo Flores Villarroel (`6305712749a5c6754d910401`), moved BK-262 `Estimation -> Ready For Dev` on 2026-08-14T07:37:59-0300 — 99 seconds after posting a 21 SP estimation comment. BK-262 remains correctly DEFERRED (not blocked) on SCOPE: 21 SP against this project's practical ceiling for an unattended pick (13 SP), and its own estimator left the split-or-not call explicitly to the PO, still open. That is a real, separate reason to defer it — but it is a scope call, not a violated ruling, and D2 as previously stated was wrong. **(3) BK-219's entry is corrected**: its blocker, BK-215, is live status `Estimation` — not merely "no branch/commit" as the 2026-08-15 entry stated (still true, but incomplete) — with zero branches and zero commits anywhere in git history, git-verified this run (`git branch -a`, `git log --all --grep="BK-215"` both empty). Full detail in §6's 2026-08-16 conclusion.)
>
> **Prior sync**: 2026-08-14 (autonomous-delivery `story` — **live pool was 3, not the 2 this document predicted, and BK-337 was CLAIMED after its two-run deferral was OVERTURNED.** Audited against `origin/staging` tip `e10dcb3`; `origin/main` verified an ancestor of `staging` (staging 701 ahead), the release invariant holds. **Migration ledger high-water mark, queried live: `0069_story_traceability_module`** (`20260812182631`) — next free number `0070`; local `supabase/migrations/` agrees at the tip, but the **ledger carries a `0068_story_traceability_report_v2` entry with no file on disk** (recorded, not acted on). The 2026-08-13 entry mandated an AUDIT of BK-337's deferral reason rather than a third restatement; a 4-lens scored panel (`model: opus`) delivered it and returned **7 / 7 / 8 / 9 — unanimous claim**. Both premises failed: the "whole new read surface (route + handler + RPC)" claim was **never verified even once** (asserted 2026-08-12 with no code recon, inherited verbatim 2026-08-13) and is false on two of three legs — `bunkai_bug_json` already exists, is already granted, and the Tech Lead's own TQ2 says *"no new RPC is needed at all"*; and "Story Points empty" is a rule invented once for one ticket against a project-wide default where 60+ stories read `-` and where `.agents/jira-required.yaml` FORBIDS an unattended run from filling the field. Scope-growth: **one hit of six**, and that hit (`create or replace` on a live object) is the exact shape BK-48 shipped unattended two days earlier. Sized at **5 SP**. **Two candidates appeared that this document did not know existed**: **BK-262** (`Ready For Dev`, **21 SP**) — NOT claimable, a published 2026-08-13 ruling forbids it (*"Promoting it would hand `story` mode an unrefined, oversized ticket"*) and its promotion **contradicts that ruling**, flagged for the operator; and **BK-229** (`Ready For Dev`, 8 SP, epic BK-224 Billing) — NOT claimable, **shift-left refinement never ran** (no comment trail at all), and its epic is documented as *"deliberately parked Backlog, post-MVP"* at `master-design-plan.md:448`, which the live status contradicts. **BK-439/440/441 are all still `Backlog`** — shift-left has not run on any of them, exactly as the 2026-08-13 handoff predicted. Full evidence in §6's 2026-08-14 conclusion.)
>
> **Prior sync**: 2026-08-13 (autonomous-delivery `story` — **live pool was 2 (BK-267, BK-337), and neither is implementable by an unattended run this cycle — one because it was oversized and got split by published ruling, the other because its deferral reason is now itself due for an audit.** Audited against `origin/staging` tip `b9f3fc69`; `origin/main` verified an ancestor of `staging`, the release invariant holds. **Migration ledger high-water mark, queried directly against the live ledger: `0069_story_traceability_module`** (version `20260812182631`) — this corrects the 2026-08-12 entry below, whose "unchanged at `0068`" claim was already wrong at that entry's own audit timestamp; see the struck clause there. **BK-267** (`TMS-ATC Library | Browse, search, and filter ATCs across every project`, epic BK-13, recorded at **1 SP** while carrying **14 AC blocks / 19 Gherkin scenarios**) was found by an AI Product Owner ruling (Jira comment `12315`) and an independent AI Tech Lead ruling (comment `12316`) to be sized at **~10-11 SP**; both rulings mandated a three-way split, and comment `12317` closed it. **BK-267 is now `ABORTED`** (not deleted), replaced under epic BK-13 by **BK-439** (`Browse every ATC in the workspace from one index`, 5 SP, no incoming edge, independently shippable) **-> BK-440** (`Find an ATC by name as you type`, 3 SP) **-> BK-441** (`Narrow the index by Project, Module, layer and anchor`, 3 SP) — full edge detail in §3.1's new BK-13 addendum below. The global ATC Library mockup this cluster inherits already existed (`bk-13-atc-library-global/atc-library-global.html`, committed `927b62f1`, 2026-07-30) — **no new mockup gate opened**, unlike BK-371's §5 🔒 row. Canonical route ruled `/atcs` (workspace-level), already named in `BRIEF.md:50` and `master-design-plan.md:285`. `master-design-plan.md` §8 gained three US→Screen rows for the successors (BK-267 never had one — a genuine coverage gap, now closed) and §5 gained divergence **D31**, narrowing D18's `soon`/non-focusable rule so the "ATC Library" sidebar entry alone goes live once BK-439 ships; "Test Runs", "Bug Reports" and "Metrics" are unchanged. **BK-337 was deferred again — the SECOND consecutive run to drop it for the same reason** (unsized, Story Points still empty; still a whole new read surface). Per the codified BK-45 lesson — *"when a candidate is dropped for the same reason two runs running, audit the reason itself, not the ticket"* (BK-45 sat under a wrong deferral for three runs before a 4-lens panel overturned it) — **BK-337's deferral reason is now due for a fresh audit on the next run**, not a third inherited restatement.)
>
> **Prior sync**: 2026-08-12 (autonomous-delivery `story` — **the 2026-08-11 sync's "the pool is 0, not 1" claim was wrong. Live pool is 2, not 0, and this run claimed one of them, ending a three-run empty streak (2026-08-09 through 2026-08-11).** Audited against `origin/staging` tip `84e7409` and `origin/main` tip `e88512e`; `main` remains an ancestor of `staging`, the release invariant holds; migration ledger high-water mark ~~unchanged at `0068`~~ **corrected 2026-08-13 — the live ledger's actual high-water mark was already `0069_story_traceability_module` at this audit's own timestamp (BK-48's own migration, applied this same run); this entry undercounted by one migration, not caught until the next run queried the live ledger directly**, local `supabase/migrations/` files agree exactly, nothing applied this run. Live Ready-For-Dev pool: **BK-48** (`TMS-Traceability | Filter the chain by verdict, module, and date range`, epic BK-44 Coverage & Traceability, 5 SP) and **BK-337** (`TMS-Defect Detail | Open a defect and read its full record`, epic BK-31 Bugs & Defect Heatmap, Story Points empty). Full live distribution (95 stories, up from 93): Backlog 28, Ready For Release 24, Ready For QA 20, QA Approved 14, Shift-Left QA 3, Ready For Dev 2, Estimation 2, In Test 1, ABORTED 1. **BK-48 was CLAIMED and implemented this run** — the first pick since BK-50 on 2026-08-08. Its Jira `Dependencies` link onto **BK-30** (Epic, status `Planning`) was FOLLOWED as precedent, not re-derived: the 2026-08-05 sync already ruled the identical shape of claim for BK-211 — "BK-30's child stories are all shipped; the epic's `Planning` status is stale bookkeeping, not a functional gate" (line below) — and epic BK-44 itself currently reads `Planning` while its own children BK-45 and BK-50 are `QA Approved` and genuinely shipped. **BK-337 was DEFERRED, not blocked.** It sits at `Ready For Dev` with Story Points empty — transitioned without passing estimation — and it is a whole new read surface (route + handler + RPC, 17 AC scenarios). Unsized plus new-surface is a scope-growth signal this run declined to auto-claim; it needs an estimation pass or a human-present session. **BK-337 is still not shipped as code**: PR #152 (`ff8b79b`) remains docs-only by diff, so BK-372's inherited BK-337 dependency edge (§3.1) is still genuinely unsatisfied. **Two new stories surfaced, absent from this doc entirely**: BK-398 (`Command Palette | Search and jump across the workspace`, `Shift-Left QA`) and BK-399 (`TMS-ATC Classification | Classify by test-design technique and priority`, `Backlog`), both created 2026-08-12 by the discovery routine — neither has a `master-design-plan.md` §8 row yet; recorded in the edge-mapping TODO below. Full evidence in §6's 2026-08-12 conclusion.)
>
> **Prior sync**: 2026-08-11 (autonomous-delivery `story` — **empty run, and the pool is now 0, not 1.** Ruling `12170` — the BK-43a/b/c split mandate this doc has been recording as unowned since 2026-08-08 — was **executed** in PR #156, merge `0a89b36`. **BK-43 is now `ABORTED`** — the eight-run deferral streak did not end because BK-43 became buildable, it ended because BK-43 stopped existing as a pickable ticket. It is replaced by three successors under epic BK-31, sequenced **BK-371 -> BK-372 -> BK-373** (3 SP each): **BK-371** (TMS-Defect Sync | Point a project at a Jira destination, no incoming dependency), **BK-372** (TMS-Defect Sync | Send a newly filed defect to Jira, inherits BK-43's old BK-40 ✅ and BK-337 Dependencies edges), **BK-373** (TMS-Defect Sync | Recover a failed sync and show its state, gated on BK-372). All three are live status **`Backlog`** and **none has been through shift-left QA refinement** — the ordinary kind of blocker, not the unowned-administration kind, but now blocking 100% of the pool instead of one candidate. **BK-337 is still not shipped**: PR #152 (`ff8b79b`) remains docs-only by diff, so BK-372's inherited BK-337 edge is genuinely unsatisfied. Live Ready-For-Dev pool = **0 of 93** (Backlog 28, Ready For Release 24, Ready For QA 20, QA Approved 14, Estimation 3, Shift-Left QA 2 [BK-337, BK-262], In Test 1 [BK-41], ABORTED 1 [BK-43]). Migration ledger high-water mark `0068`, no drift. `main` still an ancestor of `staging`. Full evidence in §6's 2026-08-11 conclusion.)
>
> **Prior sync**: 2026-08-10 (autonomous-delivery `story` — **empty run, correctly, for the eighth consecutive time — but the blocker changed shape.** Live Ready-For-Dev pool = **1 story, BK-43**, unchanged. The 2026-08-09 ruling was **followed, not re-derived**: that entry closed by instructing run 8 not to re-litigate it, so no panel was dispatched and the deferral stands on sizing ruling `12170`. **What is new: BK-43 now carries a SECOND Dependencies link — BK-337 `TMS-Defect Detail`**, created 2026-08-10 by the discovery routine, live status `Shift-Left QA`. BK-337 *is* gap #5 of the five the prior panel verified — the missing bug-detail route that makes AC-4's backlink unaddressable — so the hole now has a ticket and a real edge, recorded in §3.1. **BK-337 is NOT shipped**: its PR #152 is docs-only (PBI cache + a design §8 screen-map row), no route, no handler, no RPC — verified by diff, not by status. So BK-43 is blocked twice: once by an ordinary git-verified dependency, once by the unowned ticket-split administration. **Three roadmap defects corrected**, all the same shape — §4 and §6's side-tables failing to notice what §6's own pool table already recorded: §4's ES5 row survived its whole cluster draining (BK-209/211/212/213 all shipped; only BK-214 left), §3.1 withheld the ✅ from BK-211/BK-213 while giving it to their siblings, and §6 carried **two** stale BK-209 rows demanding a human "go" on a merged story. **Escalated to the operator this run** after seven prior recordings failed: nothing is chartered to run the ten-minute `/product-management` pass that materializes BK-43a/b/c. Full evidence in §6's 2026-08-10 conclusion.)
>
> **Prior sync**: 2026-08-09 (autonomous-delivery `story` — **empty run, correctly.** Live Ready-For-Dev pool = **1 story, BK-43**, for the seventh consecutive run. It was re-verified rather than deferred by inheritance: a 3-lens scored panel returned **8/100 unanimously**, so the deferral stands — but **its recorded reason was refuted and is struck through in §4 and §6.** BK-43 needs **no** per-workspace secrets model; ruling `12177` D6 forbade one on 2026-08-05, three days before this document claimed the opposite. The real blockers, re-verified in code at migration `0068`: no destination-config table, no sync columns on `bugs`, no outbound authenticated *write*, no time-based retry primitive, and **no bug-detail route — which makes AC-4's backlink unaddressable, not merely unbuilt.** Also corrected here: §4's ES4 row still called BK-50 open work while §6 called it shipped. **Single unblock: `/product-management` materializes BK-43a/b/c per comment `12170` and re-parents the 14 ATCs.** Full evidence in §6's 2026-08-09 conclusion.)
>
> **Prior sync**: 2026-08-08, amended later the same run (autonomous-delivery `story` — **BK-50 SHIPPED.** PR #145 merged 2026-08-08T19:03Z, merge commit `7b16c0c`, ancestor-verified; live status `Ready For QA`, assigned to the shift-left owner. **The "deferred on scope-growth" verdict recorded in the same run's earlier sync (PR #144) was overturned by that run's own Rule #18 decision pass** — comments `12238` (AI Tech Lead) and `12239` (AI Product Owner) scored five options and ruled **Option E, 103 to a runner-up of 67**: a client-initiated download of a self-contained HTML document, rendered synchronously from BK-45's already-shipped authenticated route. **No object storage, no hosted artifact, no anonymous surface, no `export_jobs` table, no cron, no new dependency, no migration.** The deferral was right about the *ratified plan* and wrong about the *story*; the decisive evidence was that **the mockup post-dates comment `11047` by twenty days and contradicts it** (download icon `traceability-chain.html:536`, composed filename `:1021`, toast `:859-866`, no link control anywhere), and under Critical Rule #15 the mockup governs. Divergence row **D26** logged in `master-design-plan.md` §5 for the `.json` -> `.html` filename departure. **Process failure worth naming**: PR #144 was merged declaring BK-50 deferred *while the decision pass that would overturn it was still running* — the very rule that sync recorded, tripped within the hour. The rule is widened below. Everything in the earlier 2026-08-08 sync stands except that verdict.)
>
> **Prior sync**: 2026-08-08 (autonomous-delivery `story` run audit — **BK-45 SHIPPED**, not merely "claimed": PR #142 merged 2026-08-07T19:10Z, merge commit `f75709e`, ancestor-verified. The 2026-08-07 sync (PR #141, `f59c095`) was authored *before* that merge landed and never picked it up — the same under-reporting-its-own-day's-outcome gap flagged for BK-213 on 2026-08-04/05. Consequently **BK-50's dependency gate is RELEASED**, and every row calling it "blocked on BK-45" was stale. **BK-20's named blocker is resolved**: defect BK-187 merged 2026-08-06T20:08Z (`c0712e9`) — the §4 row still demanded its resolution. **BK-50 deferred on scope-growth, and its ratified plan is factually broken**: PO comment `11047` justified choosing Cloudflare R2 with *"R2 already exists in our stack"* — a recon of `origin/staging` found **zero object storage of any kind** (no R2, no `@aws-sdk`, no `.storage.from(`, no `@vercel/blob`, no bucket DDL in 68 migrations, no storage credentials), and **zero export precedent** (no CSV/PDF/`Content-Disposition`/`@media print`/download route/share link). Same failure shape as BK-43's superseded `secrets_ref` claim. See the 2026-08-08 run conclusion in §6. Carries forward the 2026-08-07 sync below.)
>
> **Prior sync**: 2026-08-07 (autonomous-delivery `story` run audit — **BK-45 claimed**, overturning a four-run "scope-growth" deferral on a 4-lens scored panel (A=293 / C=210 / B=107): its BK-31 dependency gate is bookkeeping that sibling stories BK-46/BK-47 already crossed on 2026-08-01, its 8-point estimate predates filters and export being carved into BK-48/BK-50, and its "not resolved" verdict counted 2 comments when live Jira has 4 — `12171` and `12176` decided all 11 open questions on 2026-08-05. **BK-211 reconciled**: the prior sync left it gate-stopped at an unmerged PR; PR #137 in fact merged 2026-08-06T22:31Z (`861c441`) with migrations `0066`+`0067` applied and ledger-verified, status `Ready For QA` — the operator's `migrations: unrestricted` change (PR #139) opened the gate the same evening. **BK-43's deferral reason corrected** — it stands, but "no connector/retry pattern exists in this repo" was false. **New cross-cutting finding**: the step-0 actor bind in every report RPC is inert on the admin-client call path, so per-CTE `project_id` scoping is the only real control. Carries forward the 2026-08-06 sync below.)
>
> **Prior sync**: 2026-08-06 (autonomous-delivery `story` run audit — **BK-211 delivered** to PR #137 after three runs of exclusion; its Q3 was ratified live 2026-08-05 19:08Z/19:27Z, ~18 min after the prior run ended, and the "hard-blocked on BK-30" claim is now falsified at code level rather than merely stale. **BK-205 reconciled**: SHIPPED via PR #132 (`5054716`) on 2026-08-05 at 20:20Z — the prior run's 18:40Z escalation was overtaken by a later session, not overruled; its §6 row is kept as history and marked superseded. **BK-188 dropped out of the pool** — now `Tech Story`/`Completed`, the prior runs' re-type recommendation was actioned. **`ATLASSIAN_URL` defect found**: a stale shell export pointed every `jira:sync-issues` run at the dead `upexgalaxy69` instance, so the PBI cache showed 3 of BK-211's 7 comments — see the 2026-08-06 run conclusion in §6. Carries forward the 2026-08-05 sync below.)
>
> **Prior sync**: 2026-08-05 (autonomous-delivery `story` run audit — **BK-213 reconciled**: §6 still recorded it only as "Claimed by the 2026-08-04 run"; it merged to `origin/staging` that same day via PR #127 (`2e91ad95`) and is ancestor-verified. The 2026-08-04 reconciliation PR #126 was cut *before* #127 landed and was never followed up, so the doc under-reported its own run's delivery. **BK-147, BK-148 and BK-265** found merged to `origin/staging` [PR #43, PR #49, PR #118] but never added to this doc at all — added as coverage gaps below, same class as the 2026-08-04 BK-47/BK-266 finds. **BK-205** added to §6: it entered the live `Ready For Dev` pool since the last sync and was judged NOT eligible for an unattended pick — see its row. **BK-211's** stale "hard-blocked on BK-30" claim corrected — BK-30's child stories are all shipped; the epic's `Planning` status is stale bookkeeping, not a functional gate. Carries forward the 2026-08-04 sync below.)
>
> **Prior sync**: 2026-08-04 (autonomous-delivery `story` run audit — BK-212 reconciled: §6 wrongly still listed PR #115 as `Open`/`In Review`; it merged to `origin/staging` on 2026-08-04 and is ancestor-verified. BK-255/256/257/258/259/260 (Home Dashboard cluster) all merged to `origin/staging` today via PRs #120-125 — this doc previously listed them `Backlog`/gate-cleared-but-unclaimed; corrected below. BK-47 (Time-to-Green) and BK-266 (Projects index) were found merged to `origin/staging` [PR #98, PR #119] but had never been added to this doc at all — added as coverage gaps. BK-213 claimed this run — see §6. Carries forward the 2026-08-03 BK-46/BK-49 reconciliation, the 2026-08-02 BK-42 ship, the 2026-08-01 autonomous-delivery `story` run + interactive BK-41 delivery, and the 2026-07-31 surgical Home Dashboard epic BK-254 addition; rest of the graph not re-sorted end to end)
> **Maintained by**: hand-authored synthesis. Live story status is **never frozen here** — it is queried on demand (see §6). See §7.

---

## 1. How to read this — authority split

> **TL;DR for the AI/human reading this — what this doc is, in one breath:**
> This doc answers **one question only: "which Jira ticket do we work next, and what is blocking it?"** It is NOT the strategy plan and it does NOT store live status.
> - Want **"why are we building epics in this order?"** → that is `master-implementation-plan.md` (strategy / epic altitude). Not here.
> - Want **"what ticket is next + what unblocks what?"** → **you are in the right doc** (§3 graph, §4 execution sprints, §5 mockup-gates). This is the part nothing else holds.
> - Want **"is BK-28 done yet / what's its current status?"** → **do not read it off this page.** Status changes daily and is never written here. Query it live with the recipe in **§6**.
>
> One-line rule: **§2–§5 are durable truth you can trust; live status is always a query, never a paste.**

This doc sits in a 3-layer roadmap stack. Each layer owns a different altitude:

```
.context/master-implementation-plan.md   EPIC / strategy   "what to build & why that order"   (derived from business-maps)
.context/dev-roadmap.md  ◄── THIS DOC     TICKET / sequence "what unblocks what, in what execution sprint"  (derived from Jira links + local design)
.context/PBI/.../implementation-plan.md   STORY / files     "how to build one ticket"           (derived by /sprint-development)
```

**Authority split — who is the source of truth for what:**

| Fact | Source of truth | Why |
|------|-----------------|-----|
| **Dependency edges** (A blocks B), execution sprints, mockup-gates | **THIS doc (local)** | Local design context (master-design-plan §8, schema topology, readiness audits) holds the *full* structure; Jira issue-links are sparse/partial. |
| Story **status** + **points** | **Jira** (snapshot in §6, dated) | Jira is live; §6 is a frozen photo — re-sync before trusting it. |
| Epic→story membership | Jira (`epic-tree.md` mirror) | Sync-owned. |
| Per-story file plan | `/sprint-development` | Out of scope here. |

Legend (used in §3–§6):
- `A ──> B` = A must be **dev-done** before B can start (hard gate).
- `A ··> B` = soft gate: B is buildable without A but low-value / partial until A lands.
- 🔒 mockup-gated = cannot start until the named mockup exists (Critical Rule #15 fidelity).
- ✅ dev-done · 🟢 ready · 🟡 gated · ⚪ backlog/estimation · 🧪 in QA/test.

---

## 2. Epic backbone — the schema-forced spine

Order below is **forced by entity topology** (`business-data-map.md §2`), not preference: no entity ships before the entities + RLS it depends on. This is the epic-level skeleton; §3 is the ticket-level detail hanging off it.

```
BK-1  Tenancy & Identity ──┬──> BK-7  Project & Module Hierarchy ──┬──> BK-12 User Stories & AC ──┐
   (workspace_id + RLS)    │        (project → module tree)        │                              ├──> BK-13 ATC Library
                           │                                       └──> BK-13 ATC Library ◄────────┘     (the differentiator)
                           │                                                  │
                           │                                                  └──> BK-24 Tests (chains of ATCs)
                           │                                                            │
                           │                                                            └──> BK-30 Manual Execution & Runs
                           │                                                                       │
                           │                                                                       └──> BK-31 Bugs & Defect Heatmap
                           │
                           ├──> BK-85 Account & Settings   (parallel once BK-1 done)
                           └──> BK-29 QA Credentials        (parallel, /qa page)

   BK-44 Coverage & Traceability ── reads ALL of the above (US→AC→ATC→Test→Run→Bug chain); last MVP epic.

   BK-254 Home Dashboard ── reads BK-30 (Runs), BK-31 (Bugs), BK-44 (Coverage) + BK-7/BK-13 (Projects/ATCs) as a landing-page aggregation; no new entities of its own.
```

| Epic | Title | Depends on | Phase |
|------|-------|-----------|-------|
| BK-1 | Tenancy & Identity (RLS) | — (foundation) | MVP S1 |
| BK-7 | Project & Module Hierarchy | BK-1 | MVP S2 |
| BK-12 | User Stories & AC | BK-7 | MVP S2/3 |
| BK-13 | ATC Library | BK-7 (+BK-12 for AC anchor) | MVP S3 |
| BK-24 | Tests (chains of ATCs) | BK-13 | MVP S4 |
| BK-30 | Manual Execution & Runs | BK-24 | MVP S4 |
| BK-31 | Bugs & Defect Heatmap | BK-30 | MVP S5 |
| BK-85 | Account & Settings | BK-1 | MVP (parallel) |
| BK-29 | QA Credentials (`/qa`) | BK-1 | MVP (parallel) |
| BK-44 | Coverage & Traceability | BK-13/24/30/31 | MVP S6/7 |
| BK-201 | Test Plans & Milestones | BK-24, BK-30 | Post-MVP P1 |
| BK-221 | Automation & CI Ingestion | BK-30 (BK-34/35/38/39), BK-88, BK-24 | Post-MVP P1 |
| BK-208 | Notifications Center | BK-30 (BK-36/39), BK-31, BK-87 | Post-MVP P1 |
| BK-210 | Team Chat | BK-1, BK-7, BK-208 (BK-209) | Post-MVP P2 |
| BK-224 | Billing & Plans | BK-1, BK-87 | Post-MVP P2 |
| BK-254 | Home Dashboard | BK-30, BK-31, BK-44 (reads across all three; no new entities) | MVP tail / P2 (seeded 2026-07-31) |

> **Post-MVP expansion epics (created in Jira 2026-07-11)** — 5 epics / 28 stories authored future-first (their ACs assume the full MVP chain is shipped). All stories sit in `Backlog` (deliberately NOT `Shift-Left QA` — they are roadmap stories, not refinement-ready), ~~all are 🔒 mockup-gated~~ mockups ✅ shipped 2026-07-30 (see §5), story points intentionally empty. **PO defaults ratified 2026-07-11** (delegated to AI-as-PO): all flagged Business-Rules decisions resolved — see the `## PO Ratification — 2026-07-11` Jira comment on each affected story; domain-glossary §3 gained the 17 new entity terms; master-design-plan §8 has the 28 US→Screen rows. Remaining pre-dev gate: mockups only. Full dependency edges live as Jira `Dependencies`/`Relates` links (49 links, direction-verified); §3.1 holds the compact summary. **Coming-soon epics (deliberately NOT created yet)**: Dashboards & Analytics, Data Import/Export, Entity Comments & Review — promote via `/product-management` when the frontier approaches.
>
> Phase-2 / Phase-3 epics (self-hosted bundle, agentic protocol, 3D mind-map, SSO, marketplace) remain **strategy-layer only** — see `master-implementation-plan.md §2`. Note: the former "CI adapters" strategy item is now partially ticket-broken as BK-221.

---

## 3. Story dependency graph — the core

The active frontier (Sprint-2 dev). This is the part Jira cannot express as a roadmap; it is the reason this doc exists.

```
                              ┌──────────────────────────────────────────────┐
   BK-27 Test Builder ✅ ─────┤ (dev-done — gate RELEASED, unblocks 7)        │
   (chain ATCs, 8 SP)         └──┬───────────────────────────────────────────┘
                                 ├──> BK-28 Test Reorder        (5)
                                 ├──> BK-33 Test Tags ✅         (8)   (dev-done — QA Approved)
                                 ├──> BK-22 ATC Usage report    (3)
                                 ├──> BK-23 ATC Duplicate       (5)
                                 ├──> BK-32 Test view expanded  (1)
                                 ├··> BK-21 ATC Propagation ✅  (5)     (dev-done — merged to staging, Ready For QA; contract ratified, ADR-0009)
                                 └──> BK-34 Start manual run ✅  (8) ──┬──> BK-35 Mark step pass/fail (5*) ⚪ Estimation
                                   (dev-done — gate RELEASED)          ├──> BK-36 Abort run ✅        (8)   (dev-done — merged to staging PR #59, Ready For QA)
                                                                       ├──> BK-37 Run history ✅      (5)   (dev-done — PRs #65+#66 merged to staging, Ready For QA; est. 1→5 by PO 2026-07-21)
                                                                       ├──> BK-38 Filter proj runs   (3) 🟢 (mockup ✅ 2026-07-30)
                                                                       └──> BK-39 Finish run verdict (5) 🟢

   INDEPENDENT (no BK-27 gate — parallel-safe):
     BK-20 ATC Search 🧪      BK-3 OAuth (8) 🟢 (AC synced)      BK-19 ATC Builder ✅   BK-18 ATC API ✅

   SETTINGS CLUSTER (epic BK-85):
     BK-86 Account/sign-out ✅ ──> BK-87 Settings hub (2) ✅ ──┬──> BK-88 Manage PATs (5) 🟢
        (dev-done — QA Approved)   (dev-done — Ready For QA)   └──> BK-89 View workspaces (2) 🟢 ──> BK-90 Leave workspace (5) 🟢
                                    └─ Settings mockup ✅ 2026-07-30 (§4.10, bk-85-account-settings/ 5-screen suite) — gate lifted for the whole cluster
```

### Flat edge list (the durable contract — validate against Jira §7)

| Blocker (must be dev-done) | Unblocks | Type | Reason |
|----------------------------|----------|------|--------|
| BK-27 Test Builder | BK-28, BK-33, BK-32 | hard | reorder/tags/view all operate on a Test's `test_steps`, created by BK-27 |
| BK-27 | BK-22 | hard | "Used in N tests" count reads `test_steps` rows |
| BK-27 | BK-23 | hard | duplicating an ATC must account for its test memberships |
| BK-27 | BK-21 | soft | propagation cascades edits *to tests*; pointless before tests exist |
| BK-27 | BK-34 | hard | a run executes a Test; no Test → no run |
| BK-34 Start manual run | BK-35, BK-36, BK-37, BK-38, BK-39 | hard | all operate on a `run` / `run_atc` / `run_step` row created by BK-34 |
| BK-34 Start manual run | BK-148 Project Environments | hard | env CRUD operates on the `project_environments` table + `runs.environment_id` FK created by BK-34 — **Relates BK-34 (gate satisfied: BK-34 merged to staging)** |
| BK-86 Account | BK-87 Settings hub | hard | BK-87 owns the topbar entry point BK-86 renders into |
| BK-87 Settings hub | BK-88 PATs, BK-89 Workspaces | hard | both are Settings sub-views; need the hub shell |
| BK-89 View workspaces | BK-90 Leave workspace | hard | leave action lives in the workspaces list + needs its active-workspace contract |
| BK-40 File defect from a failing run step | BK-258 TMS-Home \| Show open bug count and severity breakdown | hard | a queryable open-bug count needs bugs to exist first — BK-40 is the write path that creates them |
| BK-41 List and filter defects by module/status/severity | BK-258 TMS-Home \| Show open bug count and severity breakdown | hard | the severity breakdown needs a severity-filterable read surface, which BK-41 is the first to build |
| BK-46 Surface untested ACs/modules with not-run filter ✅ dev-done (merged to staging, PR #93 + 4-slice chain, ancestor-verified 2026-08-03) | BK-259 TMS-Home \| Show workspace test coverage summary ✅ **SHIPPED 2026-08-04** (PR #125) | hard | computing an overall coverage % needs the tested-vs-untested computation BK-46 builds first; no other Coverage story computes it. Both sides of this edge are now merged to `origin/staging`. |
| BK-571 Archive an ATC and restore it from the archive | BK-601 See what is archived and restore an archived module | hard | BK-601 REUSES BK-571's archive/restore vocabulary rather than inventing a second one; Jira `Dependencies` link, direction verified (BK-601 sequenced AFTER BK-571). Declared in `master-design-plan.md:590`, never in this table until 2026-08-24. **Gate NOT satisfied**: BK-571 is unshipped and itself 🔒 mockup-gated (§5). |
| BK-499 PAT read/identity/notification enforcement ✅ dev-done (merge `f7d3bbc`, `--is-ancestor` exit 0, re-verified 2026-08-24) | BK-595 Read a single ATC by id | hard | BK-595's §8 row (`master-design-plan.md:608`) declares a sequence AFTER BK-499. **Gate SATISFIED.** Declared in the design plan, never in this table until 2026-08-24. BK-595 stays unclaimable for an unrelated reason: live `Backlog`, unpointed, never through shift-left. |

**No incoming edge (start anytime, gated only by their own readiness):** BK-20, BK-3, BK-86, BK-255 (TMS-Home welcome banner) ✅ **SHIPPED 2026-08-04** (PR #120), BK-256 (TMS-Home active runs — reads BK-30, already dev-done in this graph) ✅ **SHIPPED 2026-08-04** (PR #122), BK-257 (TMS-Home recent projects) ✅ **SHIPPED 2026-08-04** (PR #121).

**Soft / informational only (not a sort-blocking edge):** BK-260 (TMS-Home condensed activity feed) — Jira `Relates` to BK-49 (Activity stream). BK-49's `feat/BK-49-activity-stream` branch (PR #83 + its 3-slice chain) is merged into `origin/staging`, ancestor-verified 2026-08-03 — BK-260 reuses BK-49's endpoint as a thin presentation layer. BK-260 itself ✅ **SHIPPED 2026-08-04** (PR #123).

### 3.1 Post-MVP expansion cluster — compact edge summary (full links live in Jira)

Created 2026-07-11. `A ──> B` = A depends on B (hard, Jira `Dependencies`); `··>` = soft (Jira `Relates`). ~~All 28 stories 🔒 mockup-gated (§5)~~ **Mockup gates LIFTED 2026-07-30** (§5 — every post-MVP screen set designed). The stories remain `Backlog` by deliberate roadmap decision (post-MVP frontier), not by design gate.

```
BK-201 Test Plans & Milestones:
  BK-202 Create plan ──> epic BK-24 (**gate SATISFIED** — children BK-27 `54749ba`, BK-28 `d4fb8e2`, BK-32 `2f509e4`, BK-33 `856a613` ALL merged + ancestor-verified; the epic's own `Planning` status is stale bookkeeping, same shape already ruled on for BK-211 and BK-48) ✅ **dev-done (2026-08-20)** — merged to staging via PR #188, merge `5fddd4c`, ancestor-verified; migration `0073_test_plans` applied live ─┐   BK-205 Create milestone ──> (free) ✅ dev-done (2026-08-05) — PR #132, merge `5054716`, ancestor-verified
  BK-203 Add/remove tests ──> BK-202 ✅ — **UNBLOCKED 2026-08-20** (mockup ✅ `plan-detail.html`) │   BK-206 Assign plans ──> BK-205 ✅, BK-204 (still blocked — BK-204 has no commit on staging)
  BK-204 Track progress ──> BK-203, epic BK-30
  BK-207 Close plan ──> BK-202 ✅ — hard edge RELEASED 2026-08-20; still soft-gated on BK-204 (no commit on staging)  ··> BK-204

BK-221 Automation & CI Ingestion:
  BK-222 Submit automated run ──> BK-88, BK-34, BK-39        (entry point of the cluster)
  BK-223 Stream step results ──> BK-222, BK-88, BK-34, BK-39  ··> BK-35
  BK-226 Upload CI file ──> BK-222, BK-88
  BK-228 Commit/branch links ──> BK-226
  BK-225 Mode filter ──> BK-38  ··> BK-222, BK-227
  BK-227 Automation status ──> BK-27

BK-208 Notifications Center:
  BK-209 Inbox ✅ dev-done (2026-08-03) — merged to staging via PR #113 (feat/BK-209-notifications-inbox); notifications table+RLS+RPC, inbox API, bell/panel UI, realtime wiring. Ready For QA.
  BK-211 Run events ✅ dev-done (2026-08-06) — merged to staging via PR #137 (feature/BK-211-run-terminal-notifications), merge commit 861c441, ancestor-verified. Depended on BK-209 ✅, BK-39 ✅, BK-36 ✅.
  BK-212 Bug events ✅ dev-done (2026-08-03) — merged to staging via PR #115 (feat/BK-212-bug-notifications); activity_log-driven notify trigger + inbox rendering + deep link into run detail. Depended on BK-209 ✅ and BK-264 ✅ (added below — the epic BK-31 edge resolved to a concrete prerequisite story once planning found bug assignment/status-transition had no ticket at all). Ready For QA.
  BK-213 Preferences ✅ dev-done (2026-08-04) — merged to staging via PR #127 (feature/BK-213-notification-preferences), merge commit 2e91ad9, ancestor-verified. Depended on BK-209 ✅, BK-87 ✅.
  BK-214 Email digest ──> BK-209 ✅  ··> BK-213 ✅        (only unshipped story left in this cluster)

BK-31 Bugs & Defect Heatmap (addendum, 2026-08-03):
  BK-264 TMS-Defect Triage (assign + status transition) ✅ dev-done (2026-08-03) — merged to staging via PR #114 (feat/BK-264-defect-triage); discovered mid-sprint as the missing prerequisite for BK-212 (bugs table had no assignee column and no status-transition write path). Linked as a Dependencies predecessor of BK-212 in Jira. Ready For QA.

BK-31 Bugs & Defect Heatmap (addendum, 2026-08-10):
  BK-337 TMS-Defect Detail (open a defect, read its full record) ──> (no incoming edge)   ~~[Shift-Left QA — refinement running, NOT yet buildable]~~ ~~[Ready For Dev, Story Points empty — deferred, not blocked, as of 2026-08-12]~~ [**CLAIMED 2026-08-14** — two-run deferral OVERTURNED by a 4-lens scored panel (7/7/8/9, unanimous); see the 2026-08-14 addendum below]
    BK-337 was created 2026-08-10 by the discovery routine, and its arrival closes a long-standing
    hole in this graph: the 2026-08-09 BK-43 judge panel found "no bug-detail route at all" as gap
    #5 of 5, which made AC-4's backlink *unaddressable* rather than merely unbuilt. That gap now
    has a ticket and a real dependency edge — originally onto BK-43, now onto BK-372 (below), since
    BK-43 no longer exists as a pickable ticket.
    **Update 2026-08-12**: BK-337 cleared shift-left refinement and reached `Ready For Dev`, but with
    Story Points left empty — it transitioned without passing estimation. Combined with being a whole
    new read surface for this codebase (route + handler + RPC, 17 AC scenarios), that is a scope-growth
    signal an unattended run must not auto-claim; the 2026-08-12 run deferred it pending an estimation
    pass or a human-present `/sprint-development` session (see §6). It is **still not shipped as
    code** — PR #152 (`ff8b79b`) remains docs-only by diff — so BK-372's dependency edge below is
    still genuinely unsatisfied.
    **Update 2026-08-14 — the deferral above is OVERTURNED, and both of its premises failed audit.**
    The 2026-08-13 entry mandated that this reason be audited rather than restated a third time; a
    4-lens scored panel (`model: opus`) executed that audit and returned **7 / 7 / 8 / 9, unanimous
    claim**. (1) *"A whole new read surface (route + handler + RPC)"* was **never verified even
    once** — asserted 2026-08-12 with no code recon cited, then inherited verbatim on 2026-08-13 by a
    run that says in its own words it "FOLLOWED, not re-derived". Two lenses established from code
    that it is false on two of three legs: `bunkai_bug_json` **already exists**
    (`0046_bugs.sql`), is already `language sql stable` INVOKER, is already granted to `authenticated`
    (`0054:218-219`) and already appears in `lib/types/supabase.ts:1427` — the Tech Lead's own TQ2
    says *"no new RPC is needed at all"*; and the handler is a `GET` sitting beside the shipped
    `[id]/status` and `[id]/assign` routes at identical path grain. Only the page route is genuinely
    new. (2) *"Story Points are empty"* is a rule invented once, for this one ticket: an empty points
    field is this project's **deliberate default** (`product-management/SKILL.md:333` anti-pattern
    I16; 60+ stories in `.context/PBI/` read `-`, including shipped ones), `.agents/jira-required.yaml`
    marks the field PO/planning-owned and instructs prompts to *"read them, never author them"*, and
    the CLI cannot distinguish "genuinely unset" from "merely unreturned" (logged 2026-08-12). The
    same 2026-08-12 run that deferred BK-337 for lacking points also overrode BK-43's and BK-267's
    *present* 1 SP values as meaningless — the field was treated as decisive in one direction and
    worthless in the other, on neighbouring tickets, the same day. Scope-growth criteria
    (`audit-and-selection.md:197-211`) score **one hit of six**: the `create or replace` of a live
    object — the exact shape **BK-48 shipped unattended two days earlier** (`0069`), under
    `migrations: unrestricted`. Sized by the cost lens at **5 SP** against BK-48 (5 SP = 9 files /
    1238 insertions) and BK-11 (3 SP = 556). **CLAIMED 2026-08-14.**
    **SHIPPED 2026-08-14** — PR #169 (`feature/BK-337-defect-detail-read`) merged to `staging`,
    merge commit `c6fd6a84`, ancestry independently verified (`git merge-base --is-ancestor`).
    Migration `0070_bug_detail_composer.sql` applied (widens `bunkai_bug_json` in place — a
    `create or replace`, no schema change), matching the sizing lens's own prediction. Jira
    auto-transitioned `In Progress -> In Review -> Ready For QA`. BK-372's dependency edge below is
    now genuinely satisfied — verify by git ancestry (done), not by BK-337's Jira status alone.

BK-31 Bugs & Defect Heatmap (addendum, 2026-08-11 — BK-43 ABORTED, split executed):
  BK-371 TMS-Defect Sync | Point a project at a Jira destination ──> (no incoming edge)
  BK-372 TMS-Defect Sync | Send a newly filed defect to Jira ──> BK-40 ✅, BK-337
  BK-373 TMS-Defect Sync | Recover a failed sync and show its state ──> BK-372
    Ruling `12170` (2026-08-05) mandated a BK-43a/b/c split at the size BK-43 was written; that
    mandate was **executed** 2026-08-11 via PR #156 (merge `0a89b36`) — BK-43 is now `ABORTED` and
    its two Dependencies edges (BK-40 ✅, BK-337) were carried forward onto **BK-372**, the successor
    that owns "send a defect to Jira" (the half of BK-43's old scope that actually needs those two
    prerequisites). BK-371 (destination config) has no incoming edge — it can start as soon as it
    clears shift-left. BK-373 (failure recovery) gates on BK-372, not on BK-371, because recovery UI
    needs a sync attempt to exist first. **All three are `Backlog` and none has been through
    shift-left QA refinement** — do not treat "successor exists" as "successor is buildable."
    **BK-337's half of BK-372's dependency edge cleared 2026-08-14** (PR #169, merge `c6fd6a84`,
    ancestry-verified) — BK-372 still needs BK-40 ✅ (already clear) AND its own shift-left
    refinement before it is pickable; do not treat "dependency edges clear" as "buildable" on its
    own.

BK-1 Tenancy & Identity (addendum, 2026-08-17 — BK-262 ABORTED, split executed):
  BK-497 PAT | Require every API route to declare its capability posture ──> (no incoming edge) ✅ dev-done (2026-08-18) — merged to staging via PR #182, merge commit `ce9a38d`, ancestor-verified.
  BK-498 PAT | Enforce capability scopes on the authoring domain ──> BK-497 ✅ dev-done (2026-08-19) — merged to staging via PR #186, merge commit `0becadc`, ancestor-verified.
  BK-499 PAT | Enforce capability scopes on read, identity and notification routes ──> BK-497 ✅ **dev-done (2026-08-21)** — merged to staging via PR #193, merge commit `f7d3bbc`, ancestor-verified. **The BK-262 split is CLOSED — all three slices shipped.**
    BK-262 carried 21 SP — the single largest estimate in this project's history against a ceiling of
    13 — and was deferred on scope by FOUR consecutive runs. It was never blocked: its refinement was
    complete (9 refined ACs, 17 ATP outlines, zero open confirmation markers) and its human estimator
    had already named the remedy, writing *"the split-or-not call is left to PO"* and deliberately not
    splitting it. Nobody executed that. Under Critical Rule #18 a reservation naming the PO **role**
    belongs to the AI Product Owner profile — this project has no human PO — so the 2026-08-17 `story`
    run decided it rather than waiting. An AI Product Owner ruling (Jira comment `12441`) and an
    independent AI Tech Lead ruling (comment `12442`), reconciled against each other BEFORE
    publication, split it three ways. BK-262 is now `ABORTED` (not deleted). Sizes: BK-497 5 SP,
    BK-498 8 SP, BK-499 8 SP — summing to 21, so the split does not inflate the estimate, and every
    slice now sits inside the 13 ceiling. Jira links: BK-498 and BK-499 each `depends on` BK-497 via
    `Dependencies` (direction verified after creation); all three `Relates` to BK-262, since this
    instance has no `supersedes` link type. **Entry statuses are deliberate and differ**: BK-497 and
    BK-498 were created at `Ready For Dev` because they inherit BK-262's completed, verified
    refinement; BK-499 at `Backlog` because every refinement defect found during verification falls
    inside it. The ruling explicitly invites QA to pull either of the first two back to `Shift-Left
    QA` with a single transition — **that is not a contradiction of the ruling and must not be
    recorded as one.** **No mockup gate**: this cluster is API-only and renders no screen.
    **Execution order — ALL THREE SLICES ARE SHIPPED (updated 2026-08-22).** BK-497 ✅ merged
    2026-08-18 (`ce9a38d`, PR #182), BK-498 ✅ merged 2026-08-19 (`0becadc`, PR #186) and BK-499 ✅
    merged 2026-08-21 (`f7d3bbc`, PR #193), each ancestor-verified against `origin/staging`.
    **ES-PAT is DRAINED.** The sequential-merge constraint (the route-capability coverage snapshot is
    the one file more than one slice edits) held across all three; zero capability placeholders remain
    anywhere under `app/`. **No migration in any of the three**
    (re-derived against `0008_access_tokens.sql:34-36`; the sweep introduces no fifth scope value).
    Standing finding carried by BK-497: `GET /api/v1/tokens` (`app/api/v1/tokens/route.ts:111`) has
    no capability check and no bearer guard while its two siblings hand-roll one, so a PAT can
    enumerate its owner's other PATs without being able to mint or revoke one — RLS-scoped to the
    caller's own tokens, so a scope-enforcement gap rather than cross-tenant exposure. The unguarded
    handler count is GROWING: 48 (2026-08-06) -> 49 (2026-08-11) -> **50** (2026-08-17), against 87
    handlers across 68 route files.

BK-13 ATC Library (addendum, 2026-08-13 — BK-267 ABORTED, split executed):
  BK-439 TMS-ATC Library | Browse every ATC in the workspace from one index ──> (no incoming edge)
  BK-440 TMS-ATC Library | Find an ATC by name as you type ──> BK-439
  BK-441 TMS-ATC Library | Narrow the index by Project, Module, layer and anchor ──> BK-440
    An AI Product Owner ruling (Jira comment `12315`) and an independent AI Tech Lead ruling (comment
    `12316`) both found BK-267 — recorded at 1 SP while carrying 14 AC blocks / 19 Gherkin scenarios —
    sized at ~10-11 SP, and both mandated a three-way split; comment `12317` closed the ruling. BK-267
    is now `ABORTED` (not deleted) and replaced by the three successors above, all under epic BK-13.
    Sizes: BK-439 5 SP, BK-440 3 SP, BK-441 3 SP. Jira links created BK-439 -> BK-440 -> BK-441 via
    `Dependencies`; each successor also `Relates` to BK-267. Canonical route ruled `/atcs`
    (workspace-level, `app/(app)/atcs/page.tsx`), already named in `BRIEF.md:50` and
    `master-design-plan.md:285`. **Mockup gate: CLEARED, and not newly opened** — the global ATC
    Library mockup (`.context/designs/bunkai-test-management-tool/bk-13-atc-library-global/`
    `atc-library-global.html`) shipped 2026-07-30, committed `927b62f1`, well before this split, and
    BK-439/440/441 inherit it directly (`master-design-plan.md` §4.9) — unlike BK-371 (that
    document's §5 🔒 row), this cluster opens no new mockup gate. **Execution order**: BK-439 has no
    incoming edge and is independently shippable — the `/atcs` route, the sidebar entry going live
    with an unfiltered count badge (supersedes D18 for the "ATC Library" nav entry only,
    `master-design-plan.md` §5 D31), the cross-project read plus its `GET /api/v1/atcs` endpoint, the
    eight-column dense row, row-open navigation to `/projects/{slug}/atcs/{id}` with a
    destination-naming toast, and four screen states. BK-440 (incremental name-only search, `/`
    focuses the field, Esc clears, term in the URL with restore-on-Back) gates on BK-439 — it narrows
    an index that has to exist first. BK-441 (four facets — Project, Module cascading from Project,
    layer, anchor — strict AND, one-gesture "Clear all") gates on BK-440, extending the same search
    surface. **None of the three has been through shift-left QA refinement yet** — do not treat
    "successor exists" as "successor is buildable," the same caution the BK-31 addendum above already
    states for its own cluster. `master-design-plan.md` §8 gained one row per successor under BK-13
    (BK-267 never had a §8 row — a genuine coverage gap, now closed, not a status refresh).

BK-210 Team Chat:
  BK-215 Workspace channel ──> epic BK-1   (gate for the whole epic)
  BK-216 Project channel ──> BK-215, epic BK-7
  BK-217 Mentions ──> BK-215, BK-209       (cross-epic: Notifications inbox)
  BK-218 Rich entity links ──> BK-215  ··> epics BK-13/BK-24/BK-30
  BK-219 Edit/delete ──> BK-215            BK-220 Search ──> BK-215

BK-224 Billing & Plans:  (epic ──> epic BK-1)
  BK-229 View plan/usage ──> BK-87 ✅ dev-done (2026-08-16) — merged to staging via PR #176, merge commit `18662df`, ancestor-verified.
  BK-230 Upgrade ──> BK-229 ✅ (UNBLOCKED 2026-08-16)    BK-232 Limit warnings ──> BK-229 ✅ (UNBLOCKED 2026-08-16)
  BK-231 Invoices ──> BK-230   BK-233 Downgrade/cancel ──> BK-230

BK-7 Project & Module Hierarchy (addendum, 2026-08-15 — BK-398 shipped):
  BK-398 TMS-Command Palette | Search and jump across the workspace ──> (no incoming edge)
    Created 2026-08-12 by the discovery routine (see the edge-mapping TODO in §6, where it was
    recorded as an outstanding item pending a §8 screen-map row). SHIPPED 2026-08-15 — PR #173
    (`feature/BK-398-command-palette`) merged to `origin/staging`, merge commit `3f6816f`, ancestry
    independently verified (`git merge-base --is-ancestor`) — it was the tip of
    `origin/staging`. Migration `0071_workspace_search.sql` applied. Live status now `Ready For QA`.
    No downstream dependents identified yet; add a §3 edge if one appears.
```

### 3.2 Home Dashboard cluster (BK-254) — seeded 2026-07-31

Six stories, no new entities — Home is a landing-page aggregation over Runs/Bugs/Coverage/Projects. Mockup already shipped (`home.jsx`, master-design-plan §4.2 — no §5 gate needed). Full dependency edges live as Jira `Dependencies`/`Relates` links (4 links, direction-verified); the two hard edges also appear in the main flat edge list above.

```
BK-254 Home Dashboard:
  BK-255 Welcome banner        ✅ SHIPPED 2026-08-04 — merged to staging via PR #120, ancestor-verified.
  BK-256 Active runs table     ✅ SHIPPED 2026-08-04 — merged to staging via PR #122, ancestor-verified.
  BK-257 Recent projects       ✅ SHIPPED 2026-08-04 — merged to staging via PR #121, ancestor-verified.
  BK-258 Open bugs summary     ✅ SHIPPED 2026-08-04 — merged to staging via PR #124, ancestor-verified.
  BK-259 Coverage summary      ✅ SHIPPED 2026-08-04 — merged to staging via PR #125, ancestor-verified.
  BK-260 Condensed activity    ✅ SHIPPED 2026-08-04 — merged to staging via PR #123, ancestor-verified.
```

**Open design question (not a dependency, flagged for a human):** the `home.jsx` mockup shows a "SPRINT 24-Q2 · DAY 7/10" eyebrow line implying a Sprint/iteration entity. No such entity exists in the schema or in `business-data-map.md`. BK-255 deliberately does not build it — see its Out of Scope field and the `## Gap` comment on BK-255.

---

## 4. Execution sprints

An **Execution Sprint (ES)** is a gate-released batch: a set of stories safely workable in parallel once the prior ES's gates are dev-done. ES are dependency-driven batches, **not** calendar sprints and **not** the strategy-layer Master Sprints in `master-implementation-plan.md`.

> This table is **regenerated from the live dependency graph each run** — a claim this table has twice failed to honour (ES-PAT and ES-ATC-LIB carried full §3.1 edges from 2026-08-13/08-17 with no §4 row until 2026-08-18). Read it as the intent, not a guarantee; verify a row against §3 before relying on it — it is a derived projection of §3 + current gate state, not a hand-frozen plan. Status words below ("shipped", "done this cycle") describe gate-release events, not live Jira status (query that via §6).

| Exec Sprint | Stories | Gate released by | Notes |
|-------------|---------|------------------|-------|
| **ES0 ✅** | BK-27 | — | Shipped → QA Approved. Released the whole ES1 fan-out. |
| **ES1 ✅ (fully drained)** | ✅ all shipped: BK-28, BK-22, BK-23, BK-32, BK-20, **BK-33 Test Tags** | BK-27 ✅ | ES1 fan-out drained — reorder/usage/duplicate/view + ATC Search + Test Tags all landed (most QA Approved; BK-22/BK-23 dev-merged, awaiting QA — staging deploy gap per BK-142, code IS on staging). BK-86 (Account) also shipped → QA Approved; BK-3 (OAuth) now AC-synced + parallel. |
| **ES1.5 ✅ (BK-87 dev-done)** | BK-87 (after BK-86 ✅) ; **BK-21 ✅ (shipped)** | BK-86 ✅ ; BK-27 ✅ | **BK-87 ✅ dev-done 2026-07-30** — stacked 2-PR split (git-flow-master decision tree, 830-line forecast): PR1 shell/nav/auth-guard/identity (#63) + PR2 workspace list (#64), both merged to staging; 1 review BLOCKER (member-count RLS undercount) fixed pre-merge. Ready For QA, assigned to shift-left QA owner. **BK-21 ✅ dev-done 2026-06-25** — 10 contract Qs ratified (ADR-0009) + OpenAPI drift fixed; merged to staging (PR #57) + edit-path unified (PR #58); Ready For QA. |
| **ES2 ✅ (shipped)** | BK-34 Start manual run | BK-27 ✅ | **Shipped → QA Approved** — released the Runs tail (BK-35/36/37/38/39). |
| **ES2.5** | BK-88, BK-89 (after BK-87) | BK-87 | BK-88 mockup ✅ 2026-07-30 (`settings-tokens.html`) — remaining gate: its own 9 Qs. **BK-89 promoted to RFD 2026-06-24** but open Dev contract (add `role` to `GET /workspaces` + active-workspace transport) — resolve before coding; mockup ✅ (`settings-workspaces.html`). |
| **ES3 ✅ (fully drained 2026-07-31)** | BK-36 ✅, BK-37 ✅, BK-39 ✅, BK-38 ✅, BK-35 ✅, BK-90 ✅ — all shipped, all merged to staging, all Ready For QA / Ready For Release | BK-34 ✅ ; BK-89 ✅ | Entire ES3 fan-out drained in a single 2026-07-31 batch (avalanche-style run). BK-39 was actually the oldest of the batch (PR #60, merged 2026-06-25) — the roadmap had simply never been updated to reflect it. All six confirmed genuine `git merge-base --is-ancestor` hits against `origin/staging`, not tracker-status inference. |
| **ES2.5 ✅ (drained 2026-07-31)** | BK-88 ✅ | BK-87 ✅ | PRs #68+#70 merged to staging. Ready For QA. Shipped with 4 PO Qs + 1 security Q unanswered in-thread (see settings-cluster note in §3) — flagged for QA/PO follow-up, not a re-open trigger. |
| **ES4 (epic BK-31 Bugs)** | BK-41 ✅, **BK-42 ✅ (shipped 2026-08-02)** — both dev-done, merged to staging, Ready For QA ; ~~BK-43 — refinement genuinely resolved (comment 12069, 2026-08-01), still `Ready For Dev`, not yet claimed~~ ~~**BK-43 — read §6, not this cell.** Its refinement IS resolved (zero open questions, uniquely so on this board), but the later ruling `12170` (2026-08-05) found it **not implementable as written at 1 SP** and mandated a BK-43a/b/c split that has never been created. "Not yet claimed" reads as an available pick and is misleading without that forward pointer~~ **The mandated split has now been created — that clause is false as of 2026-08-11.** BK-43 is **`ABORTED`** (ruling `12170` executed via PR #156, merge `0a89b36`, 2026-08-11), replaced by **BK-371** (Point a project at a Jira destination, 3 SP, no incoming edge) **-> BK-372** (Send a newly filed defect to Jira, 3 SP, carries BK-43's old BK-40 ✅ / BK-337 Dependencies edges) **-> BK-373** (Recover a failed sync and show its state, 3 SP, gated on BK-372) — all three epic BK-31, all live status `Backlog`, **none refined through shift-left QA yet** | BK-40 ✅ ; BK-41/BK-42 for their own downstream (none yet) ; BK-337 (Shift-Left QA, not shipped) for BK-372 | BK-42: PR #108 merged (`c2fb9722`, ancestor-verified), migration `0052_defect_heatmap_report.sql` applied 2026-08-02. BK-371/372/373 are what remains before epic BK-31 is fully drained — blocked on ordinary shift-left refinement now, not on unowned ticket administration. |
| **ES4 (BK-44 Coverage)** | BK-45, BK-50 | BK-24 ✅, BK-30 ✅ — **the BK-31 gate is CORRECTED (2026-08-07): it was bookkeeping, not a functional edge** | ~~Hard-blocked regardless of refinement quality until epic BK-31 actually finishes (only BK-43 left).~~ **Overturned 2026-08-07.** Nothing BK-45 reads depends on BK-43 existing: the traceability mockup contains zero external-tracker fields (`grep -niE "jira\|external\|sync\|tracker"` over all 1048 lines of `traceability-chain.html` → no matches), §4.7's defect column is internal `bugs` identity only, and `0046_bugs.sql:94-115` has no sync column — BK-43 can only ADD columns, never change what BK-45 reads. Decisive precedent: **BK-46 (PR #93) and BK-47 (PR #98), same epic BK-44, both merged 2026-08-01 while BK-31 was undrained** (BK-42 merged 2026-08-02). This row gated only BK-45/BK-50 and never BK-46/BK-47 — applied inconsistently inside one epic. ~~**BK-45 claimed 2026-08-07.** BK-50 remains genuinely blocked on BK-45's response shape.~~ **Both halves corrected 2026-08-08.** BK-45 did not stop at "claimed" — it **SHIPPED** the same day: PR #142, merge commit `f75709e`, ancestor-verified against `origin/staging`. **BK-50's gate is therefore RELEASED**: the `GET /api/v1/projects/{id}/traceability` response shape it exports is live on staging, and `components/traceability/TraceabilityChainView.tsx:32-37` states in its own header that it deliberately omitted the Export button as BK-50's scope. ~~BK-50 is no longer dependency-blocked by anything — it is deferred on **scope-growth** instead (see its §6 row). Epic BK-44 now has BK-48 (`Shift-Left QA`) and BK-50 left.~~ **Stale — corrected 2026-08-09.** This clause was written mid-run on 2026-08-08 and never updated after that same run's amendment shipped BK-50. **BK-50 SHIPPED**: PR #145, merge commit `7b16c0c`, ancestor-verified against `origin/staging`. §4 and §6 had been contradicting each other on this since 2026-08-08. ~~**Epic BK-44 now has only BK-48 (`Shift-Left QA`) left**, which matches §6.~~ **Stale — corrected 2026-08-12.** BK-48 cleared shift-left and reached `Ready For Dev` (5 SP) before this row was next reconciled, and the 2026-08-12 autonomous-delivery `story` run **claimed and implemented it**, ending a three-run empty streak (2026-08-09 through 2026-08-11). Its Jira `Dependencies` edge onto **BK-30** (Epic, `Planning`) was followed as precedent rather than re-derived — the same ruling already applied to BK-211 on 2026-08-05 (an epic's `Planning` status is stale bookkeeping, not a functional gate), and epic BK-44 itself reads `Planning` while its own children BK-45/BK-50 are `QA Approved`. See §6's 2026-08-12 conclusion for the claim evidence. |
| **ES5 (BK-208 Notifications, post-MVP)** | ~~BK-209 (first-of-cluster, "free" per §3.1) ; BK-211/212/213 (blocked on BK-209)~~ **CLUSTER DRAINED — only BK-214 left** | ~~none (BK-209) ; BK-209 (rest)~~ BK-213 ✅ (BK-214) | ~~BK-209 is dependency-clear and has all its refinement questions genuinely *answered* in-thread, but its "PO Ratification — 2026-07-11" comment was posted 11 minutes *before* the actual Q&A content it claims to ratify, by a different account than the one that answered — a blanket 28-story batch delegation, not per-story human sign-off. Also 13 SP (advisory) and first-of-epic (new notification substrate, no prior schema to extend) — oversized for an unattended pick per the scope-growth check. Flagged conditional: needs an explicit human "go" before either an autonomous or interactive run claims it.~~ **STALE, corrected 2026-08-10.** All four of BK-209/211/212/213 shipped and are ancestor-verified on `staging`: PR #113 (`aed91e0`), PR #137 (`861c441`), PR #115 (`62d4d58`), PR #127 (`2e91ad9`). This cell survived the whole cluster draining underneath it and still described BK-209 as an unclaimed conditional pick — §6's pool table had recorded every one of those merges and §4 was never reconciled against it. **BK-214 (email digest) is the only story left in ES5**; its own gate BK-213 is now ✅, so it is dependency-clear and its readiness has NOT been re-checked here. Read §6, not this cell, for current state. |
| **ES-HOME (seeded 2026-07-31)** | BK-255, BK-256, BK-257 — no upstream edges in this graph; BK-260 — soft-coupled to BK-49 ✅ merged to origin/staging, ancestor-verified 2026-08-03 | none (BK-30's read data already dev-done in this graph) | New Home Dashboard epic (BK-254). All 4 of these stories are now ✅ **SHIPPED 2026-08-04** (PRs #120-123). |
| **ES-HOME-UNBLOCKED (2026-08-03), all SHIPPED 2026-08-04** | BK-258 ✅ PR #124; BK-259 ✅ PR #125; BK-260 ✅ PR #123 | — (dependency layer cleared, then all three shipped) | All 3 of BK-254's dependency-gated stories are now merged to `origin/staging`, ancestor-verified. Previously this doc under-reported BK-46/BK-49 as unmerged, then under-reported these three as still-Backlog after their gates cleared; corrected 2026-08-04 after a direct `git merge-base --is-ancestor` check. |
| **ES-PAT (epic BK-1, seeded 2026-08-17, first ES row written 2026-08-18)** | **BK-497** (capability posture, 5 SP, no incoming edge) **-> BK-498** (authoring-domain enforcement, 8 SP) **-> BK-499** (read/identity/notification enforcement, 8 SP) | — (BK-497 has no incoming edge) ; BK-497 for BK-498 ; BK-497 for BK-499 | **This row did not exist until 2026-08-18 and its absence is the defect being corrected.** The cluster was created 2026-08-17 when BK-262 (21 SP) was `ABORTED` and split by attributed AI PO + AI Tech Lead ruling, and §3.1 gained full edge detail the same day — but §4 was never regenerated, so the table still ended at ES5/ES-HOME while claiming in its own header to be *"regenerated from the live dependency graph each run."* Binding constraints carried from the split ruling, **not to be re-derived**: the capability seam is the **DOMAIN, not the verb** (six route files hold their GET and their write in the same file, so a verb cut has the second PR re-editing the first's lines), **no file may receive a final capability posture from two stories**, BK-498 and BK-499 **must merge sequentially** because the route-capability coverage snapshot is the one file both slices edit, and **no slice needs a database migration** (re-derived against `0008_access_tokens.sql:34-36`). **No mockup gate** — the cluster is API-only and renders no screen; §7 would normally require a §8 screen-map row before dev, and the API-only justification is recorded here in its place. **BK-497 ✅ SHIPPED 2026-08-18** (PR #182, merge `ce9a38d`, `--is-ancestor` exit 0), **BK-498 ✅ SHIPPED 2026-08-19** (PR #186, merge `0becadc`, exit 0) and **BK-499 ✅ SHIPPED 2026-08-21** (PR #193, merge `f7d3bbc`, exit 0). **ES-PAT ✅ FULLY DRAINED 2026-08-21** — all three slices merged and ancestor-verified; zero capability placeholders remain under `app/`. Note: §6 carries no 2026-08-17/18/19 conclusion, so the original "see §6's 2026-08-18 conclusion" pointer never resolved; those runs recorded to the escalation log instead. |
| **ES-ATC-LIB (epic BK-13, seeded 2026-08-13, first ES row written 2026-08-18)** | BK-439 (workspace-wide ATC index, 5 SP, no incoming edge) -> BK-440 (name search, 3 SP) -> BK-441 (facet filters, 3 SP) | — (BK-439 has no incoming edge) ; BK-439 for BK-440 ; BK-440 for BK-441 | Same omission class as ES-PAT: §3.1 has carried the edges since 2026-08-13, §4 never gained the row. All three are live `Backlog` and **none has been through shift-left refinement** — the roadmap's own standing warning applies literally here: *do not treat "successor exists" as "successor is buildable."* Mockup gate ✅ **cleared, not newly opened**: all three render into `§4.9 · bk-13-atc-library-global/atc-library-global.html`. Not eligible for an unattended pick until refined. |
| **ES-PLANS (epic BK-201, edges live since 2026-07-11, first ES row written 2026-08-21)** | **BK-202** (create a plan, 3 SP) **✅ SHIPPED 2026-08-20** → **BK-203** (add/remove tests) · **BK-207** (close a plan) ; **BK-205** (create milestone) ✅ SHIPPED 2026-08-05 → **BK-206** (assign plans) ; **BK-204** (track progress) | epic BK-24 ✅ for BK-202 ; BK-202 ✅ for BK-203 and BK-207 ; BK-203 + epic BK-30 ✅ for BK-204 ; BK-205 ✅ + BK-204 for BK-206 | **Third instance of the exact defect §4's own header admits** — ES-PAT and ES-ATC-LIB were the first two. §3.1 has carried this cluster's full edge detail since 2026-07-11 and BK-202 shipped on 2026-08-20, yet §4 still ended at ES-ATC-LIB while claiming to be *"regenerated from the live dependency graph each run."* It was not, for the third time. **BK-202 ✅ SHIPPED** — PR #188, merge `5fddd4c`, `--is-ancestor` exit 0; migration `0073_test_plans` applied live (additive; `test_plans` + two SECURITY DEFINER RPCs), and it ships **SELECT-only RLS** by deliberate decision after the implementing run verified via `information_schema.role_table_grants` that `authenticated` holds INSERT/UPDATE/DELETE on every public table — so copied write policies would have been a live second write path, not defence in depth. **BK-205 ✅ SHIPPED 2026-08-05** — PR #132, merge `5054716`, ancestor-verified. **BK-203 and BK-207 are both newly dependency-clear** as of BK-202's merge (BK-207 retains only its soft `··>` edge onto BK-204, which has no commit on staging). Neither has been re-checked for refinement readiness here — read §6, not this cell. Mockup gate ✅ **satisfied**: `bk-201-test-plans-milestones/` — all three files verified present on disk this pass, not inferred from the Jira field, which has been observed stale in the BLOCKED direction on this very cluster. **Post-ship defect trail (added 2026-08-22, previously recorded nowhere structural):** two defects were filed against BK-202 the day after it merged and **both shipped 2026-08-21** — **BK-591** (merge `bf39c39`, PR #195) and **BK-592** (merge `40c58ba`, PR #196), each `--is-ancestor` exit 0, with migration **`0074_test_plan_nbsp_whitespace_class`** applied. The `test_plans` surface therefore sits at **`0074`, not `0073`** — a run planning a migration against this cluster must take its number from the live ledger, which is the only place that distinction is visible. |
| **ES-CHAT (epic BK-210 Team Chat, edges live since 2026-07-11, first ES row written 2026-08-24 — NEWLY PROJECTED)** | **BK-215** (workspace real-time channel — cluster root) **->** BK-216 (project channel), BK-217 (mentions), BK-218 (rich entity links), BK-219 (edit/delete own messages), BK-220 (search history) | epic BK-1 ✅ for BK-215 ; BK-215 for all five successors (BK-217 additionally needs BK-209 ✅, cross-epic) | **Newly projected 2026-08-24 — same omission class as ES-PAT and ES-ATC-LIB**: §3.1 has carried these edges since 2026-07-11 and §4 never gained a row. **The whole cluster is blocked at its root**: BK-215 sits at live status `Estimation` with **zero commits and zero branches** anywhere in git, so not one of the five successors is workable. That is precisely why BK-219 has been the pool's only genuinely git-blocked candidate run after run — the block is BK-215, not BK-219. Mockup gate ✅ cleared 2026-07-30 (§5, `bk-210-team-chat/`, 4 screens). Projection, not a plan: verify against §3.1 before relying on it, per this section's own reading rule. |
| **ES-BILLING (epic BK-224 Billing & Plans, edges live since 2026-07-11, first ES row written 2026-08-24 — NEWLY PROJECTED)** | **BK-229** (view plan, seats & usage) **✅ SHIPPED 2026-08-16** (PR #176, merge `18662df`, ancestor-verified) **->** BK-230 (upgrade to a paid plan), BK-232 (plan-limit warnings) ; **BK-230 ->** BK-231 (billing details + invoices), BK-233 (downgrade/cancel) | **BK-87** Settings hub ✅ for BK-229 ; BK-229 ✅ for BK-230 and BK-232 ; BK-230 for BK-231 and BK-233 | **Newly projected 2026-08-24 — same omission class as the row above.** The BK-229 gate is cleared, so BK-230 and BK-232 are dependency-free today; BK-231 and BK-233 wait on BK-230. Mockup gate ✅ cleared 2026-07-30 (§5, `bk-224-billing/`, 5 screens). Note the standing §7 scope-growth caveat on **BK-230** (13 SP, commits the repo to a Stripe integration that exists nowhere in it) — dependency-clear is not the same as claimable unattended. Projection, not a plan: verify against §3.1 first. |

---

## 5. Mockup-gate registry (Critical Rule #15)

A story whose primary screen has no mockup **cannot start** until the mockup lands (or a spec-only departure is ratified in master-design-plan §5 + ADR).

> **2026-07-30 — all gates lifted; ONE HAS SINCE REOPENED (see the 🔒 row below).** A 10-batch Open Design fleet (MCP-driven Mode A, design
> system `user:bunkai`) shipped mockups for every gated screen — MVP and post-MVP. Batches live at
> `.context/designs/bunkai-test-management-tool/bk-*/` with per-screen specs in master-design-plan
> §4.6–§4.15 and §8 rows pointing at the concrete files. ~~**No story in this roadmap is
> mockup-blocked anymore.**~~ **Superseded 2026-08-11 — one new gate opened.** The registry below is
> kept as the historical record + file index; see the 🔒 row appended beneath it for the exception.

| Mockup (was gated) | Screen ref | Unblocked | Mockup files (batch folder) |
|---------------|-----------|--------|--------|
| ✅ **Settings** (Account · PATs · Workspaces) | §4.10 | BK-87, BK-88, BK-89, BK-90 | `bk-85-account-settings/` — 5 screens (hub/account, tokens, workspaces, account-menu overlay, coming-soon pattern) |
| ✅ **Test Runs index** | §4.8 | BK-37, BK-38 | `bk-30-test-runs-index/` — test-runs-index, test-run-history |
| ✅ **Metrics** | §4.7 | epic BK-44 | `bk-44-metrics-coverage/` — metrics-dashboard, traceability-chain |
| ✅ **Bug Reports** (+ BK-42 heatmap as List/Heatmap toggle) | §4.6 | epic BK-31 | `bk-31-bug-reports/` — bug-reports-index, bug-detail |
| ✅ **Global ATC Library** | §4.9 | nav-completion (BK-20 ref) | `bk-13-atc-library-global/` — atc-library-global |
| ✅ **Test Plans & Milestones** | §4.11 | BK-202..BK-207 | `bk-201-test-plans-milestones/` — 3 screens |
| ✅ **Automation & CI** | §4.12 | BK-225..BK-228 (BK-222/223 API-first, UI-light) | `bk-221-automation-ci/` — 1 screen + 3 extension crops |
| ✅ **Notifications** | §4.13 | BK-209..BK-214 | `bk-208-notifications/` — 3 screens (prefs extends Settings hub) |
| ✅ **Team Chat** | §4.14 | BK-215..BK-220 | `bk-210-team-chat/` — 4 screens |
| ✅ **Billing** | §4.15 | BK-229..BK-233 | `bk-224-billing/` — 5 screens (extends Settings hub) |
| 🔒 **Jira-destination / integration-settings surface** (BK-371) | — none exists | not yet — BK-371 stays gated | none — `.context/designs/bunkai-test-management-tool/` has no screen for "point a project at a Jira destination." This is a **new gate, opened 2026-08-11**, not a re-opening of a lifted one: BK-371 is a successor of the ABORTED BK-43, and its scope (destination-config UI) was never part of any of the 10 mockup batches shipped 2026-07-30 — `bk-31-bug-reports/bug-detail.html`'s "External tracker panel" (§4.6) is a *read-only display* of sync state, not a configuration surface. **Do not build BK-371 UI from inference; do not invent the missing screen here.** Clear this gate the same way the 2026-07-30 batch cleared the others, then flip this row to ✅. |
| 🔒 **Thirteen post-2026-07-30 stories whose §8 rows are mockup-gated and unratified** (~~BK-315~~ **ratified 2026-08-27 as D37 — removed from this list on 2026-08-28; locate its §8 row by row text, not by line number**, BK-399, BK-442, BK-443, BK-507, BK-508, BK-512, BK-513, BK-555, BK-571, BK-572, BK-596, BK-601, **BK-666** — appended 2026-08-29, its §8 row had existed since PR #212 with zero mentions in this document) | see each story's `master-design-plan.md` §8 row | not yet — all thirteen stay gated | **Recorded 2026-08-21, and the gap itself is the finding: §5 and §8 have been contradicting each other on how many stories are mockup-blocked — one versus twelve.** **Corrected 2026-08-22 on two counts, both re-verified in the file rather than inherited.** (a) **BK-554 is NOT gated and has been removed from this list** — its §8 row (`master-design-plan.md:618`) reads `⚠️ extension needed`, not `🔒`: the host screen `test-run-history.html` (§4.8) exists and merely lacks a flakiness treatment. That is a spec-extension item, not a mockup gate, and counting it here overstated the blocked set. (b) the previously-cited line numbers were **off by one row and stale**: `grep -nE '🔒' .context/design/master-design-plan.md` now returns **eleven §8 story rows beyond the BK-371 row above — lines 579, 588, 602-605, 616-617, 628, 641, 642** (the thirteenth 🔒 hit, line 503, is a §5 divergence-table cell, not a §8 row). §5 listed none of them. These are **new gates opened as each story was created**, not re-openings of anything the 2026-07-30 batch lifted. The one with live consequences today is **BK-315** (Export a Project's ATCs to CSV, 1 SP, `Ready For Dev`): it is the cheapest story on the board and was deliberately NOT picked on 2026-08-21 because `master-design-plan.md:604` says *"Do not invent the control"* — verified in the repo rather than the field, the only "CSV" string in `atc-library-global.html` is sample ATC title text, not an export affordance. **Do not build any of these eleven from inference.** Clear each gate the way the 2026-07-30 batch cleared the others, then flip its row. **Amended 2026-08-23 — the list is now TWELVE: BK-596** (Rename a Test, archive it and restore it) was created 2026-08-23 and its §8 row (`master-design-plan.md:613`) carries a **split verdict**, which is a shape this row had not seen before: rename/delete is COVERED by the explorer context menu that already exists (`project.jsx:283`), while the **archive/restore vocabulary alone is 🔒 gated** — no mockup anywhere draws an archived-row treatment, a restore action, an include-archived toggle or the in-use warning. Gate the archive half only; the rename half is buildable today. Its sibling **BK-595 is deliberately NOT on this list** — its §8 row reads *"n/a — API-only, no mockup applicable"*, so it has no gate to clear. **Amended 2026-08-24 — the list is now THIRTEEN: BK-601** (See what is archived and restore an archived module, epic BK-7) was created 2026-08-23 by the `discovery` routine and gained its §8 row via PR #204 (merge `20930ab`, `--is-ancestor` exit 0). Its row is 🔒 mockup-gated and unratified on the same grounds as BK-571 and BK-596, whose precedent it explicitly inherits: an exhaustive sweep of `.context/designs/bunkai-test-management-tool/` draws no archived-row treatment, no restore action and no include-archived toggle. Its sibling **BK-600 is deliberately NOT on this list** — its §8 row reads `✅ covered`, and the wizard-vs-flat-checklist adaptation it depends on is ratified, not gated (`master-design-plan.md` §5 **D35**, transcribed 2026-08-24 from BK-35's `D-UI-1`). **Live 🔒 line list, recounted 2026-08-24 against the post-D35 file** (the 2026-08-22 figures above are kept as the dated record of that pass, not as current state): `grep -n '🔒' .context/design/master-design-plan.md` returns **thirteen §8 story rows beyond the BK-371 row above — lines 580, 589, 590, 604-607, 613, 620-621, 633, 646-647**; line 503 remains the §5 divergence-table cell, not a §8 row. Every §8 number in this table shifted **+1** on 2026-08-24 because the D35 row was inserted into §5, above §8 — a reminder that these citations are only as good as the pass that wrote them. **One stale citation is left uncorrected on purpose and is flagged here instead**: the `master-design-plan.md:618` reference to BK-554's row above now points three lines short (BK-554 is at `:622`); it sits inside a dated historical clause and was outside this pass's correction set. |

> Mockups with screens already drawn (`login.jsx`, `app.jsx`, `project.jsx`, `editor.jsx`, `run.jsx`, `home.jsx`) cover every ES1/ES2 story. Only remaining conscious design gap besides the BK-371 row above: **BK-5 Members** (Settings shows it as "coming soon" — design when its frontier approaches). Post-MVP stories stay `Backlog` by deliberate roadmap decision, not by mockup gate.

---

## 6. Live status — query it, never freeze it

> **Why there is no status table here.** Story status (`Ready For Dev`, `In Progress`, `QA Approved`, …) and story points live in **Jira**, which changes every day. Any table pasted here goes stale within days and then actively *lies* to whoever reads it. So this section is a **recipe, not a photo**: run one of the two queries below to get *today's* truth. The durable structure — which ticket unblocks which (§3), execution sprints (§4), mockup-gates (§5) — is safe to trust as written; only status is volatile.

**Recipe A — refresh the local PBI cache** (writes files under `.context/PBI/`, good for offline reading):

```bash
bun run jira:sync-issues        # READ package.json for the exact script before running
# then read .context/PBI/epic-tree.md  — the epic→story index with status per story
```

**Recipe B — one-shot live query** (Atlassian MCP, nothing written to disk):

```
project = BK AND sprint in openSprints() AND issuetype = Story ORDER BY rank ASC
```

Useful fields: `summary, status, assignee, customfield_10016` (story points). Then cross-reference each `BK-NN` against the §3 graph + §4 execution sprints to see what is *actually* workable now.

**To answer "what is next to work?"** — run Recipe B, then pick the highest-ranked story where ALL of these hold:
1. its §3 blocker is already dev-done (`QA Approved` / `In Test` / merged), AND
2. its §5 mockup-gate (if any) is cleared, AND
3. its pre-dev blockers below (if any) are resolved.

### Per-story pre-dev blockers — LOCAL knowledge (NOT a Jira field; will NOT appear in a §6 query)

These are gating questions / contract decisions captured during shift-left refinement. A story can read `Ready For Dev` in Jira while still blocked by one of these. Clear before starting dev:

| Story | Pre-dev blocker (resolve first) |
|-------|----------------------------------|
| BK-3  | ~~Sync AC field to the 10 refined ACs.~~ **DONE (2026-06-24)** — AC field synced. **BUT (found 2026-08-01)**: the 10 blocker-question answers (PO/Dev/Design, dated 5/26) are all headed "simulated for QA engineering practice" with no ratification comment since. AC-sync ≠ refinement-resolved. Still not autonomous-eligible. |
| BK-20 | ~~Status is literally **BLOCKED** (not a pre-dev question) — QA run 6/30 FAILED 23/24 TCs, defect BK-187 ("response shape wrong") open in traceability. Resolve the defect before this can move at all.~~ **Blocker RESOLVED 2026-08-06, recorded here 2026-08-08.** BK-187 was fixed and merged to `origin/staging` via **PR #140** (`fix/BK-187-atc-search-spec-correction`), merge commit `c0712e9`, ancestor-verified. The named defect no longer exists as an open gap in code. This row had outlived its own blocker by two days — BK-187 shipped but was never recorded anywhere in this document. **Before treating BK-20 as claimable, re-verify its live Jira status and whether the failed QA run was re-executed** — a fixed defect is not the same as a re-passed run. |
| BK-41 | ~~No PO/Dev comment ever posted after moving to Ready For Dev.~~ **RESOLVED + SHIPPED (2026-08-01)** — 6 Qs ratified (comment 12071: BK-40 dependency confirmed merged, no PAT scope gate, SECURITY INVOKER via existing RLS not a new DEFINER+actor-param RPC, keyset pagination, severity-then-recency sort, multi-select filters). Merged to staging as 3 stacked slices (PRs #101 DB / #103 API / #105 UI). Ready For QA, assigned jesusgpythondev. |
| BK-42 | ~~No PO/Dev comment ever posted.~~ **RESOLVED (2026-08-01), SHIPPED (2026-08-02)** — 11 Qs ratified (comment 12068), incl. one real correction (AC-11's 403 → 404 non-disclosure convention). Merged to staging (PR #108, `c2fb9722`), migration `0052_defect_heatmap_report.sql` applied. Ready For QA, assigned jesusgpythondev. |
| BK-43 | ~~8 unanswered Open Questions, self-contradicted readiness claim.~~ **RESOLVED (2026-08-01)** — actually 9 Qs (comment 12069): sync target confirmed Jira Cloud (already named in project context, not invented), create-only one-way sync, dedup via existing `external_id`. Refinement status READY, still Ready For Dev. |
| BK-209 | ~~Blanket forward-dated batch ratification, not per-story sign-off.~~ **SUPERSEDED — BK-209 SHIPPED 2026-08-03 (PR #113, `aed91e0`, ancestor-verified); see the pool table below.** Kept for the finding, not the status. **RE-VERIFIED (2026-08-01, comment 12070)** — independent per-story re-check found the real gap was worse than first flagged (Q&A posted 5 days after the ratification comment, not 11 minutes; all 3 roles answered by the same account). Every prior answer independently re-affirmed or corrected on its own merits; one new RLS pattern recommended (`SECURITY INVOKER` + `recipient_user_id = auth.uid()`, no actor-bind class at all). The trailing clause "still Backlog/post-MVP (not yet promoted to this sprint's frontier)" was already false when the pool table below recorded the merge, and is struck 2026-08-10. |
| BK-43 | 8 unanswered HIGH/MEDIUM "Open Questions for PO/Dev" (integration mechanism, retry policy, deletion semantics, auth, dedup) — self-estimated 1 SP and self-declared "Next: Ready For Dev" without answering any of them. |
| BK-209 | ~~All refinement questions are genuinely *answered*, but the "PO Ratification — 2026-07-11" comment was posted 11 minutes *before* the Q&A it claims to ratify, by a different Jira account than the one that answered — a blanket batch delegation, not per-story sign-off. 13 SP, first-of-epic (new notification substrate). Needs explicit human "go."~~ **SUPERSEDED 2026-08-10 — SHIPPED 2026-08-03 (PR #113, `aed91e0`).** This is the SECOND stale BK-209 row in this same section; the row above carried the same false status. The ratification finding itself still stands as a process observation (see the systemic-pattern note at the end of §6) — the "needs explicit human go" gate does not, because the story is already merged. |
| BK-88 (informational, already shipped) | 4 PO Qs (revoked-token visibility, confirmation copy, expiry display, clipboard fallback) + 1 security Q raised 6/10 were never answered before merge — only the privilege-escalation bug (BK-135) got fixed. Not a re-open trigger; flagged for a QA/PO follow-up pass. |
| BK-90 (informational, already shipped) | 6/10 refinement answers explicitly labeled "practice-exercise... not real confirmations"; a 7/31 03:07 "confirmed by PO" comment was self-reversed 4 minutes later by the same author. Shipped anyway. Not a re-open trigger; flagged for QA follow-up. |
| Design §8 | ~~Add screen rows for BK-35 / 36 / 37 / 39.~~ **Resolved (2026-06-20)** — master-design-plan §8 already has screen rows for BK-35/36/37/38/39 (lines ~276–280). No action. |

### Current Ready-For-Dev pool (opened 2026-08-03; table rows appended through 2026-08-08, run-conclusion prose below continues through 2026-08-12 — read to the end of the section, the earliest rows are NOT the current state)

Live-queried directly against Jira (`acli jira workitem view` + `acli jira workitem comment list`
per story — **not** the `jira:sync-issues` PBI cache; that cache was independently found to
silently drop each story's newest comment during this pass, which would have wrongly read BK-42
and BK-43 as unresolved — see the discovery note below). This is the actual `Ready For Dev` set
across the epics touched by this pass, superseding any narrower framing above.

| Story | Refinement status | Notes |
|-------|-------------------|-------|
| ~~BK-42~~ | ✅ Resolved (comment 12068) | **SHIPPED 2026-08-02** — no longer Ready For Dev. PR #108 merged to staging (`c2fb9722`), migration `0052_defect_heatmap_report.sql` applied, Ready For QA. Left in this table only as the historical record of why it was picked; see the pre-dev-blocker table above and the escalation log for the full trail. |
| BK-43 | ✅ Resolved (comment 12069) | **2026-08-03 autonomous-delivery `story` run: considered, NOT claimed.** Dependency-clear and refinement-resolved, but the acceptance criteria imply a genuinely new architectural surface for this codebase — an outbound integration to an unnamed "external tracker" with automatic sync-on-file, retry-on-failure, and a sync-failed UI state; ~~no connector/webhook/retry pattern exists yet anywhere in this repo~~, and the target system isn't even named. This trips the scope-growth check (new architectural pattern, no reusable precedent) — deferred to a human-present `/sprint-development` session, not an unattended pick. **Rationale CORRECTED 2026-08-07 — the deferral stands, the stated reason was factually wrong.** A connector/retry pattern DOES exist: `lib/jira/client.ts` (166 lines; exponential backoff `[1000,2000,4000,8000,16000]` at `:79`, `Retry-After` honoring `:87-96`, typed `JiraAuthError`/`JiraError` `:32-43`), plus a real async-job architecture — `0019_import_jobs.sql` (+ `0020` one-active-job index), `lib/jira/import-runner.ts` (295 lines), request-triggered `after(() => runImportJob(...))` at `app/api/v1/imports/route.ts:79-84`. The target IS named (Jira Cloud, comment `12069`). **What is genuinely absent, and is why it stays deferred**: (1) no outbound **write** — the client exports only `searchIssues`, and its `POST` at `:134` is `POST /search/jql`, a read; (2) no **per-workspace** third-party credential model — today it is one global `ATLASSIAN_*` triple (`lib/env.ts:36-38`), and a multi-tenant TMS needs a new secrets model, which is a security posture and an ADR, not a story detail; (3) no `external_id`/`sync_status` on `bugs`; (4) no time-based retry primitive anywhere (no cron, no `pg_cron`, no `supabase/functions/` — the only async trigger is request-scoped `after()`). Also: comment `12069`'s "dedup via **existing** `external_id`" is wrong — `external_id` lives on `user_stories` (`0003_authoring.sql:20`, unique index `0016:22-24`), not on bugs. Comparable inbound sync BK-17 is ~1400-1500 lines and BK-43 needs strictly more, against a **1-point** estimate. |
| ~~BK-45~~ | ⚠️ **NOT resolved** — ***superseded, see the 2026-08-07 row below*** | ~~Only 2 comments total exist on the issue~~ (Benjamin Segovia's 2026-06-11 refinement + Ely's 2026-07-30 mockup note) — no ratification since. Quoting the 6/11 comment directly: *"Top blockers: (1) BK-24/BK-30/BK-31 still in Planificación — chain layers not sprintable yet. (2) 11 open PO/Dev questions in ATP DRAFT... @Ely please review open questions before sprint planning."* The AC field still carries 4 live `NEEDS PO/DEV CONFIRMATION` placeholders (empty-state copy, uncovered-indicator copy — none resolved). Do not autonomous-claim until a real ratification lands. Re-confirmed unresolved 2026-08-03. |
| ~~BK-50~~ | ~~Blocked on BK-45~~ | ~~Exports the chain BK-45 renders — no BK-45 branch exists yet (not started), so BK-50 is transitively blocked regardless of its own readiness. Re-confirmed 2026-08-03.~~ ***Superseded — see the 2026-08-08 row below.*** |
| ~~**BK-50**~~ | ⚠️ ***Superseded — deferral overturned the same run, see the SHIPPED row below*** | **2026-08-08 autonomous-delivery `story` run: gate RELEASED, story DEFERRED on scope-growth.** The dependency block is genuinely gone — BK-45 merged `f75709e`, ancestor-verified — and the shift-left is real: comments `11047` (PO) and `11048` (Dev) answer all six blocking questions from `11044`. So this is neither dependency-blocked nor refinement-blocked. **It fails on as-built reality instead.** Comment `11047` chose "static HTML in Cloudflare R2 behind a no-login signed URL" and justified it with *"Cloudflare R2 already exists in our stack for file storage — no new infrastructure."* **That premise is false.** A recon of `origin/staging` found **no object storage in any form** — zero hits for R2, `@aws-sdk`, `S3Client`, presigned/`getSignedUrl`, `.storage.from(`, `@vercel/blob`; no storage SDK in `package.json`; no bucket DDL across all 68 migrations; no storage credentials in `.env.example` or `.env` — and **no export precedent whatsoever** (no CSV, no PDF lib, no `Content-Disposition`, no `@media print`, no download route, no share-link or token-gated public page). This is the identical failure shape to BK-43's 2026-08-01 ratification asserting a `secrets_ref` column that did not exist, superseded on 2026-08-05 by comment `12170`: **a ratification comment is not evidence about the codebase.** Building it as ratified needs (1) an R2 bucket plus access keys — **credentials an unattended run cannot provision, so it fails closed at the first credential step** (anti-pattern A13 forbids improvising one); (2) a new storage SDK and env vars across three Vercel scopes; (3) an `export_jobs` migration with RLS; (4) **the app's first anonymous data-access surface** — `middleware.ts:10-11` gates everything but `/login,/auth,/api/auth`, and `0068:318` explicitly revokes the traceability RPC from `public` and `anon`, so serving a full evidence chain to an anon caller is a new security posture, not the application of a ratified pattern; (5) cleanup cron where none exists (no `pg_cron`, no `supabase/functions`, no Vercel cron); (6) a server-side static-render path with no precedent, plus an ADR for both the storage choice and the anonymous-access model. **What DOES exist and is directly reusable**: the data source (`bunkai_report_story_traceability`, `0068:98-131`), the async-job pattern (`0019_import_jobs.sql`, `after()` at `app/api/v1/imports/route.ts:82`, atomic claim at `lib/jira/import-runner.ts:48-56` — though it has no retry and no scheduling), and the chain-rendering components. **Also unresolved in the ticket text**: AC Scenario E2 requires an unauthenticated link view be **rejected 403/404**, the exact opposite of ratified decision `11047` Q3 — a QA engineer building from the AC field would assert the wrong outcome. An attributed AI PO + AI Tech Lead ruling was published to the ticket by this run to settle the v1 scope and correct the R2 premise. ~~**Needs a human-present session** for the credential provisioning and the anonymous-access posture regardless of how the scope ruling lands.~~ **That last sentence is what the ruling refuted — the credential provisioning and the anonymous posture were properties of the ratified PLAN, not of the story.** |
| **BK-50** | ✅ **SHIPPED 2026-08-08 — the deferral directly above was overturned by this same run's decision pass** | **PR #145 merged 2026-08-08T19:03:06Z**, merge commit `7b16c0c`, `git merge-base --is-ancestor` verified against `origin/staging`. Branch `feature/BK-50-traceability-export-snapshot`. Live status **`Ready For QA`**, assignee **Benjamin Segovia** (the shift-left owner who authored the ATP) — verified live via REST, not from the executor's report. **Adjudication: BLOCKER 0 · MAJOR 0 · MINOR 4 (2 fixed, 2 accepted with reasoning) · NIT 0**, in both the PR body and the run report. Tests 45/45 in `lib/traceability` (13 new), `types:check` clean, lint clean. **What shipped, and what deliberately did not**: `lib/traceability/export-snapshot.ts` (+208) renders BK-45's `jsonb` chain into one self-contained HTML document — verified by inspection to contain **no external references, no `<script>`, no `<link>`, no `fetch`**, with 17 `escapeHtml` call sites guarding user-supplied titles — plus the Export button and toast on `TraceabilityChainView.tsx`. **Zero migrations, zero `package.json` or `.env` changes, zero new API routes, and `middleware.ts` untouched** — all four verified against the merge diff rather than taken on report. It reuses BK-45's shipped `GET /api/v1/projects/{id}/traceability` under the caller's existing session, so `0068:318`'s revoke from `public`/`anon` stays intact and no exposure was widened. **The anonymous-link capability was explicitly reserved for a human** by the AI Tech Lead ruling (`12238` §5) as a first-of-its-kind security posture that Rule #18 does not cover, and it was NOT built; the recommended follow-up story was deliberately not created. Divergence **D26** logged in `master-design-plan.md` §5 for the `.json` -> `.html` filename departure (no ADR: nothing schema, auth or invariant-shaped is touched, and it is a one-line reversal in `buildSnapshotFilename`). ~~**Epic BK-44 now has only BK-48 (`Shift-Left QA`) left.**~~ **Stale — corrected 2026-08-12**: BK-48 has since cleared shift-left, reached `Ready For Dev`, and was claimed by the 2026-08-12 run — see §6's 2026-08-12 conclusion. |
| BK-188 | N/A — not a codeable story | "QA Engineering Support — Complete Summary (Jun 2026)" — a QA reporting/summary artifact misfiled as `Story` type, not a feature to implement. Excluded from candidate consideration 2026-08-03; flag for PM to re-type or close. |
| ~~BK-209~~ | ✅ Resolved (comment 12070) | **SHIPPED 2026-08-03** — merged to staging via PR #113 (`feat/BK-209-notifications-inbox`), ancestor-verified. No longer Ready For Dev (now Ready For QA). Was the gate for the whole BK-208 cluster (BK-211/212/213/214). |
| ~~BK-264~~ | N/A — created mid-sprint, no refinement phase | **SHIPPED 2026-08-03** — merged to staging via PR #114 (`feat/BK-264-defect-triage`), ancestor-verified. Unblocked BK-212. |
| BK-211 | ⚠️ **Genuinely NOT resolved** | BK-209 gate is now CLEARED (merged 2026-08-03), so this is dependency-unblocked — but its own refinement is not: the "PO Ratification — 2026-07-11" comment (11407, blank one-liner) precedes the actual Q&A (11408) — the forward-dated blanket pattern this doc already flags as category (b). Q3 explicitly states *"pending PO ratification (since 2026-07-17)... still awaiting PO sign-off"* — an open question, not resolved. Do not autonomous-claim. |
| ~~BK-212~~ | ✅ Resolved (comment 11407+ chain) | **SHIPPED 2026-08-04** — merged to staging via PR #115 (`feat/BK-212-bug-notifications`), ancestor-verified. §6 previously listed this PR as still `Open`/`In Review` (stale as of 2026-08-03); corrected. No longer Ready For Dev. |
| BK-213 | ✅ **Resolved as of 2026-08-04** | Was flagged "Genuinely NOT resolved" on 2026-08-03 (Q3 non-retroactivity fixture left open). Re-checked live 2026-08-04: a genuine Dev/QA close-out comment (Carlos, 7/18) has since landed, closing every raised question with concrete reasoning; the one leftover item (test-fixture non-retroactivity) is explicitly triaged as non-blocking, not hidden or disclaimed. Dependencies BK-209 ✅ and BK-87 ✅ both merged. No existing branch/PR. Mockup exists (`settings-notifications.html`). Dev's own comment calls it "self-contained CRUD, no new code" — passes the scope-growth check. **Claimed by the 2026-08-04 autonomous-delivery `story` run.** |
| ~~BK-213~~ | ✅ Resolved | **SHIPPED 2026-08-04** — merged to `origin/staging` via PR #127 (`feature/BK-213-notification-preferences`), merge commit `2e91ad95`, ancestor-verified 2026-08-05. Migration `0062_notification_preferences.sql` (new table + RLS) applied. No longer Ready For Dev (now Ready For QA, assigned to the shift-left QA owner). The row above recorded only the *claim*; this row records the *delivery* — the 2026-08-04 reconciliation PR #126 merged before #127 landed and never picked it up. |
| BK-211 | ⚠️ **Still NOT resolved** | Re-checked live 2026-08-04: BK-209 gate remains CLEARED, but Q3 ("pending PO ratification since 2026-07-17... still awaiting sign-off") is still open — no new comment since 2026-08-03. Also still hard-blocked: depends on BK-30 (Manual Execution & Runs), which remains in Planning with no real trigger to hook into yet. Do not autonomous-claim. |
| BK-211 | ⚠️ **Still NOT resolved (2026-08-05)** — but the *reason* is narrower than the row above says | Re-checked live 2026-08-05: Q3 ("QA-proposed, pending PO ratification since 2026-07-17") is **still open** — no ratifying comment has landed since; the only newer activity is the 2026-07-30 mockup link. That alone disqualifies it. **Correction to the row above**: the companion claim "hard-blocked on BK-30, still in Planning" is *stale bookkeeping, not a functional gate* — BK-30's child stories (BK-34/35/36/37/38/39) are all dev-done and ancestor-verified, and §3.1's own edge list for BK-211 names its real dependencies as BK-209 ✅, BK-39 ✅, BK-36 ✅ — all merged. The epic ticket's `Planning` status simply was never advanced. So BK-211 is **dependency-clear and refinement-blocked**, not dependency-blocked. Still: do not autonomous-claim until Q3 gets a real PO sign-off. |
| ~~BK-205~~ | ✅ **Superseded by delivery — the row below is stale** | **SHIPPED 2026-08-05** — merged to `origin/staging` via PR #132 (`feature/BK-205-milestones-create-with-target-date`), merge commit `5054716`, ancestor-verified 2026-08-06. Live status `Ready For QA`. **Timeline matters here, because the row below reads as a live refusal and is not one**: the 2026-08-05 `story` run escalated BK-205 at 18:40Z and ended; the code merged at 20:20Z the same evening from a *different, later session*. The escalation was overtaken by events, not overruled and not ignored. Its five evidence points were accurate when written — the fifth (`no milestones table exists at all`) is precisely what PR #132 changed. Keep the row below as the historical record of why an unattended run declined it; do not read it as current state. |
| ~~BK-188~~ | N/A — left the pool on its own | Re-checked live 2026-08-06: BK-188 is now type **`Tech Story`**, status **`Completed`**. The three prior runs' "misfiled `Story`, flag for PM to re-type or close" recommendation has been actioned. No longer a candidate, no longer needs flagging. |
| BK-205 | ⚠️ **NOT genuinely resolved — nominal Ready For Dev** | **Entered the live pool since the 2026-08-04 sync. Considered and NOT claimed by the 2026-08-05 autonomous-delivery `story` run**, on a 3-lens scored judge panel (2-1 against; the lone dissent addressed only *size*, and conceded the spec ambiguities in its own risk section). Dependency-clear (§3.1 marks it `──> (free)`), mockup exists (`milestones-board.html`), and the work itself is a normal-sized additive pick (~1.3-1.6x BK-213, one new table, dense reusable CRUD precedent in `0032_project_environments_crud.sql` + the environments UI). It fails on **refinement authenticity**, not scope: (1) the live description carries, twice, *"Treat every decision below as a strong starting draft, not a final sign-off — the real stakeholders should confirm or override **before Ready For Dev**"* — a precondition never met; the AC field is still headed "Shift-Left DRAFT" with two scenarios tagged *"DRAFT, pending real PO sign-off"*. (2) Comment `12163` raised blocker C1 saying it *"needs a real PO/Dev/Design decision before Ready For Dev... not AI inference"*; comment `12164`, same non-human author ~10 min later, closed it by exactly that inference, and the same account transitioned `Estimation -> Ready For Dev` 24 minutes after closing its own blocker. Last human (PO) touch: 2026-07-30. (3) C1's resolution is a **deliberate mockup departure** (ship without BK-206's attach-plans/readiness UI) with **zero §5 Divergences row and no ADR** — Critical Rule #15 requires that ratification *first*. (4) The cited backing artifact `shift-left-refinement.md` does not exist in the repo despite four citations. (5) The internal-whitespace ratification is justified as matching *"Backend's already-built `UNIQUE(project_id, lower(trim(name)))` index"* — verified live: **no `milestones` table exists at all**. Needs a human PO/Design ratification pass before any run claims it. |
| BK-211 | ✅ **RESOLVED as of 2026-08-06 — every row above is superseded** | **Claimed and delivered to PR by the 2026-08-06 autonomous-delivery `story` run.** Three prior runs excluded this story for "Q3 unratified since 2026-07-17". That is no longer true, and the reason it took three runs to notice is itself a finding — see the `ATLASSIAN_URL` note below. Q3 was ratified live on **2026-08-05 at 19:08Z / 19:27Z** (comments `12169` AI Product Owner, `12173` AI Tech Lead) — i.e. roughly 18 minutes *after* the 2026-08-05 run had already ended at 18:50Z. **That run's exclusion was therefore correct at the moment it was made**; the ratification simply landed later the same evening, from the same session that shipped BK-205. This run added `12196` (AI PO, Q3 re-ruled with a channel-aware predicate) and `12197`/`12198` (AI Tech Lead, buildability + the superseding cost). **The "hard-blocked on BK-30" claim is now falsified at code level, not merely called stale bookkeeping**: the terminal path is real and user-reachable — `RunnerView.tsx:583/597` -> `finish\|abort/route.ts` -> `bunkai_finish_run`/`bunkai_abort_run` -> `activity_log` `run.finished`/`run.aborted`, with 89 `run.finished` + 2 `run.aborted` rows live. Delivered as **PR #137**, review-clean (0 BLOCKER/MAJOR/MINOR/NIT), **deliberately NOT merged** — it stops at the migration gate (see below). |
| ~~BK-211~~ | ✅ **SHIPPED — the gate opened and a later session finished it** | **Reconciled 2026-08-07.** The row above is the last word of the 2026-08-06 run, which ended while the story was still gate-stopped; it reads as an unmerged PR and is no longer current. Reality: **PR #137 merged to `origin/staging` 2026-08-06T22:31:07Z**, merge commit `861c441`, branch `feature/BK-211-run-terminal-notifications` ancestor-verified with **0 commits ahead** and an empty diffstat. **Both migrations were applied and re-verified**: the live ledger carries `0066_run_event_notifications` (20260806222747) and `0067_run_finish_abort_via` (20260806222820), corroborated independently by Jira comment `12201`. Live status `Ready For QA`, assigned to the shift-left QA owner. **There is no half-shipped state** — the concern that the code could land without its schema did not materialize. What unblocked it was a policy change, not a workaround: the operator set `migrations: unrestricted` the same evening (PR #139), which covers the `CREATE OR REPLACE` rewrite `0067` needed. |
| **BK-45** | ✅ **CLAIMED 2026-08-07 — a 3-run deferral overturned on evidence** | **Claimed by the 2026-08-07 autonomous-delivery `story` run** after a 4-lens scored judge panel returned **A(claim)=293 / C(claim+gates)=210 / B(defer)=107** — A and C both mean claim, only B meant defer. All three premises of the standing "scope-growth" deferral failed: **(1) dependency** — the BK-31 gate is bookkeeping (see the ES4 row above; siblings BK-46/BK-47 already crossed it on 2026-08-01); **(2) size** — the 8-point estimate is stale by construction, because `out-of-scope.md` and `master-design-plan.md:244-245` put filtering in **BK-48** and export in **BK-50** on this same mockup file, so ~half of it is not BK-45's; its own surface is 6 render states, 7 ACs, **zero mutating actions, 1 additive migration, no new tables/columns, no new ADR**, projecting to ~1800-2600 insertions / 14-18 files — BK-46 scale (2287/16), the *bottom* of the measured 1900-4200 band; **(3) refinement** — the row above says "only 2 comments total exist"; **live Jira has 4**. Comments `12171` (AI Product Owner / BA) and `12176` (AI Tech Lead), both 2026-08-05, decide **all 11 open questions plus the 4 AC placeholders plus EC11**, none of them un-decidable-without-a-human. Shift-left genuinely ran (comment `11275`, 2026-06-11, 7 Gherkin scenarios + 23 test outlines, **no practice disclaimer**), and the blocker was open 6/11→8/05 — **55 days**, not the minutes-later self-ratification pattern that disqualified BK-205. |
| ~~**BK-45**~~ | ✅ **SHIPPED — the row above stops at "claimed" and is no longer current** | **Reconciled 2026-08-08.** The row above is the last word of the 2026-08-07 run as it stood mid-run; the story went on to merge the **same day**. **PR #142 merged 2026-08-07T19:10:05Z**, merge commit `f75709e`, branch `feature/BK-45-us-bug-traceability-chain`, `git merge-base --is-ancestor` verified against `origin/staging`. Migration **`0068_story_traceability_report.sql`** applied; live status **`QA Approved`**, assigned to the shift-left owner. Shipped surface: the `bunkai_report_story_traceability(p_actor_user_id, p_user_story_id)` RPC, `GET /api/v1/projects/{id}/traceability?story={id}`, and `components/traceability/TraceabilityChainView.tsx`. **Why the doc missed it**: the 2026-08-07 reconciliation PR #141 (`f59c095`) merged at 19:17Z — *seven minutes after* `f75709e` — but its content was authored before that merge and never picked it up. **This is the third occurrence of the same pattern** (BK-213 on 2026-08-04, BK-211 on 2026-08-06, BK-45 now): a run reconciles the roadmap, then ships a story, and the reconciliation it already cut under-reports its own run's outcome. **A run that both reconciles this doc and ships a story must write its roadmap commit LAST, or amend it after the story merges.** **Benign ledger anomaly to expect**: the live ledger holds two `0068` rows (`0068_story_traceability_report` and `..._v2`) against one file on disk, because the migration was applied twice; the final live definition matches the committed file, byte-diffed. A ledger-vs-disk audit will flag it — expected, not drift. |

**2026-08-03 run conclusion**: no story in the live Ready-For-Dev pool passed all eligibility gates (dependency-clear + genuinely refined + unclaimed + not oversized). Story-mode run ended empty — see run report.

**2026-08-04 run conclusion**: BK-213's refinement genuinely resolved since the prior day's check (see row above) — claimed and dispatched to `/sprint-development`. All other candidates unchanged from 2026-08-03 (BK-43 still deferred/scope-growth, BK-45/BK-211 still unresolved, BK-50 still transitively blocked, BK-188 still not a codeable story). **BK-213 subsequently merged to `origin/staging` via PR #127 the same day** — recorded in the table above on 2026-08-05, since PR #126 had already been cut when it landed.

**2026-08-05 run conclusion**: **empty run — no story claimed.** Live pool was 6 (BK-43, BK-45, BK-50, BK-188, BK-205, BK-211); all six dropped. BK-43 (scope-growth deferral, unchanged), BK-45 (11 open questions, unchanged), BK-50 (transitively blocked, unchanged), BK-188 (not a codeable story, unchanged), BK-211 (Q3 still unratified — though its BK-30 blocker claim was corrected as stale, see row above), and **BK-205** — the only new candidate — judged NOT eligible on refinement authenticity by a scored judge panel (see its row). The run's deliverable was this reconciliation instead. An empty run is the designed outcome when nothing is genuinely unblocked; the alternative would have been to build a story whose product decisions no human has ratified.

**2026-08-06 run conclusion**: **BK-211 claimed, built, and delivered to PR #137 — stopped at the migration gate, deliberately unmerged.** Live pool was 4, not 6: BK-188 has left it (now `Tech Story` / `Completed`) and BK-205 has shipped. Of the remaining four — BK-43 (scope-growth deferral, unchanged: a whole outbound-integration surface behind a 1-point estimate), BK-45 (scope-growth: 8 points, highest in the pool, a brand-new Traceability screen), BK-50 (**the one legitimate dependency block** — needs BK-45's assembled-chain response shape, and BK-45 has no merge commit on staging), BK-211 was selected.

Two things about this run are worth carrying forward, because they change how the previous three runs should be read:

1. **The governing rule changed between runs.** Commit `5125a9c` ("align decision authority with CLAUDE.md Rule #18") merged 2026-08-06 07:22 — *after* the 2026-08-05 run. Under the older rules an unratified product question was an escalation; under Rule #18 it is work to do. Several exclusions in the rows above were correct under the rules in force when written and would be decided rather than deferred today. ~~**BK-43 and BK-45 are NOT in that category** — they fail the scope-growth check, which Rule #18 does not override.~~ **Half of this is withdrawn (2026-08-07): BK-43 does fail the scope-growth check; BK-45 does not, and was claimed.** Rule #18 was never the reason BK-45 became eligible — the deferral simply rested on three premises that measurement contradicts (see its row above). The lesson worth carrying: a deferral repeated across runs acquires the *appearance* of a settled finding while nobody re-tests it. This one had been restated four times without anyone opening the mockup's `out-of-scope.md` or counting the live comments.
2. ~~**BK-211's delivery is gated on a migration, and the gate is doing its job.**~~ **Resolved the same evening — see BK-211's SHIPPED row above.** The paragraph below was accurate when written and is kept as the record of why the run stopped; it is not current state. Both migrations were applied 2026-08-06T22:27-22:28Z and PR #137 merged at 22:31Z, after the operator raised the policy to `migrations: unrestricted` (PR #139), which covers the `CREATE OR REPLACE` rewrite that `0067` required. Original text follows.
   **BK-211's delivery is gated on a migration, and the gate is doing its job.** The ratified suppression predicate (`12196`) needs the interactive-vs-automated session signal to reach the trigger. `principal.via` exists at the HTTP layer but does not reach the DB, and `runs.executor_mode` is not a usable proxy (it is stamped at run *start* and describes intent, not how the run was closed). So the story requires adding `p_via` to `bunkai_finish_run` and `bunkai_abort_run` — a **rewrite of two live `SECURITY DEFINER` functions**. `.agents/project.yaml` sets `migrations: autonomous`, which covers **additive DDL only** and still stops for drop/rename/rewrite. Both migration files (`0066_run_event_notifications.sql` additive, `0067_run_finish_abort_via.sql` rewrite) are written and committed; **neither is applied**. Applying only the additive one would leave the trigger reading a `via` nothing writes, silently breaking AC Scenario 5 — so nothing was applied at all, rather than half.

> **`ATLASSIAN_URL` — the defect that made three runs read a dead Jira, discovered 2026-08-06.** `.env:41` and `.agents/project.yaml:32` both correctly name `upexgalaxy71.atlassian.net`. The **shell environment** of these routine sessions carries a stale export pointing at the pre-migration `upexgalaxy69` instance, and Bun will not let `.env` override an already-set process var. So `bun run jira:sync-issues` silently rebuilt `.context/PBI/` from the **dead** instance, exit code 0, no warning. `acli` was never affected (it authenticates to site 71 independently) — which is exactly why live queries and comment posting always worked while the cache quietly rotted. Measured blast radius on BK-211: **3 cached comments vs 7 live**, and the 4 missing ones were the entire 2026-08-05 ratification set. Any run that had trusted the cache would have re-excluded BK-211 indefinitely. **Every sync must be prefixed** `ATLASSIAN_URL=https://upexgalaxy71.atlassian.net/ bun run jira:sync-issues ...` until the operator clears the stale export from the profile that launches these routines and restarts the session (`/jira-instance-migration` covers this class of change). **A future run finding the PBI cache disagreeing with live Jira should check this env var before concluding a ticket is unrefined.**

**2026-08-07 run conclusion**: **BK-45 claimed and dispatched — the pool's longest-standing deferral overturned.** Live pool was exactly **3** (BK-43 1pt, BK-45 8pt, BK-50 5pt); BK-211 has left it (shipped, `Ready For QA`). Nothing was in flight anywhere: **zero open PRs, zero unmerged branches carrying a BK key**, zero items `In Progress`. Of the three — **BK-43** stays deferred (architectural novelty plus a per-workspace-credential security posture; its stated rationale was factually wrong and is corrected in its row above), **BK-50** is the one genuine dependency block (it exports the chain BK-45 renders, and BK-45 is not yet on staging), and **BK-45 was selected** on a 4-lens panel — see its row.

Three things from this run change how the rows above should be read:

1. **A repeated deferral is not a verified one.** BK-45 was deferred four times on premises nobody re-tested: a dependency edge that its own sibling stories had already crossed, a point estimate that predated the carve-out of filters and export into BK-48/BK-50, and a "not resolved" verdict counted from 2 comments when live Jira had 4. Each restatement made the next one cheaper to write. **When a candidate is dropped for the same reason two runs running, the reason itself is what the next run should audit** — not the ticket.
2. **The step-0 actor bind in every report RPC is inert on the real call path, and this is a live finding, not a BK-45 detail.** All three shipped report RPCs open with `if auth.uid() is not null and auth.uid() <> p_actor_user_id then raise ... 'project_not_found'` (`0048:69-71`, `0049:161-162`, `0052:101-102`) — but the routes reach them through `createAdminClient()` (`app/api/v1/projects/[id]/coverage/route.ts:29`), so `auth.uid()` is NULL and the guard short-circuits, exactly as `0049:47-52` documents. **The most-copied line is the least protective one.** The load-bearing control is the per-CTE `project_id = p_project_id` predicate — per-query judgment, not mechanically copyable, and precisely what failed live in `0047`. Compounding it, **`project` is not an RLS boundary anywhere in this schema** (`0049:36-44`), so a missing predicate leaks across projects *inside* a workspace with no backstop. Every future report RPC inherits this; it belongs in ADR-0012's orbit, not in one story's plan.
3. **Two claims in the record were wrong and are corrected here.** The escalation log's 2026-08-06 assertion that ruleset `bypass_actors` is `null` and "nobody bypasses, admin or not" is false as read on 2026-08-07 — it is `[{actor_type: "OrganizationAdmin", bypass_mode: "always"}]`, and `current_user_can_bypass` is `"always"` for the automation account. And BK-43's "no connector/webhook/retry pattern exists yet anywhere in this repo" is false — `lib/jira/client.ts` plus `0019_import_jobs.sql` and `after()`-triggered `lib/jira/import-runner.ts` are exactly that pattern.

> **Ticket hygiene, surfaced not fixed (2026-08-07)**: BK-45 still carries **10 literal `NEEDS PO/DEV CONFIRMATION` strings** in its field text — 4 in Acceptance Criteria, 6 in the ATP — although comments `12171` and `12176` resolved every one of them on 2026-08-05. Not overwritten by this run: rewriting the AC field wholesale risks clobbering the 7 ratified Gherkin scenarios, and a field rewrite is a heavier act than the defect warrants. Flagged for whoever runs QA on it, so the stale placeholders are not mistaken for live gaps.
>
> This also puts a boundary on the "systemic shift-left gap" pattern flagged above. That pattern is real and BK-205's changelog evidence stands. But **BK-211 was not an instance of it** — its ratification genuinely existed and was simply invisible to the cache. Two different failures that produce the identical symptom ("story looks unratified"), and they need different fixes: one is a process question about who may close a shift-left blocker, the other is one stale environment variable.

**Discovery, not yet fixed**: `bun run jira:sync-issues get <KEY> --include-comments` calls
`GET /rest/api/3/issue/{key}/comment` with no pagination params and no ordering assumption bug
visible in the code, yet independently and reproducibly omitted the single newest comment on
BK-42, BK-43, and (checked for completeness) BK-41 — each confirmed present, `visibility: public`,
via a direct `acli jira workitem comment list --paginate --json` call moments later. Root cause not
diagnosed (out of scope for this pass); flagged here so nobody trusts "no comments since X" from the
local cache alone without a live cross-check, and a follow-up ticket should investigate
`fetchComments()` in `scripts/sync-jira-issues.ts`.

> **Systemic pattern flagged 2026-08-01, still recurring 2026-08-03** (see escalation log): a recurring shift-left gap — stories reach `Ready For Dev`/`Ready For QA` via either (a) explicitly-disclaimed "simulated/practice" refinement answers, or (b) a blanket forward-dated "AI-as-PO" ratification comment that precedes the actual Q&A it claims to cover, or (c) a self-reversed claim of PO confirmation with no independent human artifact behind it. This is not one story's problem — it hit BK-3, BK-41, BK-42, BK-43, BK-90, BK-209, and (2026-08-03) BK-211, BK-212, BK-213 independently. Worth a process-level look, not a per-ticket fix.
>
> **Still recurring 2026-08-05, and now with a measurable cost.** BK-205 is the clearest instance yet, because the self-ratification is visible in the changelog rather than only inferable: the same non-human account raised a blocker as *"needs a real PO/Dev/Design decision... not AI inference"*, closed it by inference ten minutes later, and transitioned the story `Estimation -> Ready For Dev` twenty-four minutes after that — with the story's own description still instructing that the real stakeholders confirm *"before Ready For Dev"*. The cost is no longer hypothetical: this pattern is now the **sole** reason the 2026-08-05 story run shipped nothing, since BK-205 was otherwise dependency-clear, mockup-ready and correctly sized. Each occurrence also quietly converts a *product* decision into an *implementation* one — BK-205's C1 ratifies a mockup departure that Critical Rule #15 says needs a §5 row plus an ADR first. The process fix (who is allowed to close a shift-left blocker, and what artifact proves it) is worth more than any single ticket it is currently costing.

### Edge-mapping TODO — stories seen on the board but not yet in the §3 graph

- **BK-98** "TMS-Projects | Tree / Table / Mind-map views in a hardened explorer" — lands the `EPIC-BK-008` "Views" surface as a story under BK-7; resolves part of the §2.1 ⚠️ "Views folded into BK-7" note in `master-implementation-plan.md`. Add a §3 edge if it gains downstream dependents.
- **BK-101** "🚀 TMS-Workspace | View the workspaces I belong to" — **Resolved (2026-06-20)**: BK-101 was a **duplicate** of BK-89 and has been **deleted from Jira** (user-confirmed). **BK-89 stands as the real story** — it keeps its ES2.5 edge, is not superseded. No further action.
- **BK-47** "TMS-Automation | Time-to-Green trend" — **found 2026-08-04, previously missing from this doc entirely.** ✅ SHIPPED — merged to `origin/staging` via PR #98 (aggregate of a slice chain on `feat/BK-47-...`), ancestor-verified. Add a §3 edge if it gains downstream dependents; none identified yet.
- **BK-266** "TMS-Projects | Projects index" — **found 2026-08-04, previously missing from this doc entirely.** ✅ SHIPPED — merged to `origin/staging` via PR #119, ancestor-verified. Add a §3 edge if it gains downstream dependents; none identified yet.
- **BK-147** "persistent app shell + route-driven workbench tabs" — **found 2026-08-05, previously missing from this doc entirely (zero mentions).** ✅ SHIPPED — merged to `origin/staging` via PR #43 (2026-06-19), ancestor-verified. Notable as a coverage gap because it is the shell every later workbench-tab story renders into (ADR-0003), so it is an implicit upstream of much of §3 without ever appearing as an edge.
- **BK-148** "Project Environments" — **found 2026-08-05, previously missing as its own entry.** ✅ SHIPPED — merged to `origin/staging` via PR #49 (2026-06-21), ancestor-verified. The doc referenced it only obliquely, inside BK-34's gate note at §4 ("env CRUD operates on the `project_environments` table"), and never gave it a shipped-status line of its own despite it being one of the earliest-shipped stories in the graph. Its `0032_project_environments_crud.sql` migration is now the reference precedent for per-project case-insensitive-unique-name CRUD (cited in BK-205's §6 row).
- **BK-265** "reach Runs, Bugs and Metrics from a project sub-nav" — **found 2026-08-05, previously missing from this doc entirely (zero mentions).** ✅ SHIPPED — merged to `origin/staging` via PR #118 (2026-08-04), ancestor-verified. Owns `project-sub-nav.tsx`, so any future story adding a project-scoped section (e.g. BK-205 Milestones, BK-202 Test Plans) extends its nav array — worth an edge once one of those is claimed.
- **BK-398** "Command Palette | Search and jump across the workspace" (epic BK-7 Project & Module Hierarchy) — **found 2026-08-12, created the same day by the discovery routine.** ~~Live status `Shift-Left QA`. No `master-design-plan.md` §8 screen-map row exists for it yet — outstanding, not fixed by this pass (a concurrent agent may be writing its own §5 row to that same file this run).~~ **✅ SHIPPED 2026-08-15** — this edge-mapping gap is now closed, not merely tracked: merged to `origin/staging` via PR #173, merge commit `3f6816f`, ancestry-verified. Full addendum in §3.1's new BK-7 block above. No longer outstanding.
- **BK-399** "TMS-ATC Classification | Classify by test-design technique and priority" (epic BK-13 ATC Library) — **found 2026-08-12, created the same day by the discovery routine.** Live status `Backlog`. No `master-design-plan.md` §8 screen-map row yet — same outstanding item as BK-398 originally was. Add a §3 edge once refined; none identified yet. **Still outstanding as of 2026-08-15** — not shipped, not re-verified this pass beyond the edge check below.
- **BK-315** "TMS-ATC Library | Export a Project's ATCs to CSV" (epic BK-13 ATC Library) — **found 2026-08-15, previously missing from this doc entirely.** ~~Live status `Backlog`, 1 SP.~~ **Corrected 2026-08-21: live status `Ready For Dev`, 1 SP confirmed.** **Deliberately NOT picked on 2026-08-21 despite being the cheapest story on the board**, for two independent reasons. (a) **Hard mockup gate, unsatisfied** — `master-design-plan.md:604`: *"export control mockup-gated, unratified … Do not invent the control."* Verified in the repo rather than the field: the only "CSV" string in `atc-library-global.html` is sample ATC title text. Now registered in §5. (b) **An unresolved PO-vs-Dev conflict sits on top of it** — PO ruled "no hard cap"; Dev then counter-proposed a 5,000-10,000 row cap *or* a `ReadableStream` rewrite and explicitly asked for a PO sign-off that never came, warning it changes the estimate. Also note the AC field is stale in the reassuring direction: a PO comment asserts six scenarios "drop their NEEDS PO/DEV CONFIRMATION flag" while the live AC field (`customfield_10097`) still carries all six — the answers were never written back. **Zero Jira issue links** (`acli jira workitem link list --key BK-315` → `issueLinks: []`) — no Dependencies/Relates edge exists to record. Its own `out-of-scope.md` explicitly disclaims the cross-project ATC Library index: *"the global ATC Library view... is ahead of this story's scope and belongs to a future story"* — so it does NOT depend on the BK-439/440/441 cluster. It reads the already-shipped per-project ATC library (BK-18/BK-19/BK-20, all `Ready For Release`/`QA Approved`). **No incoming edge identified** — genuinely unknown/absent, not omitted.
- **BK-269** "TMS-Run Execution | Automatically abort abandoned runs after inactivity" (epic BK-30 Manual Execution & Runs) — **found 2026-08-15, previously missing from this doc entirely.** ~~Live status `Backlog`, 1 SP.~~ **Corrected 2026-08-21: live status `Ready For Dev`, 3 SP** (`customfield_10036` = 3.0, queried live). **The three PO questions this row called "genuinely open" were ANSWERED on 2026-08-17** — that clause is stale and is retired here. **But BK-269 was still DEFERRED on 2026-08-21, and the reason is not refinement:** its answers commit the story to infrastructure that exists nowhere in this repo — a `pg_cron` job every 15 minutes, a Supabase Edge Function, a new service-role `POST /api/v1/admin/sweep/run-timeout` route, and a new `runs.last_step_activity_at` column with a write path in the mark-step endpoint. Verified absent this pass: `grep -rl "pg_cron|cron.schedule" supabase/ lib/ app/` → no hits; `supabase/functions` → no such directory; `vercel.json` → does not exist. **An unattended run cannot observe a scheduled sweep actually firing inside its own window**, so the story's core behaviour is unverifiable end-to-end by the run that ships it. 3 SP on paper, greenfield in practice. **Separately, its decision comments violate Critical Rule #18** — headed `## PO Responses` / `## Dev Responses` and signed `Confirmed by: PO (Ely)` / `Confirmed by: Dev`, posted by an account that is not Ely, i.e. an AI decision styled as human sign-off. Not rewritten by any run; **operator item.** If a future run claims it, the workable shape is to ship the endpoint and the column and defer the cron wiring to a follow-up. **Zero Jira issue links.** Its own `business-rules.md` states the recommended approach *"reuses the abort capability a QA Engineer already has today"* rather than a new Run status — a soft precedent on BK-36 (Abort a run in progress, ✅ shipped, `QA Approved`), not a hard Jira-linked gate; BK-36 being already dev-done means this is not a blocking edge in practice. Three PO questions remain genuinely open (inactivity threshold value, per-workspace/per-project configurability, owner notification) — a refinement gap, not a dependency gap. **No incoming edge identified.**
  - **✅ CLAIMED AND SHIPPED 2026-08-24** (interactive `/sprint-development`, operator on the line). **Everything above from "still DEFERRED" onward is now historical — do not read it as current state.** The deferral rested on the premise that the story required greenfield infrastructure, and that premise was **half wrong**: the Edge Function, the `supabase/functions/` directory, the `POST /api/v1/admin/sweep/run-timeout` route, the `CRON_SECRET` and the `runs.last_step_activity_at` column were all correctly identified as absent — **but none of them turned out to be necessary.** The three prior runs verified absence in the repo and concluded unavailability; the check that actually settled it was `pg_available_extensions`, which shows **`pg_cron` 1.6.4 available on the live project**. Shipped as a pure in-database sweep: `create extension pg_cron` + a `SECURITY DEFINER` function taking no caller-supplied identity, scheduled every 15 minutes. **No new principal class, no ADR-0001 amendment, no HTTP surface, no secret, no schema change** — the idle signal is `coalesce(max(run_steps.executed_at), runs.started_at)`, the same signal `lib/home/active-runs.ts` already computes. **Lesson worth carrying**: "grep finds no usage in the repo" proves a thing is unused, never that it is unavailable — three consecutive runs deferred a 3 SP story on that conflation.
  - The three "genuinely open" PO questions are **CLOSED**: threshold **4h** (function default, operational value a literal in the `cron.job` row, floor guarded by SQLSTATE `45215`); per-workspace configurability stays **out of scope** and needs no settings table today; owner notification is **resolved by inheritance** — the live `activity_log_notify_run_event` trigger (`0066`) already notifies the Run's starter on any `run.aborted` row, and with `actor_user_id = NULL` its suppression predicate is false, so it fires with no new code. Decided by a scored **AI Product Owner / AI Tech Lead** panel and published to the ticket **correctly attributed**, which also supplies the missing Critical Rule #18 counter-example this row complains about — the older `## PO Responses` / `## Dev Responses` comments are still mis-signed and remain an **operator item**.
  - **First production pass, observed:** 379 Runs sat in `running`, all 379 idle past 4h, the oldest started **2026-06-20**. The cron closed every one within its first tick across 9 workspaces, writing 379 `activity_log` rows carrying `via='sweep'` and 379 notifications to 6 distinct starters. The active-runs count went 379 → 0. That is the corrupted-dashboard problem this story was written to fix, measured.
- **BK-268** "TMS-Activity | Surface ATC edits with the Tests they affect" (epic BK-44 Coverage & Traceability) — **found 2026-08-15, previously missing from this doc entirely.** Live status `Backlog`, 1 SP. **Zero Jira issue links.** Its scope requires an ATC edit to appear in the workspace activity feed *"on the same terms as ATC creation... the feed already tracks"* — a soft dependency on BK-49 (TMS-Activity | Stream a read-side feed, ✅ shipped, `Ready For QA`), already satisfied by that ship. Its own "Dependency note" cites `ADR-0009` (ATC edit propagation contract, still `Proposed`) as context only, stating explicitly it *"does not ratify or modify that ADR."* Two PO questions remain open (whether an edit should also notify watchers; rendering when the affected-Tests count is large). **No incoming edge identified.**
- **Epic BK-183** "QA Defect Management" — **found 2026-08-15, never named anywhere in this document despite being live since 2026-06-25.** Not a feature epic — its own `epic.md` calls it *"the default intake container for defect-shaped work items... it carries no coherent product scope, by design,"* spanning 40 children across unrelated features (auth, workspace switching, module creation, ATC builder, markdown editor, coverage/traceability, PAT issuance, staging-deploy issues). Its `epic.md` description text still names **BK-262** (`PAT | Enforce capability scopes on every non-ATC route`) as its one Story-type child, but the `epic-tree.md` refresh run in this same pass shows BK-183 at **0 Stories** and BK-262 now listed under **epic BK-1** (Tenancy & Identity) instead — a live re-parent this pass did not cause and cannot explain from local data; flagged for the operator as a discrepancy between BK-183's own (stale) description prose and its current live child set. BK-262 is **`ABORTED` as of 2026-08-17**, split by attributed ruling into BK-497 / BK-498 / BK-499 under epic BK-1 (see the 2026-08-17 sync header). ~~BK-262 itself is `Ready For Dev`, 21 SP, still contradicting the 2026-08-13 published ruling that it must not be promoted (see the 2026-08-15 run conclusion below, discrepancy D2, unresolved)~~ — **that claim was retired twice over**: D2 was closed as a mis-diagnosis on 2026-08-16 (no comment on BK-262 ever forbade its own promotion), and the ticket no longer exists as a pickable candidate at all. No §3 edge applies to BK-183 either way — it is an administrative bucket, not a dependency node.
- **BK-442** "TMS-Run History | Compare a run against the previous run of the same test" (epic BK-30 Manual Execution & Runs) — **found 2026-08-18, previously missing from this doc entirely (zero mentions).** Live status `Backlog`, unpointed. Reads the run-history surface BK-37 already ships (`Ready For QA`), so its substrate exists; **no incoming edge identified** and shift-left has not been run. Absent from `master-design-plan.md` §8 — outstanding, tracked not fixed.
- **BK-443** "App Shell | Save, name, and return to a filtered view" (epic BK-7 Project & Module Hierarchy) — **found 2026-08-18, previously missing from this doc entirely (zero mentions).** Live status `Backlog`, unpointed. Cross-cuts every filtered surface shipped so far (BK-48 traceability filters, BK-41 defect list, BK-46 coverage), which makes it an implicit *downstream* of several §3 nodes rather than a blocked one — the same shape BK-147 was recorded as, in reverse. **No incoming edge identified.** Absent from §8. Not refined.
- **BK-465** "TMS-Defect Filing | Capture expected and actual results at filing" (epic BK-31 Bug Reports) — **found 2026-08-18, previously missing from this doc entirely (zero mentions).** Live status `Backlog`, unpointed. Extends the defect-filing path BK-40 ✅ shipped; **note the adjacency to BK-500 — shipped 2026-08-18** (merge `a30dfad`, ancestor-verified), which *was* a live defect in that same filing dialog (`evidence_url` seeded unfiltered). Whoever claims BK-465 should read BK-500 first — they touch the same surface. **No incoming edge identified.** Not refined.
- **BK-467** "🚀 TMS-ATC Library | Export a Project's ATCs to CSV" (epic BK-13) — **found 2026-08-17, appeared in this doc only inside a sync header until now.** Live status `Ready For QA`, 1 SP, **with no commit and no branch anywhere in git** (ancestry-verified across three separate runs). It is **not** the H1 merge-automation trap: its changelog holds a single `Backlog -> Ready For QA` transition, and its QA owner has said they created it by accident. It is a **mis-clone of BK-315** (identical summary, same epic), with **no Jira link between the two** — that pairing is this document's inference, flagged as such. Deliberately left uncorrected by every run that has found it: reversing another person's ticket administration is not an unattended run's call. **Operator item.**
- **BK-492** "ELIMINAR ESTA USER STORY" — **found 2026-08-18.** Live Story, `Backlog`, 1 SP, **no parent epic**, last touched 2026-08-17. Titled by its own author as garbage to be deleted, yet it still counts toward the open-Story total and will keep surfacing in every §6 pool query until someone removes it. Not deleted by this run — deleting a tracker item is irreversible and belongs to a human. **Operator item.**
- **BK-500 / BK-502** (Bugs, epic BK-183) — **found 2026-08-18, previously missing from this doc entirely.** Both filed 2026-08-17 by the `bug` routine and **BOTH SHIPPED 2026-08-18** by the `bug` run that filed them — this row recorded them as *"Both live `Open`"* for one day after they merged, and that is the correction. Git decides, not the tracker: **BK-500 merged `a30dfad` (PR #184)** and **BK-502 merged `6370891` (PR #183)**, both `git merge-base --is-ancestor <sha> origin/staging` exit 0, re-verified 2026-08-19 after a fresh fetch. BK-500 was the `origin/staging` tip when this row was written on 2026-08-19; the tip is now `67f76b3`. BK-500: the report-bug dialog seeds a step's `evidence_url` unfiltered, so a legacy non-`http(s)` value 422s — same surface as BK-465 above. BK-502: `jira:sync-issues --include-comments` is claimed to silently drop comments for **Bug and Improvement** types. **BK-502 is unverified in either direction as of 2026-08-18**: this run checked live-vs-synced comment totals on four Story-type issues (BK-219 3/3, BK-497 0/0, BK-498 0/0, BK-262 6/6) and found Story-type sync intact, but did not test the Bug/Improvement path the bug actually claims. Recorded as untested, **not** as refuted — the distinction matters, because this document has twice recorded a false absence from the wrong tool. **That untested/refuted distinction still stands as of 2026-08-19** and is unaffected by the ship: BK-502's fix landed, but the Bug/Improvement comment-sync path it claims was never exercised, so the underlying claim remains unverified rather than closed.
- **Epic BK-70** "QA Test Repository" — **found 2026-08-15, never named anywhere in this document.** **Zero Stories** (`.context/PBI/epic-tree.md`: Stories 0, Points 0) — its own `epic.md` confirms it holds 116 children, all issue type `Test` (QA test cases), grouped by a summary-prefix convention against 11 feature stories (BK-6, BK-9, BK-28, BK-32, BK-33, BK-34, BK-38, BK-39, BK-40, BK-42, BK-50), not by product scope. Recorded here as an **empty epic** per this pass's instruction — no Story-level dependency edge applies; it is a Test-item repository, not a feature epic.
- **BK-542** "report undeclared route postures instead of throwing" (Bug, against BK-497's posture scanner) — **found 2026-08-21, zero mentions anywhere in this document, and no PBI cache file under `.context/PBI/bugs/` either.** ✅ **SHIPPED 2026-08-20** by the `bug` routine — PR #190, merge `323b01c`, `--is-ancestor` exit 0. Three decisions from that run are recorded in the escalation log and should not be re-derived: the unbalanced-paren throw in `postureAt` deliberately **stays** a throw (a syntax error cannot reach `bun test` through a skipped type check, so degrading it would convert an unparseable file into a quiet row — a fail-open in the file that exists to close one); `UNDECLARED_POSTURE` is the **falsy empty string**, not a truthy `'undeclared'` sentinel, because the assertion meant to catch it filters on `!row.posture` and a truthy value would pass vacuously; and the pre-existing `lib/runs/start-run.test.ts` ATC-01 failure on `origin/staging` (`step_count` expected 1, received 2 — shared-instance test-data drift) was **reported, not filed**, because filing belongs to `discovery` mode's capped remit. That drift is still open. **Operator item.**
- **BK-507 / BK-508 / BK-513 / BK-554 / BK-555** — **found 2026-08-21, zero mentions in this document.** All five are live Stories that gained a `master-design-plan.md` §8 US→Screen row via PR #189 (`1547636`, inside merge `4dea987`, ancestry exit 0) and **no roadmap row at all** — the same coverage-gap class this section has now logged seven times. All five are 🔒 mockup-gated and unratified (see the new §5 row). No incoming edges identified; none refined. Add §3 edges once refined.
- **BK-512** "workspace deletion semantics" — **found 2026-08-21, zero mentions.** The **ADR ratifying its semantics shipped ahead of the mockup** (`b98c4c8`, inside merge `4dea987`, PR #189, ancestry exit 0) — the story itself is unshipped and 🔒 mockup-gated. Worth distinguishing from the rows above: this one has a ratified decision waiting on a screen, not an unrefined idea.
- **BK-571 / BK-572** — **found 2026-08-21, zero mentions.** Both gained §8 rows via PR #191 (`2c3f38a`, inside merge `3fd06c8`, ancestry exit 0) — which is the current `origin/staging` tip — and neither has a roadmap row. Both 🔒 mockup-gated and unratified. No incoming edges identified; not refined.
- **BK-591 / BK-592** (Bugs, epic BK-183, filed against BK-202's Test Plans surface) — **found 2026-08-22, zero mentions in this document.** Both were filed 2026-08-21, the day after BK-202 merged, and linked with `Blocks` rather than the expected `Defect` link type. **BOTH SHIPPED 2026-08-21** by the `bug` run (session `5a18e97a`): BK-591 merge `bf39c39` (PR #195), BK-592 merge `40c58ba` (PR #196), both `--is-ancestor` exit 0; the cache resync landed as PR #197 (`4bf7501`). Two standing findings from that run are carried here so they stop being invisible: **(a)** `supabase/migrations/0064_milestones.sql` lines 49 / 139 / 220 hold the **identical** Postgres `\s` / U+00A0 defect BK-591 just fixed, and `lib/milestones/` carries BK-592's duplication — deliberately not fixed, and **structurally unfileable under the current mode split** (a `bug` run may not file, and `discovery`'s cap is spent on stories). **Operator item.** **(b)** the comment in `supabase/migrations/0073_test_plans.sql:24` and in `lib/test-plans/validation.ts:25-27` asserting that Postgres `\s` does not match U+00A0 is **FALSE**, and was verified false against the live database rather than argued.
- **BK-593 / BK-594** ("Attach a screenshot as evidence on a step result" / "Attach screenshots as evidence when filing a defect") — **found 2026-08-22, zero prior mentions.** Both gained `master-design-plan.md` §8 US→Screen rows via PR #198 (merge `772e394`, ancestry exit 0), at lines 623 and 634. **Neither is mockup-gated** — both rows read `✅ covered` (BK-593 against `run.jsx` §4.5 lines 224-227, which already draw `Notes & evidence · optional · attached to step result` with a `📎 Attach` button; BK-594 across the runner's report-bug drawer plus `bug-detail.html`) — so they do **not** belong in the §5 🔒 registry, and they are the only two newly-mapped stories in weeks whose design gate is satisfied on arrival. No incoming dependency edge identified. Refinement state not checked here.
- **BK-269 and BK-499 have no `master-design-plan.md` §8 US→Screen row**, which Critical Rule #15 requires before dev starts. **Both are genuinely non-UI** (BK-269's Mockup field says *"No new screen"*; BK-499 puts UI out of scope) and §8 does carry rows for no-screen stories — BK-371 and BK-398 both have one. So this is a **bookkeeping gap, not a gate**: a row should be added for each. Recorded rather than fixed here, because §8 is a concurrently-edited file and this run already holds an open branch against the roadmap. **Re-verified 2026-08-22** — `grep -c 'BK-499\|BK-269' .context/design/master-design-plan.md` still returns **0**. **Re-verified 2026-08-24 — that grep now returns `1`, not `0`, and the delta is not what the count suggests.** The single hit is the literal string `BK-499` inside **BK-595's** §8 row (`master-design-plan.md:608`), which declares BK-595's *"Sequence AFTER BK-499"*. **Neither BK-499 nor BK-269 has a §8 row of its OWN** — the substantive point above is unchanged and the bookkeeping gap is still open; only the grep's arithmetic moved, and a future run must not read `1` as "closed". The status of the gap has changed for BK-499 even though the text has not: **BK-499 SHIPPED on 2026-08-21 without ever getting its §8 row**, so for that story this is no longer a pre-dev gate question — it is a permanent traceability hole in the US→Screen map. Nothing structural blocks closing it: PR #198 added §8 rows for BK-593 and BK-594 the same week. **PARTIALLY CLOSED 2026-08-24** — BK-269's §8 row was added in the same PR that implements it (under **BK-30 Manual Execution & Runs**, between BK-39 and BK-442), so half of this gap is gone and `grep -c 'BK-269' .context/design/master-design-plan.md` now returns non-zero. **BK-499's hole remains open and is now purely retrospective** — that story shipped without its row on 2026-08-21 and nothing since has added one.
- **BK-595 / BK-596** ("TMS-ATC API | Read a single ATC by id" / "TMS-Test | Rename a Test, archive it and restore it") — **found 2026-08-23, zero prior mentions anywhere in this document.** Both were created on 2026-08-23 by the `autonomous-delivery` `discovery` run `e5f98d79` and both gained `master-design-plan.md` §8 US→Screen rows via PR #202 (merge `3548d13`, ancestry exit 0), at lines 608 and 613. **Their §8 gates differ and the difference matters.** **BK-595** (epic BK-13) is **not gated at all** — its row reads *"n/a — API-only, no mockup applicable"*, following the BK-18 backend-row precedent; its contract is already published at `.context/SRS/api-contracts.yaml:314` (`get` with `expand`) and it is the **only endpoint named by literal path** in the `non-functional-specs.md` §1 performance table (`GET /atcs/{id}`, `< 200ms p95`), a target that cannot be measured while the route answers 404. It carries a **declared sequence: AFTER BK-499**, which is satisfied — BK-499's merge `f7d3bbc` is ancestor-verified on `origin/staging` (exit 0). **BK-596** (epic BK-24) carries a **split verdict**: rename/delete is COVERED by the existing explorer context menu (`project.jsx:283` draws `Rename` F2 and a danger `Delete`; `data.js:50,70,71` place `type: 'test'` nodes under modules), while the **archive/restore vocabulary is 🔒 mockup-gated and unratified** — no mockup anywhere draws an archived-row treatment, a restore action, an include-archived toggle or the in-use warning. It reuses BK-571's vocabulary rather than inventing a second one, and is sequenced **AFTER BK-571**. Neither has a §3 dependency edge; add one if either gains downstream dependents. **Both are live status `Backlog`, unpointed and never through shift-left** — so neither is claimable by a `story` run until it is refined, which is a QA-authoring gap and not a delivery blocker.
- **BK-600 / BK-601** ("TMS-Run Execution | Drive step verdicts and bug filing from the keyboard" / "TMS-Projects | See what is archived and restore an archived module") — **found 2026-08-24, zero prior mentions anywhere in this document.** Both were created on **2026-08-24 by the `autonomous-delivery` `discovery` routine**, and both gained `master-design-plan.md` §8 US→Screen rows the same day via **PR #204** (merge `20930ab`, `git merge-base --is-ancestor` exit 0 against `origin/staging`, which it is also the tip of). Both are **live tracker status `Backlog`, unassigned and unpointed**, so neither is claimable by a `story` run until it is refined. **Their §8 verdicts differ.** **BK-600** (epic BK-30) reads **`✅ covered` and is NOT gated** (`master-design-plan.md:624`): `run.jsx:212-214` already carries the `P`/`F`/`B` bindings as props and `run.jsx:248` draws the full keyboard footer legend, so nothing has to be invented. Its wizard-vs-flat-checklist adaptation is **ratified, not gated** — `D-UI-1` on BK-35, now transcribed into the design plan's §5 registry as **D35** (2026-08-24); that same ratification is what makes BK-600 a *sanctioned follow-up* rather than a new departure. **BK-601** (epic BK-7) **IS 🔒 mockup-gated and unratified** (`master-design-plan.md:590`) and is now the thirteenth key in the §5 registry: no mockup anywhere draws an archived-row treatment, a restore action or an include-archived toggle. It is **sequenced AFTER BK-571** via a Jira `Dependencies` link whose direction was verified, and that edge is added to the §3 flat edge list by this pass — it had lived only in the design plan. **Neither has a §3 dependency edge of its own beyond that one**; add one if either gains downstream dependents. **This is the THIRD CONSECUTIVE SYNC in which the newest `discovery`-created stories were missing from this file** — BK-593/594 (08-22), BK-595/596 (08-23), BK-600/601 (08-24). The mechanism is now fully identified and is not an accident of any one run: **`discovery` writes the §8 design row and never writes `dev-roadmap.md`**, so the design plan and this roadmap disagree about which stories exist for exactly as long as it takes the next `story` run to notice. Fixing it means changing the `discovery` contract, not catching it again here.
- **BK-666 / BK-667** ("TMS-Test | Browse, filter and sort every Test in a Project" / "API Contract | Make the five remaining create endpoints safe to retry") — **found 2026-08-29, zero prior mentions anywhere in this document** (`grep -c` returned 0 for both). Both gained `master-design-plan.md` §8 US->Screen rows on 2026-08-28 via PR #212 (`58f1eb9`, inside merge `c7fab93`), four days before this run and one day before it noticed. **This is the FOURTH consecutive recurrence of the same mechanism** — BK-593/594 (08-22), BK-595/596 (08-23), BK-600/601 (08-24), now these: `discovery` writes the §8 design row and never writes `dev-roadmap.md`. Patching the symptom here for a fourth time does not fix it; the fix belongs in the `discovery` contract. **BK-666** (epic BK-70 / Tests surface) is **🔒 mockup-gated** — no mockup in `.context/designs/bunkai-test-management-tool/` draws a Tests list, and its §8 row states explicitly that `project.jsx` is NOT the host and must not be made entity-aware (its `TableView({ tree })` flattens ATCs only and labels the count `ATCs`, so the live `<AtcTable>` render is FAITHFUL, not a divergence). Added to the §5 registry this run. Its route surface half-exists: `app/(app)/projects/[projectSlug]/tests/` holds `[testId]` and `new` but no list page. **BK-667** (epic BK-1, `Idempotency-Key` on five existing create endpoints) is **API-only and NOT gated** — no screen of its own, following the BK-18 / BK-595 backend-row precedent. Neither has a §3 edge yet; add one if either gains a downstream dependant.

---

**2026-08-08 run conclusion — AMENDED, and the amendment is the point.** The conclusion below was written mid-run and **its verdict on BK-50 was overturned by this same run before it ended**. Read it as history, then read the amendment beneath it. What survives: the audit, the BK-43 deferral, and the method note. What does not: "empty run", and the claim that the pool is structurally exhausted.

> ~~**2026-08-08 run conclusion**: **empty run — no story claimed, and the pool is now structurally exhausted rather than merely blocked.**~~ Live `Ready For Dev` pool is **2** (BK-43 1pt, BK-50 5pt); BK-45 has left it (shipped, `QA Approved`). Nothing was in flight anywhere: **zero open PRs, zero unmerged branches carrying a BK key**. The one `--no-merged` branch (`claude/gifted-visvesvaraya-422167`) holds a single commit that is a merge *of* staging into itself — no unique code, its BK-42 work shipped separately via PR #108. All 51 PRs merged in the last 10 days verify as ancestors of `origin/staging`, **including four (#94-#97) based on an internal chain branch rather than staging** — the exact shape the ancestry check exists to catch, and each one legitimately passed because its parent chain branch was itself merged. No tracker/git status lie this run.

Both remaining candidates were dropped, and the interesting fact is that they were dropped for **the same structural reason**:

1. **BK-43** — deferral stands, following the record rather than re-deriving it. Its own latest refinement (comments `12170` AI PO and `12177` AI Tech Lead, both 2026-08-05) concludes it is *"not implementable as written"* at 1 SP and must be split into BK-43a/b/c. **Those tickets do not exist**, and BK-43 was never moved off `Ready For Dev` after that verdict. The gap is ticket administration; inventing the split is not an unattended run's job.
2. **BK-50** — gate released, deferred on scope-growth. Its ratified plan assumes a Cloudflare R2 bucket that does not exist, and it needs the app's first anonymous data-access surface. Full evidence in its §6 row above.

~~**The systemic finding, which matters more than either ticket**: both remaining stories are external-integration work whose ratified plans assume credentials and infrastructure the repo does not have (BK-43: a per-workspace third-party secrets model; BK-50: object storage plus anonymous access). **An unattended run cannot provision a credential — it fails closed by design (A13) — so neither is deliverable by this routine at any point in the future, no matter how many times it fires.**~~ **Both halves of this finding are now retracted.** BK-50 shipped later the same run (see the amendment below). And the BK-43 half was refuted on 2026-08-09: **BK-43 needs no per-workspace secrets model and never did** — its own AI Tech Lead ruling `12177` decision **D6**, written 2026-08-05, three days *before* this paragraph, states the opposite in terms: *"Credentials stay deployment-level; do not introduce per-workspace secret storage inside this feature"*, binding the feature to the deployment-level `ATLASSIAN_*` triple BK-17 already ships against (`lib/env.ts:36-38`). A run that had read the ticket it was deferring could not have written this sentence. The deferral outcome is unchanged and the reason is replaced — see the 2026-08-09 conclusion below. What remains true: two consecutive story runs flagged shrinking supply, and **supply, not selection, is what empties this routine now.** Unsticking it needs `/product-management` materializing BK-43a/b/c, or `discovery` mode producing stories that build on existing infrastructure.

**A method note worth keeping**: this run's near-miss was the roadmap itself. It called BK-50 "blocked on BK-45" when BK-45 had merged the day before, and BK-50 was the only candidate whose gate had actually moved. **A roadmap that says "blocked" over shipped work is the error class that empties a run** — reconciling before selecting is not bookkeeping, it is what made the selection correct.

**AMENDMENT (same run, ~1h later): BK-50 SHIPPED — PR #145, `7b16c0c`, ancestor-verified.**

The deferral above was correct about BK-50's **ratified plan** and wrong about **BK-50**. Both are true at once, and the distinction is the whole lesson:

1. **The plan really is undeliverable.** Comment `11047` chose Cloudflare R2 on the stated grounds that "R2 already exists in our stack" — it does not exist in any form — and comment `11048` chose an async `export_jobs` job table on the grounds that chain assembly would exceed a 10s budget, when it is **one** jsonb RPC round trip already serving an interactive page synchronously. Both false premises were **load-bearing**: each was the stated reason its option was cheap. Neither has been retracted anywhere but here and on the ticket.
2. **The story was never the plan.** Once the premises fell, the correct v1 was smaller than the 5-point estimate, not larger: a render function, a handler, a button and a toast. `CLAUDE.md` Rule #18 exists precisely so an unattended run resolves that rather than reporting it — an open product question is work to do, not a blocker.
3. **The evidence that decided it was sitting on the ticket the whole time.** The mockup was attached 2026-07-30, **twenty days after** comment `11047`, and contradicts it outright: download icon, composed filename, confirmation toast, and no link or share control anywhere. Under Critical Rule #15 the later artifact governs. Thirteen months of refinement, three runs of deferral, and nobody had reconciled the two.

**The corrected systemic reading.** The earlier conclusion claimed both remaining stories were undeliverable because they need credentials an unattended run cannot provision. That was **half right and it generalised too fast from one example**. ~~It holds for BK-43, whose per-workspace third-party secrets model is genuinely absent.~~ **That last clause was itself wrong, and the 2026-08-09 run caught it — the correction did not go far enough.** It was false for BK-50 — where the credential requirement was an artifact of a mistaken plan, and evaporated the moment the plan was checked against code. It was **equally false for BK-43**: no per-workspace secrets model is absent from BK-43's design because none was ever in it (ruling `12177` D6, 2026-08-05). This paragraph corrected one instance of a claim it was simultaneously repeating in the next clause. **The pool is 1 (BK-43), not 0, and "needs infrastructure we lack" is a claim to verify against the codebase before it is used to close a run**, exactly as it turned out to be worth verifying for R2 — **and the verification must cover the sentence you are writing, not only the one you are fixing.**

**The rule this run recorded, then immediately broke, now widened.** PR #144 was merged declaring BK-50 deferred **while the decision pass that would overturn it was still running** — the same mistake as BK-213 / BK-211 / BK-45, committed within the hour of writing it down. The original wording covered a *story* merging after the roadmap commit. It is too narrow. **A run must write its roadmap commit LAST, after every piece of in-flight work that could change this document's conclusions has landed — decision passes and dispatched agents included, not only merges.** Four occurrences now; the failure is not the roadmap going stale, it is publishing a conclusion while the thing that decides it is still in flight.

---

**2026-08-09 run conclusion**: **empty run — no story claimed, and that is the correct outcome.** Audited against `origin/staging` tip `86cdf1f`. Zero open PRs, so nothing was contested. Live Jira (`project = BK AND issuetype = Story AND status = "Ready For Dev"`) returned **exactly one story: BK-43**, for the seventh consecutive run.

**BK-43 was NOT deferred by inheritance.** A seventh deferral copied from the sixth is precisely the failure BK-45 documented — four deferrals on three premises that were all false — so this run re-verified the premises against code and live Jira instead of restating them, via a 3-lens scored judge panel (infrastructure reality / minimal buildable slice / adversarial refutation). **All three lenses independently returned 8/100** on "safely deliverable by an unattended run today." The deferral holds. Its stated reason did not.

**What was wrong, and is corrected above.** The 2026-08-07 and 2026-08-08 entries deferred BK-43 on the grounds that it needs a per-workspace third-party secrets model, and hardened that into "not deliverable by this routine at any point in the future." **BK-43's own AI Tech Lead ruling `12177` decision D6, dated 2026-08-05, forbids per-workspace secret storage inside this feature** and binds it to the deployment-level `ATLASSIAN_*` triple BK-17 already ships against (`lib/env.ts:36-38`, `lib/jira/client.ts:112-114`). The premise was dead two days before it was first written and three before it was hardened. Both paragraphs are struck through above. Note the shape: the 2026-08-08 amendment corrected this exact claim for BK-50 while restating it for BK-43 in the next clause.

**What actually blocks BK-43, re-verified in code at migration `0068`:**

| gap | verified absent |
| --- | --- |
| destination-config table (`project_tracker_settings`) | zero hits for `integrations`/`tracker_settings` across all 68 migrations |
| sync columns on `bugs` | live `information_schema` — 17 columns, no `external_id`, no `sync_status`. `0046_bugs.sql:93-116` + only later ALTER `0054:79` (`assignee_user_id`) |
| outbound authenticated **write** | `lib/jira/client.ts` exports exactly one function, `searchIssues` (`:114`). Its only POST (`:133`) is a JQL search — a read |
| time-based retry primitive | no `vercel.json` at all, no `crons`, no `app/api/cron/**`, no `pg_cron`/`pgmq`/`pg_net`. `import_jobs` exists but is request-scoped `after()` with no sweeper, no attempt counter |
| bug-detail route to host AC-3's badge and AC-4's backlink | `app/(app)/projects/[projectSlug]/bugs/` has `page.tsx` only. `lib/notifications/entity-routes.ts:53-66` says so in its own header and **returns `null` for a standalone defect because no route exists to link to** |

Five of five primitives absent, against a **1-point** estimate. Two lenses independently found the deciding detail nobody had recorded: **AC-4 is not merely unimplemented, it is unaddressable** — there is no URL to put in the external issue.

**No viable partial slice exists.** Schema-only and manual-export slices satisfy **zero of six** ACs, not two of six (AC-5 and AC-6 are already vacuously true with no code written). The one independently shippable slice — the tracker-settings surface — is the thing **BK-43's own `out-of-scope.md` line 6 explicitly disclaims**. Shipping any of them under this key and transitioning to Ready For QA would hand QA a ticket whose 14 linked ATCs almost entirely fail.

**Live ticket state, fetched with the correct tenant prefix** (`ATLASSIAN_URL=https://upexgalaxy71.atlassian.net/`, the guard against the BK-211 stale-cache failure): status `Ready For Dev`, 1 point, **10 comments, all 10 returned** — no hidden ratification, the BK-211 pattern does not apply here. Most recent comment `12177`, 2026-08-05. **Nothing on the ticket postdates 2026-08-05**; last changelog entry of any kind is 2026-07-30. Queried four ways for a successor — `summary ~ "Defect Sync"`, `created >= 2026-08-05` (69 items), `summary ~ "external tracker"/"integration"`, and REST `subtasks`/`issuelinks` — **BK-43a/b/c do not exist**, zero subtasks, zero child stories.

**The single unblock, unchanged and now precisely located**: `/product-management` materializes BK-43a/b/c per the coverage split in comment `12170` and re-parents the 14 ATCs. That is ticket administration, not engineering, and it is not an unattended `story` run's job to invent — but it is roughly ten minutes of work for the routine that owns it, and it is the only thing standing between the best-refined ticket on this board and a sprint. **Seven runs have now ended empty over data entry.**

**Method note.** Two prior over-generalisations were fixed this run and the lesson is the same one both times: a claim about infrastructure gets verified against the codebase before it closes a run, **including the claim in the sentence you are writing while correcting the previous one.** The 2026-08-08 amendment retracted "needs credentials we lack" for BK-50 and repeated it for BK-43 in the very next clause.

**2026-08-10 run conclusion**: **empty run — no story claimed, and the blocker changed shape in a way that matters.** Audited against `origin/staging` tip `ff8b79b`; `origin/main` (`e88512e`) is an ancestor of `staging`, so the release invariant holds. Zero open PRs — nothing contested. All 25 most-recent merged PRs targeted `staging`, so the chain-internal-PR tracker lie did not fire. Live Jira returned **exactly one story at `Ready For Dev`: BK-43**, for the **eighth** consecutive run.

**The 2026-08-09 ruling was followed, not re-derived.** That entry closed with an explicit instruction to run 8 — *do not re-derive this; the deferral rests on sizing ruling `12170`, and overturning it requires refuting the five code-level gaps in code, not in a comment.* No judge panel was dispatched. Nothing in this run's evidence points the other way, and re-running a panel to reach the same answer is exactly the re-litigation `decision-protocol.md` forbids. **The deferral stands on the record, not on a fresh opinion.**

**What changed since 2026-08-09 — BK-43 now has a real, git-verified dependency block.** The prior run counted 15 Jira links on BK-43: 14 Tests + 1 Dependencies (BK-40). **There are now two Dependencies links.** The new one is **BK-337 — `TMS-Defect Detail | Open a defect and read its full record`**, created 2026-08-10 by the discovery routine after four fires of an operator-approval gate, live status `Shift-Left QA`.

This is not a bookkeeping edge. BK-337 *is* gap #5 of the five the 2026-08-09 panel verified — "no bug-detail route at all", the gap that makes **AC-4's backlink unaddressable rather than merely unbuilt**, because there is no URL to write into the external issue. The hole now has a ticket, an owner and a dependency edge, and it is recorded in §3.1 as a 2026-08-10 addendum to the BK-31 cluster.

**BK-337 is NOT shipped, verified by git and not by status.** PR #152 (`ff8b79b`, merged 2026-08-10T17:09Z) carries the BK-337 key but is **docs-only** — eight files of PBI cache and a `master-design-plan.md` §8 screen-map row. No route, no handler, no RPC. `app/(app)/projects/[projectSlug]/bugs/` still holds `page.tsx` alone. A run reading BK-337's tracker activity without checking the diff would conclude the dependency had cleared. It has not.

**So BK-43 is blocked twice over, and the two blockers are different in kind:**

| blocker | category | clears when |
| --- | --- | --- |
| BK-337 not on `staging` | **(a) genuine dependency** — the legitimate, ordinary kind | BK-337 clears shift-left, gets built, and `git merge-base --is-ancestor` passes |
| BK-43a/b/c never created (ruling `12170`) | ticket administration — no routine is chartered to do it | `/product-management` materializes the split and re-parents the 14 ATCs |

The first is healthy: the system produced a ticket for its own gap and the graph now describes it. **The second is not, and eight runs is the evidence.** It is roughly ten minutes of backlog data entry. `story` mode cannot do it (its dispatch target is `/sprint-development`, and inventing tickets is explicitly not its job); `bug` mode cannot; `discovery` mode resolved its own gate on 2026-08-10 and runs a fresh analysis next fire, which may or may not land on this. Recording it for assignment has now been tried seven times into files nobody opens, so **this run escalated it to the operator instead** — see the escalation log entry for 2026-08-10.

**Roadmap defects corrected this run** — three, all found by auditing §4 and §6 against each other, none touched by PR #148:

1. **§4's ES5 row survived its entire cluster draining underneath it.** It still described BK-209 as an unclaimed conditional pick needing "an explicit human go", and BK-211/212/213 as blocked on it. All four are shipped and ancestor-verified (PRs #113, #137, #115, #127). §6 had recorded every one of those merges; §4 was never reconciled against it. **BK-214 is the only story left in ES5.**
2. **§3.1's BK-208 block gave the ✅ shipped marker to BK-209, BK-212 and BK-264 but withheld it from BK-211 and BK-213** — inconsistent inside a single eight-line block, and contradicted by §6.
3. **§6 carried two separate stale BK-209 rows in its own pre-dev-blockers table**, both asserting a "needs explicit human go" gate on a story the pool table three rows down records as merged. Both are struck; the ratification *finding* is kept, because it still stands as a process observation, while the *status* it implied does not.

The shape all three share is worth naming: **every one is §4 or a §6 side-table failing to notice something §6's own pool table already recorded.** Prior runs kept finding this per-ticket (BK-213 on 08-04, BK-211 on 08-06, BK-45 on 08-08) and fixing the instance. It is not a per-ticket defect — it is a structural one, and §7's maintenance protocol already names the cure without anyone applying it: **§4 and the §6 side-tables are hand-maintained, §6's pool is the query recipe, and nothing reconciles the hand-maintained parts against the queried one.** A reconciliation pass that reads §6 bottom-up and rewrites §4 would have caught all three of these in one sweep, and would have caught the three before them.

**Also surfaced, not fixed:**

- **`pr/61` — a stale local ref carrying unmerged BK-175 auth code.** Tip `ae9f6b6` (2026-07-17), diverged at `82bae4b`, **before** the version that actually shipped (`a25398b`, PR #134, 2026-08-06). PR #61 is closed-not-merged. It touches `magic-link-form.tsx`, `auth/confirm/route.ts` + `.openapi.ts`, `public/openapi.json` — an OTP code-entry field. BK-175 did genuinely ship, so this is not an unshipped ticket; whether the OTP field is superseded scope or a real remaining gap is not answerable from git and needs a human look at the auth flow.
- **The `ATLASSIAN_URL` env var is still stale**, pointing at a different hostname than `.agents/project.yaml`. PR #151 made the sync script prefer `project.yaml`, so nothing breaks today — but `acli` and the MCP layer can still silently disagree with the script. `.env` is untracked, so no run can fix this; the operator clears the export and restarts the session.

---

**2026-08-11 run conclusion**: **empty run — and the pool is 0, not 1.** Audited against `origin/staging` tip `0a89b36`. `origin/main` remains an ancestor of `staging`; the release invariant holds. Migration ledger high-water mark `0068`, local files agree, nothing applied this run.

**The eight-run BK-43 deferral streak did not end because BK-43 became buildable — it ended because ruling `12170` was finally executed.** Ruling `12170` (2026-08-05) mandated a BK-43a/b/c split; seven prior runs (2026-08-05 through 2026-08-10) escalated the fact that nothing was chartered to do that ten-minute piece of ticket administration. This run finds it **done**: BK-43 is `ABORTED` and PR #156 (merge `0a89b36`) materialized its three successors — BK-371, BK-372, BK-373 — under epic BK-31, exactly as ruling `12170` specified. §3.1 and §4 are corrected above to carry the new edges (BK-372 inherits BK-43's old BK-40 ✅ / BK-337 edges; BK-371 has none; BK-373 gates on BK-372).

**The blocker changed CLASS, and that distinction matters more than the ticket count.** Every prior empty run since 2026-08-05 was blocked on **"paperwork nobody owns"** — a resolved, sized, ratified ticket sitting un-split because no routine was chartered to split it. That class of blocker is never acceptable; it is a process gap, and it is why this doc escalated it to the operator on 2026-08-10. Today's blocker is different in kind: BK-371, BK-372 and BK-373 are real tickets that simply **have not been through shift-left QA refinement yet** — an ordinary, expected precondition for `Ready For Dev`, the same one every other `Backlog` story in this project waits on before it can be picked. The difference is not cosmetic. An unattended `story` run must not invent shift-left refinement (CLAUDE.md Rule #18's decision authority covers open product/technical questions on an already-refined ticket, not the refinement pass itself) — so this is a genuine, correctly-respected boundary, not a paperwork gap this routine is failing to close. **What is new and worth naming**: that ordinary blocker now sits in front of **100% of the epic-BK-31 pool**, not one candidate among several — BK-43's old scope was one ticket with one deferral reason; its replacement is three tickets that all share the same single precondition.

**Live story pool, queried fresh this run**: zero at `Ready For Dev`, out of 93 total stories. Histogram: Backlog 28 (includes BK-371/372/373), Ready For Release 24, Ready For QA 20, QA Approved 14, Estimation 3, Shift-Left QA 2 (BK-337, BK-262), In Test 1 (BK-41), ABORTED 1 (BK-43). No story anywhere in the tracker currently satisfies "dependency-clear + genuinely refined + unclaimed" — the first time this doc has recorded a pool of exactly zero rather than a pool with at least one deferred candidate.

**What unsticks the next run**: either (a) **BK-337** ships application code (a route, a handler, an RPC — not another docs-only PR like #152) and lands on `staging`, which clears BK-372's dependency edge but still leaves BK-372's own shift-left refinement to run; or (b) **BK-371, BK-372 or BK-373** individually clears shift-left QA, at which point that one story (not necessarily all three, since BK-371 has no incoming edge and could clear independently) becomes a genuine candidate for the next `story` run. Until one of those two things happens, the next run is expected to end empty too, and that is the correct, designed outcome — not a routine failure.

**Also verified, not touched by this run**: the `ATLASSIAN_URL` staleness recorded in the 2026-08-10 "Also surfaced, not fixed" note above is **resolved** — `.env` now reads `upexgalaxy71`, matching `.agents/project.yaml`, and `acli` authenticates against the correct site. That note is left as-is above per this doc's append-only convention; readers should treat it as historical, not current.

---

**2026-08-12 run conclusion**: **BK-48 claimed and implemented — the empty-run streak breaks at three (2026-08-09, 2026-08-10, 2026-08-11).** Audited on four independent sources: `origin/staging` tip `84e7409`, `origin/main` tip `e88512e` (still an ancestor of `staging`, release invariant holds), the live Jira status histogram, and the migration ledger (`0068`, unchanged, nothing applied this run — matches the local `supabase/migrations/` files exactly).

**The 2026-08-11 conclusion's "the pool is 0, not 1" claim was itself wrong.** Live query returned **2** stories at `Ready For Dev`, not 0: **BK-48** (`TMS-Traceability | Filter the chain by verdict, module, and date range`, epic BK-44, 5 SP) and **BK-337** (`TMS-Defect Detail | Open a defect and read its full record`, epic BK-31, Story Points empty). Both cleared shift-left refinement sometime between the 2026-08-11 audit and this one — that prior sync had BK-337 sitting at `Shift-Left QA` and epic BK-44 down to a single `Shift-Left QA` story (BK-48); neither claim survived to this run's audit. **Full live distribution (95 total, up from 93)**: Backlog 28, Ready For Release 24, Ready For QA 20, QA Approved 14, Shift-Left QA 3, Ready For Dev 2, Estimation 2, In Test 1, ABORTED 1.

**BK-48 was selected.** Its only dependency is a Jira `Dependencies` link onto **BK-30** (Epic, status `Planning`) — that question was **not re-derived, it was answered by following standing precedent**: the 2026-08-05 sync already ruled the identical shape of claim for BK-211 ("BK-30's child stories are all shipped; the epic's `Planning` status is stale bookkeeping, not a functional gate" — see the header history above), and the same logic transfers directly here — epic BK-44 itself currently reads `Planning` while its own children **BK-45** (`f75709e`, PR #142, 2026-08-07, ancestor-verified) and **BK-50** (`7b16c0c`, PR #145, 2026-08-08, ancestor-verified) are both genuinely shipped and `QA Approved`. BK-48 filters the chain BK-45 renders, so the functional prerequisite (BK-45 existing on staging) is real, met, and independent of the epic ticket's own bookkeeping status. BK-48 was claimed and implemented this run; per this doc's own standard — a claim is not a ship until a merge commit is ancestor-verified against `origin/staging` — its delivery is not asserted here and should be re-verified by the next sync.

**BK-337 was considered and deferred, not blocked.** It is dependency-clear (no incoming edge — §3.1) and its own mockup already exists (`bug-detail.html`, §4.6/§5). But it reached `Ready For Dev` with **Story Points left empty** — transitioned without passing estimation, a shape of gap this doc has not seen before — and it is a whole new read surface for this codebase: a route, a handler, and an RPC, backing 17 AC scenarios. Unsized plus new-surface is a scope-growth signal this run declined to auto-claim; the responsible next step is an estimation pass or a human-present `/sprint-development` session, not an unattended guess at its size. **BK-337 is still not shipped as code** — PR #152 (`ff8b79b`) remains docs-only by diff (PBI cache + one `master-design-plan.md` §8 screen-map row) — so BK-372's inherited BK-337 dependency edge (§3.1) remains genuinely unsatisfied, and BK-371/BK-372/BK-373 are unchanged from the 2026-08-11 sync: all three still `Backlog`, none through shift-left refinement.

**Two new stories surfaced this run, absent from this document entirely**: **BK-398** (`Command Palette | Search and jump across the workspace`, epic BK-7, live `Shift-Left QA`) and **BK-399** (`TMS-ATC Classification | Classify by test-design technique and priority`, epic BK-13, live `Backlog`), both created 2026-08-12 by the discovery routine. Neither has a `master-design-plan.md` §8 screen-map row yet — recorded as an outstanding item in the edge-mapping TODO above, not fixed here: `master-design-plan.md` is out of scope for this document, and a concurrent agent may be writing its own §5 divergence row into that same file this run.

**Roadmap defects corrected this run**: the header's "Live Ready-For-Dev pool = 0 of 93" claim (2026-08-11) was false and is corrected above; the ES4 (BK-44 Coverage) row in §4 still described epic BK-44 as having "only BK-48 (`Shift-Left QA`) left" (in two places — its own row and BK-50's §6 row) and both are corrected; BK-337's §3.1 status tag still read `[Shift-Left QA — refinement running, NOT yet buildable]` and is corrected there. All three are the same class of staleness this document has repeatedly named across prior syncs: a hand-maintained cell not reconciled against a status change that had already happened by the time the next run read it.

---

**2026-08-13 run conclusion**: **empty run for story-claiming purposes — the pool was 2 (BK-267, BK-337), and neither is implementable by an unattended run this cycle, though BK-267's blocker was cleared by published ruling rather than left standing a further time.** Audited against `origin/staging` tip `b9f3fc69`; `origin/main` verified an ancestor of `staging`, the release invariant holds. **Migration ledger high-water mark, queried directly against the live ledger: `0069_story_traceability_module`** (version `20260812182631`) — see the roadmap-defect correction below.

**BK-267 was not claimable as written (1 SP against 14 AC blocks / 19 Gherkin scenarios), and this run did not defer it a further time — it resolved the sizing dispute instead.** An AI Product Owner ruling (comment `12315`) and an independent AI Tech Lead ruling (comment `12316`) both found the true size at ~10-11 SP and mandated a three-way split; comment `12317` closed it. BK-267 is now `ABORTED`, replaced by BK-439 (5 SP) -> BK-440 (3 SP) -> BK-441 (3 SP) under epic BK-13 — full edge detail in §3.1's new BK-13 addendum above. This is the same class of resolution the BK-43 -> BK-371/372/373 split modeled on 2026-08-11: administrative ticket-restructuring under a published ruling, not an unattended `story` claim, and not a further deferral either. None of the three successors has cleared shift-left QA refinement yet, so none is itself claimable this run.

**BK-337 was deferred for the SECOND consecutive run, on the same stated reason as 2026-08-12: unsized (Story Points still empty) plus a whole new read surface (route + handler + RPC, 17 AC scenarios).** Per the codified lesson from BK-45 — *"when a candidate is dropped for the same reason two runs running, audit the reason itself, not the ticket"* — BK-45 sat under a wrong deferral for three runs before a 4-lens scored panel overturned it on 2026-08-07 (see that entry above). **This run flags BK-337's deferral reason as due for that audit on the next run**, rather than allowing a third inherited restatement: the next run should re-verify, in code and in Jira, whether "unsized plus new-surface" still holds, rather than copying this entry's wording forward unexamined.

**Roadmap defect corrected this run**: the 2026-08-12 header entry's migration-ledger clause ("unchanged at `0068`") was already wrong at the moment it was written — the live ledger carried `0069_story_traceability_module` from BK-48's own migration, applied that same run. Struck and corrected in place in the header above, per this document's append-only convention (not rewritten).

---

**2026-08-14 run conclusion**: **BK-337 CLAIMED — the empty-run streak breaks, and it breaks on an
overturned deferral rather than on a newly-unblocked ticket.** Audited against `origin/staging` tip
`e10dcb3`; `origin/main` an ancestor of `staging`, invariant holds. Live ledger high-water mark
`0069_story_traceability_module`, next free `0070`.

**The audit this document ordered was performed, and the reason did not survive it.** The 2026-08-13
entry closed by instructing the next run to *"re-verify, in code and in Jira, whether 'unsized plus
new-surface' still holds, rather than copying this entry's wording forward unexamined."* A 4-lens
scored panel (`model: opus`, lenses: implementation cost / authorization risk / refinement readiness /
precedent) returned **7 / 7 / 8 / 9 — unanimous claim, no lens below 7**. Detail in §3.1's BK-337
2026-08-14 update. The single most useful finding for future runs: **the load-bearing half of the
deferral was never verified even once**. It was asserted on 2026-08-12 with no code recon cited
anywhere in the entry, and the 2026-08-13 run that inherited it says in its own words that it
"FOLLOWED, not re-derived". This is the BK-45 failure mode exactly — BK-45 sat under a wrong deferral
for three runs until a panel overturned it — and the distinguishing question the log already codified
applies unchanged: *"was the last deferral verified, and has anything changed since?"* Here the answer
was no, it never was.

**Refinement was confirmed genuine, not self-ratified theatre**, which matters because this project
has logged the opposite case. Both of BK-337's blocking data-model gaps were closed by **cutting**
scope, not widening it. Four independent signals: the raiser and the closer are different role
profiles; the rulings went AGAINST the raiser, and QA then **retracted its own findings and downgraded
its own risk score HIGH -> MEDIUM**; the Product Owner **corrected itself unprompted**; and five
spot-checked citations all hold verbatim against live code.

**Spec-of-record defect found, and it blocks a correct build.** Only BK-337's Acceptance Criteria
field was rewritten after the rulings. `scope.md`, `business-rules.md` and `workflow.md` still instruct
a developer to build all three items the refinement CUT — Expected vs Actual, the layer/environment
rows, and the in-list failing-step highlight — and `out-of-scope.md` still credits the sync panel to
the ABORTED BK-43. **A developer building from Scope ships work that was deliberately cut.** The
Product Owner already ordered this fixed (*"The Scope bullet is struck"*, and improvement 7 ACCEPTED
with *"A decision a developer cannot see in a field they read is a decision that gets reverted as a
bug"*) and nobody executed it. Neutralized for this run by naming all three cuts as hard prohibitions
in the implementation briefing; the durable fix is a Jira field write plus a resync, since these are
`[SYNC]` files the sync owns and no one may hand-edit.

**Two discrepancies for the operator, neither corrected silently:**

1. **BK-262 was promoted to `Ready For Dev` (21 SP) in contradiction of a published ruling.** The
   2026-08-13 discovery run ruled: *"BK-262 was NOT transitioned to `Ready For Dev`, reversing this
   run's own earlier proposal. Eleven days in `Shift-Left QA` means the refinement never completed;
   the story touches 49 of 82 handlers."* Something promoted it anyway. The ruling is FOLLOWED here,
   not re-derived — BK-262 was not claimable this run.
2. ~~**BK-229 is live at `Ready For Dev` while the design contract calls its whole epic parked.**~~
   ~~`master-design-plan.md:448`: *"none of BK-229-BK-233 are Ready For Dev - the whole epic is~~
   ~~deliberately parked Backlog, post-MVP"*. It also has **no shift-left refinement at all** — no~~
   ~~comment trail — which is the one genuine category of blocker an unattended run may not resolve~~
   ~~itself, since inventing the refinement is precisely what it must not do. Recorded for assignment.~~
   **REFUTED 2026-08-24 — both halves, and both at the source. Struck, not deleted: this file's history
   is the evidence trail for how the false negative was produced.** (a) **The quoted premise no longer
   exists in the design plan.** `master-design-plan.md:448` today is the `plan-comparison-checkout.html`
   row of §4.15's screen table — an unrelated line. The "whole epic parked" text was superseded in place
   by the **Build order note at `master-design-plan.md:452-464`** (AI Product Owner ruling, Jira comment
   `12415`, 2026-08-16), which says so in its own words: *"supersedes the prior 'none of BK-229–BK-233 are
   Ready For Dev' text, which had gone stale"*. BK-229's epic dependencies (BK-1 Tenancy/Identity, BK-87
   Settings hub) both shipped, so the parking premise stopped describing reality for that one story;
   BK-231/BK-232/BK-233 are unchanged and stay `Backlog` on their own shift-left's call. (b) **The "no
   shift-left refinement at all" half was already refuted inside this same document** by the 2026-08-19
   entry further down §6 — BK-229 carries a complete Shift-Left ATP posted 2026-08-13 (17 refined AC
   scenarios, 18 ATP outlines, named QA owner). It was invisible to both prior runs because they used
   `acli jira workitem view`, which does not return comments. BK-229 subsequently **shipped 2026-08-16**
   (PR #176, merge `18662df`, ancestor-verified). Nothing in this item survived contact with a live
   re-check.

**A third, process-level defect worth naming**: the copy of this document in the shared home checkout
was **18 commits stale** at the time of this run and contained zero mentions of BK-439/440/441. Any
agent handed the literal home-checkout path — rather than a worktree at `origin/staging` — reads a
roadmap that does not know the BK-267 split happened.

---

**2026-08-15 reconciliation (docs-only, not an autonomous-delivery `story`/`bug` run)**: **BK-398
recorded as shipped, five edge-mapping gaps closed, no story claimed.** Audited against
`origin/staging` tip `3f6816f` (this worktree's branch point). This pass did not run selection —
it is a planning-surface correction so the next audit starts from truth, per the standing directive
to keep §3/§6 reconciled against what has actually landed.

**BK-398 shipped and recorded.** PR #173 (`feature/BK-398-command-palette`) merged to `origin/staging`,
merge commit `3f6816f`, ancestry-verified — full detail in §3.1's new BK-7 addendum above and the
edge-mapping TODO entry, both updated in place rather than left as a dangling "outstanding" item.
BK-399 remains outstanding and unchanged: still `Backlog`, no §8 screen-map row, no edge identified.

**Five edge-mapping gaps closed** — BK-315, BK-269, BK-268, and epics BK-183 and BK-70 existed in
`.context/PBI/epic-tree.md` but had zero mentions anywhere in this document (verified: 0 grep hits
each before this pass). Each was read live (`bun run jira:sync-issues get <KEY>` plus a direct
`acli jira workitem link list` check for Jira-linked edges) before being recorded — see the
edge-mapping TODO entries above for the evidence and reasoning per ticket. None of the five carries
a genuine incoming dependency edge: BK-315, BK-269 and BK-268 each return zero Jira issue links, and
where their own scope/business-rules text implies a soft precedent (BK-269 on BK-36, BK-268 on BK-49),
that precedent is already shipped, so no blocking edge is recorded. Epic BK-183 is an administrative
defect-intake bucket by its own description, not a feature epic — recorded as such rather than forced
into the dependency graph. Epic BK-70 is recorded as an empty epic (0 Stories) per its own `epic.md`
and `epic-tree.md`.

**Two blockers carried forward from the 2026-08-14 conclusion, re-verified live and unchanged:**

1. **BK-229 is blocked by a missing shift-left refinement — a QA-authoring gap, not a dependency
   edge.** Re-verified live (`acli jira workitem view BK-229`): status is `Ready For Dev`, and no
   comment trail exists on the ticket. This is the one genuine category of blocker an unattended
   pass may not resolve itself (inventing the refinement is precisely what it must not do). Recorded
   for assignment to whoever runs shift-left on it, per this doc's existing convention for that class
   of gap — not re-derived, carried forward.
2. **BK-219 is blocked by BK-215, and BK-215 has no branch and no merge commit in existence.**
   Verified this pass: `git branch -a` and `git log --all --oneline` return zero hits for either key,
   and `git log origin/staging --grep="BK-215"` / `--grep="BK-219"` are both empty. BK-215 (Team Chat |
   Chat with workspace members in a real-time channel) is §3.1's named gate for the entire BK-210
   cluster (`BK-215 ──> epic BK-1`), and BK-219 (Edit/delete my own messages) gates on BK-215 directly
   — neither has any code, in any branch, anywhere in this repository's git history.

**BK-262 still contradicts the published ruling that forbids its promotion — discrepancy D2,
unresolved.** Re-verified live this pass (`acli jira workitem view BK-262`): status is `Ready For
Dev`, unchanged from the 2026-08-14 finding. The 2026-08-13 discovery-run ruling that it must not be
promoted (quoted in full in the 2026-08-14 conclusion above) still stands and is still not what the
live ticket shows. Not corrected by this pass — Jira transitions are out of scope for a docs-only
reconciliation; recorded again so it is not silently dropped between runs. This is the same
discrepancy, not a new one; D2 is not renumbered.

---

**2026-08-16 run conclusion (sprint-development, BK-229)**: **BK-229 claimed and implemented — and
two standing blockers this document carried forward across two prior entries are corrected as false,
not silently dropped.** Audited against `origin/staging` tip `f565a5a`. Live Ready-For-Dev pool at
this run's start: BK-219 (blocked, unchanged conclusion below), BK-229 (claimed), BK-262 (deferred,
D2 re-diagnosed below).

**(1) BK-229's "no shift-left refinement / no comment trail" blocker (2026-08-14 §6, item 2; carried
forward verbatim in the 2026-08-15 reconciliation's item 1) is REFUTED.** The ticket carries a
complete Shift-Left ATP posted 2026-08-13, 8:04 PM: 17 refined AC scenarios (5 original + 12 new), 18
ATP outlines (7 Positive / 3 Negative / 8 Boundary), risk level MEDIUM, six PO/Dev decisions applied,
and QA owner `pinto.lucas.nahuel` explicitly named — followed by that same QA owner's own "Ready For
Dev" handoff comment 46 minutes later. **Root cause of the false negative, found by reproducing both
prior runs' own verification method**: both used `acli jira workitem view BK-229`, and `acli view`
does not return issue comments — a gap this same document's §1 ("read via the sync script, never
`acli` `view`, for custom fields") already warns about for CUSTOM FIELDS, and this is the identical
trap applied to comments instead. `bun run jira:sync-issues get BK-229 --include-comments` surfaces
the full ATP in one call. The `master-design-plan.md:448` "whole epic parked" premise the 2026-08-14
entry also cited has separately been corrected at the source (AI Product Owner ruling, Jira comment
12415, 2026-08-16): BK-229's epic dependencies (BK-1, BK-87) both shipped, so that premise no longer
held either. Neither blocker survives contact with a live re-check; BK-229 was genuinely claimable.

**(2) Discrepancy D2 is CLOSED as a mis-diagnosis — not resolved by a Jira transition, but by finding
the original claim was never true.** No comment on BK-262 forbids ITS OWN promotion. The text quoted
across the 2026-08-13/14/15 entries — *"BK-262 was NOT transitioned to Ready For Dev, reversing this
run's own earlier proposal"* — is a discovery run's record of a decision it made about itself that
day, not a standing prohibition binding every later run or every later actor. The live changelog
settles it: **Luis Eduardo Flores Villarroel** (`6305712749a5c6754d910401`), a human, transitioned
BK-262 `Estimation -> Ready For Dev` on **2026-08-14T07:37:59-0300**, 99 seconds after posting a
21 SP estimation comment — an ordinary, authorized human PO action, not a violation of anything.
**BK-262 remains correctly deferred**, but on a different and real basis: 21 SP sits well above this
project's practical ceiling for an unattended `story`-mode pick (13 SP), and the estimator's own
comment leaves the split-or-not call explicitly to the PO, still open. That is a legitimate scope
deferral, distinct from a violated ruling — D2 is closed as a mis-diagnosis, not renumbered, per this
document's append-only convention.

**(3) BK-219's entry is corrected, not reversed** — the 2026-08-15 finding (BK-215 has no branch, no
merge commit) was true but incomplete. Re-verified live this run: BK-215 (Team Chat | Workspace
real-time channel) sits at Jira status **`Estimation`**, with **zero branches and zero commits**
anywhere in git history (`git branch -a`, `git log --all --grep="BK-215"` both empty, re-confirmed).
BK-219 (Edit/delete my own messages) still gates on BK-215 directly (§3.1) and remains genuinely
blocked — this correction adds the missing Jira-status half of the picture, it does not change the
conclusion that BK-219 is not claimable this run.

---

## 7. Maintenance protocol

- **Dependency edges (§2–§5)**: hand-maintained here. When a new story is refined, add its edge BEFORE it goes Ready For Dev.
- **Live status (§6)**: **never hand-maintained.** §6 is a query recipe, not a table. Do NOT paste status snapshots here — they rot in days. If someone needs status, they run Recipe A or B. The only hand-edited parts of §6 are the *local-only* lists (pre-dev blockers, edge-mapping TODO), which hold knowledge Jira does not store.
- **Cross-check against Jira issue-links** (validation, not authority): periodically diff §3 edge list vs Jira "blocks/is-blocked-by" links. **Local wins on structure** — Jira links are sparser and have no execution-sprint/mockup concept. Flag any Jira edge missing here, and any here missing in Jira (candidate to push up to Jira for traceability).
- **Relation to `/master-implementation-plan`** (decided 2026-06-19, do not re-litigate): that skill regenerates the epic-strategy layer (`master-implementation-plan.md`) from business-maps. It does NOT own this doc, and the two are **intentionally kept separate** — they consume different inputs (strategy ← business-maps; this doc ← Jira issue-links + local design context like mockup status), so one generator cannot produce both. Do not merge them. The two docs answer different questions: `master-implementation-plan.md` = "why this epic order" (strategy); this doc = "what ticket next + what unblocks it" (sequence).
- **Trigger to update**: a gate releases (story → dev-done), a new story enters refinement, a mockup lands (clears a 🔒), or a sprint closes.
