> **Generated by**: `/business-data-map` slash command (greenfield mode — encoded from SRS, not from live DB)
> **Last update**: 2026-05-19
> **Sources**: `.context/SRS/architecture-specs.md` (canonical ERD), `.context/SRS/functional-specs.md` (FR-001..FR-040), `.context/PRD/mvp-scope.md` (9 epics), `.context/PRD/user-journeys.md` (3 journeys), `.context/business/business-model.md`, `.agents/project.yaml`
> **Scope**: full-system (Bunkai MVP — Cloud edition, with Community edition variance called out)
> **Regenerate with**: `/business-data-map` (CREATE mode if deleted, UPDATE mode otherwise — never auto-overwrites)

# Business Data Map: Bunkai

```
╔══════════════════════════════════════════════════════════════════════════════╗
║ BUNKAI (分解) — BUSINESS DATA MAP                                            ║
║ Open-core Test Management System — ATCs are atomic, anchored to US+AC,       ║
║ chainable into Tests, executed identically by humans, agents, or CI.         ║
╚══════════════════════════════════════════════════════════════════════════════╝
```

---

## 1. Executive Summary

### What does this system do?

Bunkai is a **Test Management System (TMS)** built around a single non-negotiable structural rule: **every ATC (Atomic Test Component) must be anchored to a User Story AND at least one Acceptance Criterion**. Test cases are not free-form scripts — they are **ordered chains of reusable ATCs**, so editing one ATC propagates to every Test that chains it ("one-edit-many-tests"). Bugs live inside the test cycle (anchored to Module + ATC + Run), not delegated to Jira. The same data model serves three execution modes — **manual**, **agentic** (AI), **automated** (CI/CD adapters in Phase 2) — producing runs that aggregate transparently in dashboards.

Two editions ship from one codebase: **Bunkai Cloud** (Supabase-hosted) for the MVP, and **Bunkai Community** (self-hosted Docker Compose, Phase 2). The data model is identical across editions; only the infrastructure substrate differs.

### Main Actors

| Actor | Description |
|---|---|
| **QA Engineer** (Elena persona) | Authors User Stories, AC, ATCs, Tests. Runs manual executions, files Bugs from inside the runner. Day-to-day power user. |
| **QA Lead / Manager** (Mateo persona) | Configures Projects + Modules + integrations. Watches dashboards: heatmaps, defect density, Run trends. Approves test plans. |
| **Workspace Owner / Admin** | Invites teammates, assigns roles, mints Personal Access Tokens, configures Jira/integrations. |
| **AI Agent / CLI / CI** (Karim persona) | API consumer with a workspace-scoped Bearer token. Drives Runs end-to-end, reports step results, files Bugs — using the **same endpoints** a human UI would call. |
| **Viewer** (read-only stakeholder) | PMs, dev leads, regulators who consume dashboards without writing. |

### Value Proposition

- For **QA engineers**: structural traceability is enforced by the data model — orphan tests cannot exist. Maintenance collapses to one ATC edit propagating everywhere.
- For **QA leads**: defect heatmaps and run-status rollups fall out automatically from the schema (no manual rollups, no spreadsheets).
- For **AI agents**: every state-changing UI action has a 1:1 REST endpoint with idempotency keys. The system is operable headless from day one.
- For **regulated industries**: identical data model in self-hosted edition removes the SaaS data-sovereignty objection.

---

## 2. Entity Map

```
                                    ┌──────────────┐
                                    │   USERS      │ (Supabase Auth — auth.users)
                                    └──────┬───────┘
                                           │ owns / belongs-to
                ┌──────────────────────────┼──────────────────────────┐
                ▼                          ▼                          ▼
       ┌───────────────┐         ┌──────────────────┐       ┌────────────────┐
       │  WORKSPACES   │◄────────┤ WORKSPACE_       │       │ ACCESS_TOKENS  │
       │  (tenant root)│         │   MEMBERS (RBAC) │       │ (Bearer auth)  │
       └───────┬───────┘         └──────────────────┘       └────────────────┘
               │                          ▲
               │ owns                     │ pending invitations
               │                  ┌───────┴──────────┐
               │                  │ WORKSPACE_INVITES│
               │                  └──────────────────┘
               ▼
       ┌───────────────┐
       │   PROJECTS    │
       │ (app under    │
       │   test)       │
       └───────┬───────┘
               │ contains
   ┌───────────┼───────────┬──────────────┬─────────────┐
   ▼           ▼           ▼              ▼             ▼
┌────────┐ ┌─────────┐ ┌─────────────┐ ┌──────────┐ ┌──────────────┐
│MODULES │ │ENVIRON- │ │INTEGRATIONS │ │FEATURE_  │ │   (queries   │
│(tree)  │ │MENTS    │ │(Jira, ...)  │ │FLAGS     │ │   under      │
└───┬────┘ └─────────┘ └─────────────┘ └──────────┘ │   project)   │
    │                                               └──────────────┘
    │ groups
    ▼
┌──────────────┐         ┌─────────────────────┐
│ USER_STORIES │────────►│ ACCEPTANCE_CRITERIA │
│  (US)        │  1..n   │   (AC)              │
└──────┬───────┘         └──────────┬──────────┘
       │                            │
       │ referenced_by              │ referenced_by (M:N)
       ▼                            ▼
┌──────────────────────────────────────────────────┐
│                      ATCS                        │
│  (Atomic Test Components — anchored to US + AC)  │
│  module_id, user_story_id, layer, slug, version  │
└─────┬──────────────┬──────────────┬──────────────┘
      │              │              │
      │ ordered      │ ordered      │ M:N
      ▼              ▼              ▼
┌──────────┐   ┌──────────────┐  ┌─────────────────────────┐
│ATC_STEPS │   │ATC_ASSERTIONS│  │ATC_ACCEPTANCE_CRITERIA  │
│(content, │   │(content)     │  │(atc_id + ac_id join)    │
│ input,   │   └──────────────┘  └─────────────────────────┘
│ expected)│
└──────────┘
      ▲ (invoked by)
      │
┌─────┴────────────────────────────────────┐
│            TEST_STEPS                    │   (the chain)
│  (test_id + atc_id + position)           │
└─────┬────────────────────────────────────┘
      │ chains
      ▼
┌──────────────┐
│    TESTS     │   (ordered chain of ATCs — NOT free-form steps)
│  module_id?  │
│  tags[]      │
└──────┬───────┘
       │ executed by
       ▼
┌────────────────────────────────────┐
│             RUNS                   │   header: status, env,
│ test_id, environment, executor,    │   executor.type ∈ {human|agent|ci}
│ status, started_at, finished_at,   │   idempotency_key (24h)
│ progress_pct, abort_reason         │
└──────┬─────────────────────────────┘
       │ produces
       ▼
┌──────────────┐  per-ATC rollup
│  RUN_ATCS    │──────────────┐
└──────┬───────┘              │ snapshot_of
       │ tracks               ▼
       ▼                  [ATCS — ref only]
┌──────────────┐
│  RUN_STEPS   │  per-step result (pass/fail/block/skipped),
│              │  duration_ms, notes, evidence_url, error_message
└──────────────┘

       ┌─────────────────────────────────────────┐
       │                BUGS                     │
       │ module_id (req), atc_id?, run_id?,      │
       │ severity P1..P4, status open→closed,    │
       │ description, steps_to_reproduce,        │
       │ evidence_urls[], external_id? (Jira)    │
       └─────────────────────────────────────────┘

  Cross-cutting:
  ┌─────────────────┐  ┌──────────────────┐  ┌──────────────────┐
  │ ACTIVITY_LOG    │  │ IDEMPOTENCY_KEYS │  │ FEATURE_FLAGS    │
  │ (audit-light    │  │ (24h TTL,        │  │ (workspace? /    │
  │  on all writes) │  │  POST replay)    │  │  project? scope) │
  └─────────────────┘  └──────────────────┘  └──────────────────┘
```

### Entities and Their Business Role

> Core MVP entities = directly authored or queried by the user every day. Cross-cutting = infrastructure that enables MVP behavior but is rarely touched by hand. Sources: `architecture-specs.md` §2 (ERD + quick-reference table).

| Entity | Layer | Business Role | Why It Exists |
|---|---|---|---|
| `workspaces` | Core (tenant) | Tenant root — isolates one team/customer from another | Multi-tenant SaaS + self-hosted single-tenant share the same shape; everything else hangs off this id |
| `workspace_members` | Core (RBAC) | Maps users to workspaces with a role | RBAC anchor for RLS policies (`viewer ⊂ member ⊂ admin ⊂ owner`) |
| `workspace_invites` | Core (RBAC) | Pending email-based invitations with 24h signed tokens | Onboarding flow without forcing pre-existing accounts |
| `access_tokens` | Cross-cutting (auth) | Hashed Personal Access Tokens for headless API consumers | Lets AI agents + CLI + CI drive the system without UI session |
| `projects` | Core | The app/product under test inside a workspace | One Bunkai workspace can host many products; each gets its own module tree and run history |
| `modules` | Core (taxonomy) | Tree-shaped product taxonomy (e.g. `/cart/add-to-cart`) | Modules are **first-class entities** (not folders) — defect heatmaps and coverage roll up by Module |
| `environments` | Core (config) | Named deployment targets a Run can hit (staging, prod, ...) | Lets the same Test run against multiple environments with attribution |
| `integrations` | Core (config) | Per-project config + secrets-ref for Jira / GitHub / etc. | Externalizes credentials and per-project mappings (Jira project key, JQL templates) |
| `feature_flags` | Cross-cutting | Workspace- or project-scoped boolean toggles | Gates Phase-2 features (semantic search, agentic mode, mind-map) during gradual rollout |
| `user_stories` | Core (traceability) | Business-context anchor (`title` + Markdown `description` + optional `external_id` to Jira) | Anchors testing to product intent — ATCs without a US cannot exist |
| `acceptance_criteria` | Core (traceability) | One AC = one testable behavior, sortable, Markdown-bodied | The atomic provenance unit — every ATC links to ≥1 AC |
| **`atcs`** | **Core (heart of product)** | The atomic, reusable, US+AC-anchored test component | Replaces free-form test steps; enables one-edit-many-tests propagation |
| `atc_steps` | Core | Ordered, Markdown-bodied executable steps inside an ATC | Decoupled rows so position can be rebalanced and Realtime can broadcast per-step changes |
| `atc_assertions` | Core | Ordered assertions evaluated at the end of an ATC | Separates "do this" (steps) from "now check this" (assertions) |
| `atc_acceptance_criteria` | Core (join) | M:N — which ACs an ATC satisfies | One ATC can satisfy multiple ACs; one AC can be covered by multiple ATCs |
| `tests` | Core | Named, taggable container — owns a chain of ATCs | Tests are the unit of **execution**; ATCs are the unit of **authorship** |
| `test_steps` | Core (join) | Ordered `test_id + atc_id + position` chain table | The chain itself — by storing references (not copies), ATC edits propagate automatically |
| `runs` | Core (execution) | One execution instance of a Test against an environment | Header carries executor identity (human/agent/ci), idempotency key, progress, lifecycle |
| `run_atcs` | Core (execution) | Per-ATC rollup inside a Run — snapshot of which ATCs were executed | Aggregates child step statuses to a single ATC verdict |
| `run_steps` | Core (execution) | Per-step result rows with evidence URL, error msg, duration | The grain at which evidence and timing live |
| `bugs` | Core | Native defect (anchored to Module mandatory + ATC + Run optional) | Keeps defect context in the QA loop instead of delegating to Jira |
| `activity_log` | Cross-cutting (audit-light) | Single insert-only stream of `{actor, action, entity, payload_summary}` | Workspace activity timeline; not compliance-grade (Enterprise audit ships Phase 3) |
| `idempotency_keys` | Cross-cutting | `(endpoint, key) → cached response, expires_at` | Lets retried POSTs (especially from agents) return the original response instead of duplicating state |

### Key Relationships

The map has **one rule that organizes everything else**: every ATC carries `user_story_id` (mandatory) and is joined to ≥1 row in `atc_acceptance_criteria` (mandatory). This is the **structural traceability** the product enforces (`business-model.md` §Value Propositions): you cannot create an ATC without telling the system which user story and which acceptance criterion it serves. The product's pedagogy is delivered through this rule, not through documentation.

Other relationships of note:

- **Tests reference ATCs, they don't embed them.** `test_steps.atc_id` is a foreign key, not a copy. When an ATC mutates (`FR-012`), every Test that chains it sees the new version automatically — this is the "one-edit-many-tests" promise made tangible at the schema level.
- **Runs snapshot, they don't reference live.** `run_atcs` and `run_steps` are independent rows so historical Run integrity is preserved even if the underlying ATC is later edited. `run_atcs.atc_id` is a pointer for navigation; the step content executed at that moment lives durably in `run_steps`.
- **Bugs are anchored to Modules (mandatory).** This is what makes the defect heatmap (`FR-027`) possible without runtime joins or per-tenant configuration: Module is the rollup axis, period.
- **Bugs optionally link to ATCs + Runs.** Filing a Bug from inside a Run (Journey 2, Step 4) auto-fills both — but a free-floating Bug filed from the Bugs view still works for "I saw something weird, ticket it now".
- **Modules form a tree via `parent_module_id` + materialized `path`.** `FR-006` caps depth at 6 (soft warning at 4). Recursive CTEs power the Tree View; the materialized `path` column (`/cart/add-to-cart`) avoids recursion for breadcrumbs and subtree filters.

---

## 3. Business Flows

### Flow 1: Setup (Workspace → Project → Module → US → AC → ATC)

**Source**: User Journey 1 ("First-time setup"), FR-001 through FR-010.

```
[Elena] ─sign in (GitHub OAuth)──► Supabase Auth ──► users row + default workspace
   │                                                    │
   │ (or accepts invite link)                           ▼
   │                                       workspace_members(role=owner)
   │
   ├─create Project "Checkout v2"────────► projects row (workspace_id, slug)
   │
   ├─create Modules /cart, /payment ─────► modules rows (tree, materialized path)
   │
   ├─Settings → connect Jira ────────────► integrations row (kind=jira, secrets_ref)
   │
   ├─inside /cart/add-to-cart            ┌────────────────────────────────┐
   │   "Import from Jira" ──────────────►│ async import worker            │
   │                                      │ (Vercel cron-triggered, MVP)   │
   │                                      │ pulls Jira REST, maps issue→US │
   │                                      │ extracts AC heuristic from MD  │
   │                                      └──────┬─────────────────────────┘
   │                                             ▼
   │                                user_stories + acceptance_criteria rows
   │                                (external_id = Jira key, idempotent)
   │
   └─click "+ New ATC" on AC-01 ────────►┌─────────────────────────────────┐
       fill title, layer=UI, steps[],    │ TRANSACTION:                    │
       assertions[], tags[], ac_ids[]    │  INSERT atcs                    │
                                         │  INSERT atc_steps (ordered)     │
                                         │  INSERT atc_assertions          │
                                         │  INSERT atc_acceptance_criteria │
                                         │  INSERT activity_log            │
                                         │  emit atc.created               │
                                         └──────┬──────────────────────────┘
                                                ▼
                                  Supabase Realtime broadcasts row change
                                  → all tabs subscribed to project_id
                                    refresh the tree node
```

**Flow Narrative:**

1. User authenticates (Supabase Auth — OAuth or magic link). On first verified login, FR-001 creates a default personal workspace unless they came in via invite.
2. User creates the Project — `projects` row with workspace-scoped unique slug (FR-005). Caller must already be `member` or higher.
3. User builds the Module tree — `modules` rows with `parent_module_id` and materialized `path` (FR-006). Tree depth bounded ≤6.
4. (Optional) User connects Jira integration in workspace settings — `integrations` row carrying `config` (jsonb) and `secrets_ref`.
5. (Optional) User imports User Stories from Jira (FR-009): an async worker pulls via Jira REST `search`, parses each issue (Atlassian Document Format → Markdown), heuristically extracts AC bullets, dedupes by `external_id`, places in matching Module or `Inbox`.
6. Alternatively, user authors US + AC directly in the right panel — Markdown bodies persisted to `user_stories` and `acceptance_criteria`.
7. User creates the first ATC (FR-010) — a single transaction inserts the parent row plus all child `atc_steps`, `atc_assertions`, and `atc_acceptance_criteria` joins. The layer enum (`UI|API|Unit`) is mandatory; the save button stays disabled until set (Journey 1, Step 6 mitigation).
8. `activity_log` row appended. Supabase Realtime broadcasts the new row to every client subscribed to this `project_id` — open tabs refresh the tree.

**Business Rules:**

- **Workspace slug global, project slug workspace-scoped, module path project-scoped, ATC slug = `{module-slug}/{atc-id-padded}`.** This namespace hierarchy is what makes URLs and command-palette search resolvable.
- **ATC must specify `user_story_id` AND at least one `acceptance_criterion_id` belonging to that US** (FR-010 validation). The data model rejects orphan ATCs at the constraint layer, not at the application layer.
- **Module depth ≤6, soft warning at 4** — prevents pathological trees (Journey 1, Step 4 mitigation).
- **Jira import dedupes by `external_id`** — re-running the import is idempotent.

**Code Involved** (post-bootstrap targets, not yet implemented):

- `app/api/v1/workspaces/route.ts` — FR-002 workspace create
- `app/api/v1/projects/route.ts` — FR-005
- `app/api/v1/modules/route.ts` — FR-006 (CRUD + tree validation)
- `app/api/v1/user-stories/route.ts` — FR-007
- `app/api/v1/acceptance-criteria/route.ts` — FR-008
- `app/api/v1/atcs/route.ts` — FR-010 (transactional create)
- `app/api/v1/imports/jira/route.ts` + worker — FR-009
- Frontend: `app/(workspace)/projects/[slug]/page.tsx` and ATC editor under `app/(workspace)/projects/[slug]/atcs/`

**Side effects:**

- `activity_log` row per state-changing endpoint (FR-038).
- Supabase Realtime row broadcast on every entity insert/update (FR-040 covers `runs`/`bugs`; ATC tree updates inherit Realtime via separate channel).
- Jira backlink written to the imported issue's body during one-way pull.

---

### Flow 2: Manual Run + Bug filing

**Source**: User Journey 2 ("Manual execution"), FR-019 through FR-025.

```
[Elena] ─open Test detail, click "Start Run" ──► POST /api/v1/runs
                                                     │
                                                     ▼
                                  ┌──────────────────────────────────────┐
                                  │ TRANSACTION:                         │
                                  │  INSERT runs (status=running,        │
                                  │    started_at=now, idempotency_key)  │
                                  │  for each ATC in test_steps:         │
                                  │    INSERT run_atcs (status=pending)  │
                                  │    for each atc_step:                │
                                  │      INSERT run_steps (status=pending)│
                                  └──────────────┬───────────────────────┘
                                                 ▼
                                  Realtime: rows broadcast on runs/run_atcs
                                                 │
   ┌─────────────────────────────────────────────┘
   │
   │ for each step Elena marks:
   │
   ▼
POST /api/v1/runs/{run_id}/steps/{run_step_id}/result
   │   body: {status, duration_ms?, notes?, evidence_url?, error_message?}
   │
   ▼
┌─────────────────────────────────────────┐
│ TRANSACTION (FR-020):                   │
│  UPDATE run_steps                       │
│  RECOMPUTE run_atcs.status              │
│     (all pass→pass, any fail→fail,      │
│      any block w/o fail→block,          │
│      else running)                      │
│  RECOMPUTE runs.progress_pct            │
└──────────┬──────────────────────────────┘
           ▼
   Realtime row broadcast → any tab watching the Run refreshes

   On a fail status:
   ──► UI highlights "Report Bug" CTA
   ──► side drawer with Bug form pre-filled
       (title from ATC+step, module from ATC, steps_to_reproduce
        autopopulated from executed steps)
            │
            ▼
       POST /api/v1/bugs  (FR-025)
            │
            ▼
   ┌────────────────────────────────────┐
   │ TRANSACTION:                       │
   │  INSERT bugs (status=open)         │
   │  link atc_id (auto from run_atc)   │
   │  link run_id                       │
   │  insert activity_log               │
   │  refresh module_defect_stats       │
   │    materialized view (FR-027)      │
   └──────────┬─────────────────────────┘
              ▼
   If Jira integration enabled: async worker (FR-028)
   creates Jira issue, writes back external_id, backlinks both ways.

   When done (or blocked):
   POST /api/v1/runs/{run_id}/abort  OR  /finish
   ──► runs.status = aborted | passed | failed
       finished_at = now
       remaining run_steps → skipped
```

**Flow Narrative:**

1. User picks a Test, clicks "Start Run", chooses environment + executor identity = self (FR-019). A single transaction creates the `runs` header plus the full per-ATC and per-step skeleton (all `pending`).
2. For each step in the runner, user marks pass/fail/block — `POST .../steps/{run_step_id}/result` (FR-020). Each call updates the leaf row AND recomputes parent `run_atcs.status` AND parent `runs.progress_pct` in the same transaction.
3. Realtime row broadcast pushes updates to every other tab watching this Run (Journey 2, Step 7: Mateo's dashboard updates live).
4. On a `fail` result, the UI highlights a "Report Bug" CTA. Clicking it opens a side drawer with the Bug form pre-filled: `module_id` inherited from the ATC, `steps_to_reproduce` auto-populated from the steps already executed, `atc_id` and `run_id` set.
5. User picks severity (P1–P4 keyboard shortcuts), edits title, saves → `POST /api/v1/bugs` (FR-025). Transaction inserts the Bug, links it to Run + ATC + Module, appends to `activity_log`, triggers Module's `defect_count` materialized view refresh (FR-027).
6. If `integrations` for Jira is configured, an async worker (FR-028) creates the Jira issue and writes `external_id` + `external_url` back to the Bug.
7. User aborts (FR-021) or finishes (FR-024) the Run — `runs.status` set terminal, remaining `run_steps` swept to `skipped`.

**Business Rules:**

- **Run lifecycle is monotonic and terminal-irreversible.** Once `runs.status` becomes `passed|failed|aborted`, no further step updates are accepted (FR-020 validation: "run not in terminal status").
- **`run_atcs.status` is derived, not user-input.** It is recomputed every time a child `run_step` updates. UI cannot mutate it directly.
- **Bug `module_id` is mandatory** — heatmap depends on it.
- **Bug filed from inside a Run auto-links atc_id from the failing run_atc.** No copy-paste required (Journey 2 acceptance: "all mandatory fields without manual re-entry").
- **`runs.idempotency_key` is unique per `test_id` for 24h.** Re-issuing the same start-run with the same key returns the existing `run_id` (FR-019 behavior + FR-037 generalization).

**Code Involved:**

- `app/api/v1/runs/route.ts` — FR-019 start, FR-024 finish
- `app/api/v1/runs/[id]/abort/route.ts` — FR-021
- `app/api/v1/runs/[id]/steps/[stepId]/result/route.ts` — FR-020
- `app/api/v1/bugs/route.ts` — FR-025
- Materialized view + refresh trigger: `module_defect_stats` (FR-027)
- Frontend runner: `app/(workspace)/projects/[slug]/runs/[runId]/page.tsx`
- Bug-from-run drawer: `components/runner/bug-drawer.tsx`

**Side effects:**

- Realtime row broadcasts on `runs`, `run_atcs`, `run_steps`, `bugs` (FR-040).
- `activity_log` rows on every state change (FR-038).
- `module_defect_stats` materialized view refreshed on bug create (FR-027).
- Jira sync async worker enqueued on bug create when integration enabled (FR-028).

---

### Flow 3: Agent-driven Run via API

**Source**: User Journey 3, architecture §4.3, FR-019/020/024/025 plus FR-034 (Bearer auth) and FR-037 (idempotency).

```
[02:00 UTC scheduler] ──► AI Agent (Karim, Playwright + Claude Code)
                            │
                            │ Authorization: Bearer bk_pat_...
                            ▼
                  ┌─────────────────────────────────────┐
                  │ Next.js middleware                  │
                  │  - lookup access_tokens by sha256   │
                  │  - verify expiry + revocation       │
                  │  - attach {user_id, workspace_id,   │
                  │    scopes} to request context       │
                  └──────────────┬──────────────────────┘
                                 │ valid
                                 ▼
   GET /api/v1/me  ───► 200 {scopes, workspace_id}
   GET /api/v1/tests/TEST-008?expand=atcs.steps,atcs.assertions
       ───► 200 {test, atcs:[{atc, steps[], assertions[]}, ...]}
                                 │
                                 ▼
   POST /api/v1/runs
       body: {test_id, environment, executor:{type:"agent", identity},
              idempotency_key:"2026-05-19-nightly"}
       ───► 201 {run_id}
       (idempotency: same key in 24h returns same run_id)
                                 │
                                 ▼ (loop per step)
   POST /api/v1/runs/{run_id}/steps/{step_id}/result
       body: {status, duration_ms, evidence_url, error_message?}
       ───► 200 {run_step, derived:{run_atc_status, run_progress_pct}}
                                 │
                                 ▼ (on fail)
   POST /api/v1/bugs
       body: {title, module_id, severity, atc_id, run_id,
              description, steps_to_reproduce}
       ───► 201 {bug_id, jira_sync_status}
                                 │
                                 ▼ (done)
   POST /api/v1/runs/{run_id}/finish
       body: {status:"failed", finished_at}
       ───► 200 {run}
```

**Flow Narrative:**

1. Scheduler kicks the agent process. Agent loads its PAT from secrets and calls `GET /me` to validate.
2. Middleware verifies the token via SHA-256 hash lookup in `access_tokens`, checks expiry + revocation, attaches workspace context.
3. Agent fetches the full Test contract in one request (`?expand=...`) — ordered ATCs with their steps and assertions — so it knows the entire script up front.
4. Agent calls `POST /runs` with an `idempotency_key` (typically the run's logical name like `2026-05-19-nightly`). If the agent retries due to a transient failure, the same key returns the original `run_id` (FR-037, `idempotency_keys` table).
5. Per step: agent drives the application via Playwright, captures evidence to R2, then `POST .../result`. The transaction is **identical** to the manual flow — same endpoint, same processing (FR-020 recompute parent statuses).
6. On a failure, agent fires `POST /bugs` with the same payload shape a human would submit. Bug appears in the same dashboards.
7. Agent calls `POST .../finish` with terminal status. Run is closed.

**Business Rules:**

- **Data shape is identical to manual mode.** This is the structural decision behind Bunkai's "three-mode" claim — dashboards aggregate without conditional logic. `executor.type ∈ {human, agent, ci}` is metadata, not a switch.
- **Idempotency is mandatory for safe retry.** Agents are expected to send `Idempotency-Key` on every create endpoint (FR-037).
- **A `running` Run abandoned by an agent does not auto-close.** A sweeper cron (configurable `max_duration`, default 4h — Journey 3 alt-paths) marks abandoned Runs as `aborted` with reason "agent timeout".
- **Token scopes can be tighter than role.** A `member` user can mint a read-only token; the bearer of that token cannot create ATCs even though the underlying user could (architecture §5 RBAC).

**Code Involved:**

- `app/api/v1/auth/tokens/route.ts` — FR-034 issuance
- `middleware.ts` — Bearer verification + workspace-context attachment
- Same `/runs`, `/bugs` endpoints as Flow 2 (this is the point)
- Future sweeper cron: `app/api/cron/run-timeout/route.ts` (Vercel cron-scheduled)

**Side effects:** Same as Flow 2. Plus: each authenticated request increments a `last_used_at` on `access_tokens` (per FR-034 token-usage semantics, implementation tracked in `/project-bootstrap`).

---

### Flow 4: Tree-view rollup query (read-heavy, no writes)

**Source**: FR-029, User Journey 1 Step 4.

```
GET /api/v1/projects/{id}/tree?expanded=mod-1,mod-3
       │
       ▼
┌────────────────────────────────────────────────────────────┐
│ Recursive CTE on modules                                   │
│  WITH RECURSIVE tree AS (                                  │
│    SELECT * FROM modules WHERE project_id=? AND parent IS NULL│
│    UNION ALL                                               │
│    SELECT m.* FROM modules m JOIN tree t                   │
│      ON m.parent_module_id = t.id                          │
│  )                                                         │
│  JOIN counts of user_stories, atcs, tests per module       │
│  JOIN last-run status_dot per subtree                      │
└──────────────────────────┬─────────────────────────────────┘
                           ▼
              200 {tree: [{type, id, name, path,
                           status_dot, children:[...]}]}
```

This is the workhorse query for the left-side navigation. It is read-heavy and per-Project — needs proper indexing on `(project_id, parent_module_id)` and a fast aggregate path for `status_dot`. Tree rebuild on every change is too expensive; the Realtime row broadcast on `runs` triggers a per-subtree partial refetch on the client.

---

## 4. State Machines

### 4.1 `runs.status`

```
                ┌───────────┐
   POST /runs ─►│ running   │
                └─────┬─────┘
                      │
        ┌─────────────┼─────────────┐
        ▼             ▼             ▼
   ┌─────────┐  ┌─────────┐  ┌───────────┐
   │ passed  │  │ failed  │  │ aborted   │
   │(terminal│  │(terminal│  │(terminal, │
   │ — all   │  │ — any   │  │  carries  │
   │ atcs    │  │ atc     │  │  reason)  │
   │ pass)   │  │ failed) │  │           │
   └─────────┘  └─────────┘  └───────────┘
```

| From | To | Triggering Event | Effects |
|---|---|---|---|
| (init) | `running` | `POST /runs` (FR-019) | `started_at` set; `run_atcs` + `run_steps` skeleton inserted |
| `running` | `passed` | `POST /finish` with `status=passed` (FR-024) OR all `run_atcs` resolved `pass` | `finished_at` set; activity log |
| `running` | `failed` | `POST /finish` with `status=failed` OR any `run_atc.status=fail` at finish | `finished_at` set; affects test "last run" status |
| `running` | `aborted` | `POST /abort` (FR-021) | `abort_reason` recorded; remaining `run_steps` set to `skipped` |
| any terminal | — | — | no transitions allowed; FR-020 rejects step result writes on terminal Runs |

**Business Rules:**
- Terminal states are absorbing. A Run is never "reopened" — re-running the same Test creates a new Run row, preserving history.
- Sweeper cron (Phase 2-hardened) may force `running → aborted` if `now - started_at > max_duration`.

### 4.2 `run_atcs.status`

```
pending ──► running ──► pass    (terminal — all child steps passed)
                   │
                   ├──► fail    (terminal — at least one child step failed)
                   │
                   ├──► block   (terminal — at least one child step blocked,
                   │             none failed)
                   │
                   └──► skipped (terminal — parent Run was aborted before
                                  this ATC started)
```

| From | To | Triggering Event | Effects |
|---|---|---|---|
| `pending` | `running` | First child `run_step` transitions out of `pending` | Recomputed in FR-020 transaction |
| `running` | `pass` | All children `run_steps.status=pass` | Recomputed |
| `running` | `fail` | Any child `run_steps.status=fail` | Recomputed; subsequent passes don't undo |
| `running` | `block` | Any child `run_steps.status=block` and no `fail` | Recomputed |
| any non-terminal | `skipped` | Parent Run abort (FR-021) | Cascade via `runs.status=aborted` handler |

**Business Rule:** `run_atcs.status` is **derived**, never written by clients directly.

### 4.3 `run_steps.status`

```
pending ──► pass | fail | block | skipped
```

Simple single-write transition. The interesting logic lives one level up (`run_atcs` recomputation).

### 4.4 `bugs.status`

```
   ┌──────┐   triage    ┌──────────────┐    fix       ┌──────────┐
   │ open │────────────►│ in_progress  │─────────────►│ resolved │
   └──┬───┘             └──────┬───────┘              └────┬─────┘
      │                        │                           │
      │ resurfaced              │ won't fix /              │ verified
      │ regression              │ stale                    │ by QA
      ▼                        ▼                           ▼
      └─────────────────────────────────────► ┌──────────┐
                                              │  closed  │
                                              └──────────┘
                                                   │
                                                   │ reopen (regression
                                                   │  on the same defect)
                                                   ▼
                                                 open
```

| From | To | Triggering Event | Effects |
|---|---|---|---|
| (init) | `open` | `POST /bugs` (FR-025) | `module_defect_stats` refresh; Jira sync enqueued |
| `open` | `in_progress` | Manual status change by member+ | activity_log row |
| `in_progress` | `resolved` | Developer claims fix shipped | activity_log row |
| `resolved` | `closed` | QA verification passed | activity_log row; defect closed metric |
| `closed` | `open` | Regression filed pointing at same Bug | "reopened" badge; metric tracks reopen rate |
| any | `closed` (won't fix) | Admin closes without resolution | requires explicit reason |

**Business Rule:** Reopening is a state regression and explicitly allowed — the heatmap counts it. SRS does not enumerate sub-transitions beyond the four canonical states; downstream `/sprint-development` may refine.

### 4.5 `workspace_invites.status`

```
pending ──► accepted   (user clicks link, signs up / links existing account)
       │
       ├──► expired   (24h elapsed without action)
       │
       └──► revoked   (admin cancels)
```

24h expiry enforced by signed token, not by row lifecycle. The row is read to drive the activity timeline.

### 4.6 `imports.status` (Jira import job)

```
pending ──► running ──► succeeded   (imported_count, errors[])
                   │
                   └──► failed       (top-level error captured)
```

**Note**: the SRS architecture-specs.md ERD does **not** explicitly declare an `imports` table even though FR-009 references it (`/imports/{id}` polling endpoint) and architecture §4.4 narrates an imports flow. Documented as **Discovery Gap §7-G1** below. `/project-bootstrap` will need to materialize this table.

### 4.7 `access_tokens.status` (implicit via `expires_at` + `revoked_at`)

```
active ──► revoked   (DELETE /auth/tokens/{id} — FR-034)
      │
      └──► expired   (now() > expires_at)
```

Tokens are not explicitly status-stamped — the verification middleware computes their state on every request from `expires_at` and `revoked_at` columns.

---

## 5. Automatic Processes

### Database Triggers / Computed Columns

| Process | When It Executes | What It Does | Why It Exists |
|---|---|---|---|
| `run_atcs.status` recomputation | On every `run_steps` UPDATE (FR-020 transaction) | Folds child step statuses into a single per-ATC verdict | Derived value, never client-written — keeps the Run rollup consistent in one transaction |
| `runs.progress_pct` recomputation | Same transaction as above | `(completed_steps / total_steps) * 100` | Drives the progress bar in the runner without polling |
| `module_defect_stats` materialized view refresh | On `bugs` INSERT (FR-027) | Recomputes `defect_count` + week-over-week trend per module | Defect heatmap renders in <5s of new Bug (success criterion in `mvp-scope.md` §4) |
| `tsvector` index update | On `atcs` INSERT/UPDATE | Maintains the GIN index on title + tags for full-text search (FR-011) | ATC search & autocomplete (FR-011) without a separate search service |
| Materialized `modules.path` column update | On `modules` INSERT/UPDATE/parent change (FR-006) | Computes and stores `/cart/add-to-cart` style path | Subtree filters and breadcrumbs without recursive joins |
| Soft-delete cascade | On `modules` archive (FR-006) | Sets `archived_at` on descendant Modules, US, AC, ATC, Tests | Mass-archive without explicit per-row deletion |

### Cron Jobs / Scheduled Tasks

> MVP uses Vercel Cron with serverless functions. Phase 2 self-hosted uses BullMQ + Redis.

| Job | Frequency | What It Does | Why It Exists |
|---|---|---|---|
| `idempotency-cleanup` | Hourly (sweep keys older than 24h) | Deletes expired rows from `idempotency_keys` | Bounded table size; FR-037 only guarantees 24h replay window |
| `run-timeout-sweeper` | Every 15 min | Marks Runs `running` for > `max_duration` (default 4h) as `aborted` with reason "executor timeout" | Journey 3 alt-path: agent abandons → Run stuck `running` forever otherwise |
| `jira-bug-sync-retry` | Every 5 min | Retries bugs with `jira_sync_status=failed` (exponential backoff) | FR-028 promises eventual consistency under Jira flakiness |
| `defect-stats-refresh` | On-demand + nightly fallback | Refreshes `module_defect_stats` materialized view | Trigger-driven primarily, nightly safety net |
| `audit-log-archival` (Phase 2) | Daily | Archives `activity_log` rows older than retention window | Bounded table size — Enterprise audit Phase 3 handles compliance retention |

### Async Workers / Incoming Webhooks

| Worker / Webhook | Source / Trigger | What It Processes | System Effects |
|---|---|---|---|
| Jira import worker | `POST /api/v1/imports/jira` enqueue (FR-009) | Pulls Jira REST `search` in pages, ADF→Markdown, AC heuristic extraction, dedup by `external_id` | Inserts `user_stories` + `acceptance_criteria` rows; updates `imports.status` |
| Jira bug-sync worker | `bugs` INSERT when integration enabled (FR-028) | Creates Jira issue via REST, writes back `external_id`+`external_url`, backlinks Jira body | Mutates `bugs.external_id` + `bugs.external_url`; sets `jira_sync_status` |
| Realtime fan-out (Supabase Realtime) | Postgres logical replication on `runs`, `run_atcs`, `run_steps`, `bugs` (FR-040) | Streams row changes over WebSocket to subscribed clients | Drives live UI updates without polling |
| Activity-log appender | Server-side hook in every state-changing route handler (FR-038) | Inserts `{actor_id, action, entity_type, entity_id, payload_summary}` | Workspace activity timeline |
| Run-evidence uploader | Client-driven via signed URL from `POST /uploads/run-evidence` | Uploads screenshots/video to Cloudflare R2 (or MinIO self-hosted) | Returns URL stored in `run_steps.evidence_url` / `bugs.evidence_urls[]` |

---

## 6. External Integrations

### Jira (Atlassian)

```
       ┌─────────────────┐                          ┌────────────────┐
       │     Bunkai      │ ─── one-way pull ───────►│   Jira REST    │
       │   (workspace)   │     (FR-009 import)      │      API       │
       │                 │                          │                │
       │                 │ ─── bug sync (push)─────►│                │
       │                 │     (FR-028 async)       │                │
       │                 │◄──── (no inbound        )│                │
       │                 │      ◄── Phase 3:        │                │
       │                 │      bidirectional sync  │                │
       └─────────────────┘                          └────────────────┘
```

**What it does:**
- **In (one-way pull, FR-009)**: User Stories + ACs imported from Jira into Bunkai. Maintains `user_stories.external_id` (Jira issue key) for dedup.
- **Out (push, FR-028)**: Bug create in Bunkai → Jira issue create. Stores `bugs.external_id` + `bugs.external_url`. Backlinks Jira issue body to Bunkai URL.

**How it affects data:**
- `integrations` row carries `kind=jira` + `config` (JQL templates, project mapping) + `secrets_ref`.
- `user_stories.external_id` + `acceptance_criteria` (inferred from issue description) on import.
- `bugs.external_id` + `bugs.external_url` + `jira_sync_status` on bug sync.

**Flows that depend on this:** Flow 1 (Step 5 import), Flow 2 (Step 5 bug sync).

**Failure behavior:**
- Jira unreachable on import → import job marked `failed`, user surface in `/imports/{id}` polling.
- Jira unreachable on bug sync → `jira_sync_status=failed`, `jira-bug-sync-retry` cron retries with exponential backoff. Bug remains usable in Bunkai natively — Jira is best-effort.

### Cloudflare R2 (Cloud edition) / MinIO (Community edition)

```
   Browser ──► Bunkai ──► signed URL ──► R2 / MinIO bucket
        (PUT direct upload)
                                            │
                                            ▼
                                  evidence URL stored
                                  in run_steps / bugs
```

**What it does:** Stores Run evidence (screenshots, videos) and Bug attachments. Bunkai never proxies the blob; client uploads directly via signed URL.

**How it affects data:** `run_steps.evidence_url`, `bugs.evidence_urls[]`.

**Flows that depend on this:** Flow 2 (Step 4 evidence on failed step), Flow 3 (agent-uploaded screenshots per step).

**Failure behavior:** Signed URL generation fails → step still records pass/fail with empty evidence; user notified. Stored URL fetch failure at view time → broken-image placeholder.

### Supabase Auth (Cloud) / Better Auth (Community, Phase 2)

```
   Browser ─ /auth/sign-in ─► Next.js ─► Supabase Auth ─► JWT cookie
                                                ▲
                                                │
                                          OAuth GitHub/Google
                                          OR  magic link email
```

**What it does:** End-user authentication. JWT carried in HttpOnly cookie. OAuth callback handled by Supabase; magic link email also Supabase-managed.

**How it affects data:** `auth.users` row + downstream `workspace_members` on first verified login (FR-001).

**Flows that depend on this:** All UI flows; Flow 1 (sign-in step).

**Failure behavior:** OAuth blocked by IT proxy → fallback magic-link visible within 30s (Journey 1 alt-path).

### Sentry

```
   Bunkai (server + client) ──► Sentry (errors, perf traces)
```

**What it does:** Error capture + performance monitoring for unhandled exceptions.

**How it affects data:** None on Bunkai side — observability sink only.

**Failure behavior:** Sentry unreachable → events buffered briefly client-side, dropped on overflow. No user-visible impact.

### PostHog

```
   Bunkai (client) ──► PostHog (product analytics events)
```

**What it does:** Captures user behavior events (page views, feature usage, funnel completion).

**How it affects data:** None on Bunkai side.

**Failure behavior:** Events lost in a queue if PostHog is down. No user-visible impact.

### Phase 2 Integrations (not implemented in MVP — placeholder)

- **Playwright / Cypress / JUnit JSON adapters**: parse externally-produced test results into Bunkai `runs` + `run_atcs` + `run_steps` rows. Same data shape as manual/agent runs.
- **GitHub Issues / Linear**: optional outbound sync (parallel to Jira's path).
- **Webhook receivers (`/api/webhooks/jira`)**: inbound Jira status sync for bidirectional sync — Phase 3.

---

## 7. Multi-tenancy & Authorization (RLS)

Bunkai is a hard multi-tenant system at the database layer. Every entity that contains business data carries a `workspace_id` (directly or via a parent), and **Postgres Row Level Security** policies gate access at the row level rather than the application layer.

**Policy pattern** (architecture-specs.md §2.5):

```
For every table with workspace_id:
  USING (
    EXISTS (
      SELECT 1 FROM workspace_members wm
      WHERE wm.workspace_id = <table>.workspace_id
        AND wm.user_id      = auth.uid()
        AND wm.status       = 'active'
    )
  )
```

**Role inheritance** (`viewer ⊂ member ⊂ admin ⊂ owner`):

| Role | Capabilities |
|---|---|
| `viewer` | Read-only across every table in the workspace |
| `member` | Full CRUD on Project entities (Modules, US, AC, ATC, Test, Run, Bug) |
| `admin` | + Manages members, integrations, access tokens |
| `owner` | + Workspace deletion, billing (Cloud plan) |

**Token scopes are an additional restriction on top of role** — a `member` user can mint a read-only PAT; that bearer cannot create ATCs even though the underlying user could.

**Performance mitigation** (founder conversation, architecture §2.5): RLS overhead becomes measurable at scale. Mitigation = indexed `workspace_id` on every table + `SET LOCAL row_security = off` for trusted service-role queries (migrations, scheduled jobs) that already pre-filter explicitly.

---

## 8. Edition Variance (Cloud vs Community)

**The data model is identical across editions.** Only the infrastructure substrate differs. This is a deliberate decision: it preserves the "self-hosted is the same product" promise made in `business-model.md` §Value Propositions and removes the data-sovereignty objection for regulated buyers.

| Layer | Bunkai Cloud (MVP) | Bunkai Community (Phase 2) |
|---|---|---|
| Database | Supabase Postgres 16 | Plain Postgres 16 in Docker |
| Auth | Supabase Auth (JWT + OAuth) | Better Auth (or Auth.js) in-process |
| Realtime | Supabase Realtime (logical replication → WebSocket) | Redis pub/sub + custom WebSocket (extracted process) |
| Object store | Cloudflare R2 (S3-compatible, egress-free) | MinIO in Docker (S3-compatible) |
| Background jobs | Vercel cron + serverless functions | BullMQ + Redis |
| Search | Postgres `tsvector` GIN | Same |
| Errors | Sentry SaaS | Sentry self-hosted (community plan) |
| Analytics | PostHog Cloud | PostHog self-hosted |

**What this means for data-model work**: any schema change, RLS policy, materialized view, or trigger written today must be expressible on plain Postgres without Supabase-specific functions. Supabase-specific surfaces (Auth, Realtime, Storage) are wrapped behind thin abstractions per architecture §3 so that Phase 2 can swap implementations without touching business logic.

---

## 9. Discovery Gaps

> Greenfield project — no DB exists yet, no Supabase MCP provisioned. All entities and flows encoded directly from `.context/SRS/` and `.context/PRD/`. The following gaps surfaced during synthesis:

- **G1 — `imports` table referenced but not declared in the ERD.** `FR-009` describes a polling endpoint `/imports/{id}` returning `{status, imported_count, errors[]}`, and `architecture-specs.md §4.4` narrates the import flow, but the `imports` table is absent from the canonical ERD in `architecture-specs.md §2`. `/project-bootstrap` will need to materialize it (suggested columns: `id, project_id, kind="jira", status, jql, imported_count, errors_jsonb, started_at, finished_at`). Documented in §4.6 above.
- **G2 — `users` table is implicit.** The ERD references `USERS` as relationships but the entity quick-reference table starts at `workspaces`. Supabase Auth owns the `auth.users` row; the application layer joins through `workspace_members.user_id`. No Bunkai-managed `users` table is needed in MVP. Self-hosted edition (Better Auth) will introduce a `users` table; both editions must share the same FK target shape.
- **G3 — `user_view_state` table referenced by FR-032 but not in ERD.** Cross-cutting client persistence (active filters, sort, expanded nodes). Likely a single jsonb-bodied table keyed by `(user_id, project_id, view_kind)`. To be confirmed during `/project-bootstrap`.
- **G4 — Bug state machine sub-transitions not enumerated in SRS.** The ERD shows `bugs.status` exists, but neither `architecture-specs.md` nor `functional-specs.md` enumerate every transition. State machine §4.4 above is encoded from `mvp-scope.md` US 7.x semantics — confirm during `/product-management` AC refinement.
- **G5 — `feature_flags` consumers not enumerated.** Table declared in ERD; no current FR consumes it. Reserved for Phase-2 gradual rollout (semantic search, agentic mode, mind-map). Flag keys will be defined as features land.
- **G6 — Access-token `last_used_at` semantics.** FR-034 describes issuance/revocation/scopes but does not specify `last_used_at` tracking. Reasonable to add during `/project-bootstrap` for token-rotation UX.
- **G7 — Run "executor.identity" free-text vs FK.** `runs.executor_identity` is a string per the ERD quick-reference. For human runs it likely should reference `auth.users.id`; for agent/CI runs it's a free-form label (e.g. `claude-code-nightly-v3`). To confirm during bootstrap whether to split into two columns or keep as polymorphic string + type.

> No PRD/SRS sources were missing — `/project-foundation` Phases 1–3 produced complete inputs.

---

## 10. Downstream Consumers

This map feeds:

- **`/master-implementation-plan`** — uses the entity inventory + flow dependencies to schedule features in waves (Tenancy → Project/Module → US/AC → ATC → Test → Run → Bug → API+CLI). The dependency chain is the ERD's foreign-key topology.
- **`/sprint-development`** — every story implementation reads the relevant flow from §3 before planning. Specifically, ATC creation stories read Flow 1; runner stories read Flow 2; agent-API stories read Flow 3.
- **`/business-api-map`** — the next discovery doc. Cross-references each entity to its REST endpoints (FR-001..FR-040 → routes in `.context/SRS/api-contracts.yaml`).
- **`/business-feature-map`** — feature inventory + CRUD matrix, cross-referenced to entities here.
- **`/project-bootstrap`** — the schema-generation phase. Reads this map to confirm entity list and resolves Discovery Gaps G1-G3 (materializes `imports`, `user_view_state`, decides `executor_identity` column shape).

---

<!-- human-notes: anything written below this comment is preserved across regenerations -->
