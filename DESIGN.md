# DESIGN.md

> **Status**: Stub — to be regenerated via `/design-system` skill
> **Spec**: Google Labs Apache-2.0 DESIGN.md format (https://github.com/google-labs-code/design.md)
> **Generated by**: `/design-system` skill (5 generation paths available)
> **Last update**: 2026-05-11

---

## What is DESIGN.md?

DESIGN.md is the canonical design system of the project in the Google Labs Apache-2.0 spec format. It defines the visual identity and component vocabulary that the entire product is built against — color palette, typography, spacing tokens, component primitives, and theming approach.

It serves as the source of truth (SOT) for:

- `/project-bootstrap` (frontend-setup phase) — Tailwind, shadcn, and design-token configuration are derived from it.
- `/sprint-development` (Implementation stage) — feature implementation references DESIGN.md to keep visual consistency.
- Any future tooling that needs to reason about brand or visual primitives.

This file is currently a **stub**. Its real content is produced by running the `/design-system` skill.

---

## How to populate this file

The `/design-system` skill offers five generation paths. Pick whichever matches your situation:

1. **Default automatable** — `npx getdesign` + an LLM-matcher picks 1 of 72 curated brands based on Constitution + PRD. Fastest path; good when you trust the matcher.
2. **Manual gallery** — Browse https://designmd.ai/explore and pick a brand manually. Use this when you want to inspect alternatives visually before committing.
3. **Open Design (local)** — Run the local Open Design app via Docker (https://github.com/nexu-io/open-design). Use this when you want full offline control and inspection.
4. **Claude Design (premium)** — Hand off to https://claude.ai/design (Anthropic Labs research preview). Use this when you want a premium, AI-curated design pass.
5. **LLM-authored custom** — Have the AI author the design system from scratch, grounded in Constitution + PRD + brand intent. Use this when none of the existing brands fit and you want a bespoke system.

Run `/design-system` and the skill will walk you through the path selection.

---

## When to regenerate

Common triggers for re-running `/design-system`:

- Initial setup right after the PRD lands (invoked by `/project-foundation` Phase 2.5).
- Rebranding the project.
- Adding new primitives or tokens to the design system.
- Migrating between paths (e.g., from LLM-authored to Claude Design once it becomes available).

---

## Where this file is consumed

- `/project-bootstrap` — the frontend-setup phase reads DESIGN.md to configure Tailwind, shadcn, and design tokens.
- `/sprint-development` — the Implementation stage consults DESIGN.md so feature UI stays visually consistent.
- Future hooks: design-system linters, code-generation tools, visual-regression baselines.

If DESIGN.md is still a stub when one of these consumers runs, they should prompt the user to run `/design-system` first.

---

## Related files

- `README.md` — project overview.
- `CLAUDE.md` — operational AI memory and skill registry.
- `CONTEXT.md` — Context Engineering applied in this repo (root-level canonical reference).
- `.context/` — Context Engineering hierarchy (PRD, SRS, business maps, PBI).
- `.claude/skills/design-system/SKILL.md` — the skill that regenerates this file.
