{
  "skill_name": "git-flow-master",
  "evals": [
    {
      "id": 1,
      "prompt": "I just finished work on UPEX-277 (empty states for the reservations module). Need to commit the 4 changed files and push the branch — there's no PR yet. Repo uses main + staging.",
      "expected_output": "Skill detects main-integration strategy from layout (main+staging), persists marker to CLAUDE.md if missing, groups the 4 files into atomic conventional commits with scope UPEX-277, asks before pushing to current feature branch, and offers to open PR against staging next. No AI attribution. No git add -A.",
      "should_trigger": true,
      "files": []
    },
    {
      "id": 2,
      "prompt": "abrime un PR para esta branch contra staging, label feature, ya está todo pusheado",
      "expected_output": "Skill detects branch already pushed, renders inline PR body with Summary/Changes/Test Plan/Traceability/Risk, picks staging as base (matches main-integration strategy), proposes feature + ready-for-review labels, runs gh pr create --body-file with tempfile, returns PR URL. Does not auto-merge.",
      "should_trigger": true,
      "files": []
    },
    {
      "id": 3,
      "prompt": "git status shows 'both modified' on src/api/users.ts and 'deleted by them' on src/api/legacy-users.ts after I tried to merge staging into my feature branch. Help me fix this.",
      "expected_output": "Skill identifies a content conflict + a rename/delete conflict, follows the merge-conflict playbook from references/conflict-resolution.md (diagnose first, present options ranked by safety, never silently force/abort), guides resolution per file, verifies clean state with git status before exiting.",
      "should_trigger": true,
      "files": []
    },
    {
      "id": 4,
      "prompt": "We're starting a brand new repo for a side project. Just me, no team. What's the simplest git setup? I'll mostly push directly to main.",
      "expected_output": "Skill recognises the strategy-selection intent, presents the 7 supported strategies with one-line descriptions, recommends solo-main given the context (single contributor, simplicity), and writes the marker <!-- git-flow-master:strategy:solo-main --> to CLAUDE.md so future invocations skip the question.",
      "should_trigger": true,
      "files": []
    },
    {
      "id": 5,
      "prompt": "Implement the bulk-assign action for the users table. It needs a domain model with validation, a thin API endpoint, and a UI hook. About 600 lines total.",
      "expected_output": "Skill should NOT trigger as the primary handler — this is a feature-implementation request, owned by /sprint-development. git-flow-master only enters the picture later when /sprint-development needs branch/commit/PR operations. If git-flow-master responds at all, it should redirect to /sprint-development.",
      "should_trigger": false,
      "files": []
    },
    {
      "id": 6,
      "prompt": "Write a unit test for the calculateDiscount function that covers the edge case when the input is zero.",
      "expected_output": "Skill should NOT trigger — this is a unit-test design request, owned by /unit-testing. git-flow-master is purely a version-control-layer skill.",
      "should_trigger": false,
      "files": []
    },
    {
      "id": 7,
      "prompt": "the push got rejected with '! [rejected] feat/UPEX-200 -> feat/UPEX-200 (fetch first)'. what now?",
      "expected_output": "Skill recognises push-rejected diagnostic, follows the push-rejected playbook (fetch + diff to show what's missing both ways, present pull-merge vs pull-rebase vs force-push-with-lease ranked by safety), warns that force-push is destructive and only acceptable on unshared branches with explicit opt-in, never executes force silently.",
      "should_trigger": true,
      "files": []
    },
    {
      "id": 8,
      "prompt": "Este cambio va a tocar 800 líneas entre el schema, el backend y el frontend. Cómo lo trozeo en PRs más chicos?",
      "expected_output": "Skill recognises chained-PR planning intent. Either hands off to /chained-pr (the strategic-decision skill, which is installed in this repo) or walks the inline decision tree from references/branching-strategies.md (Q1 mechanical? Q2 linearly decomposable? Q3 shared scaffolding?). Returns a strategy (stacked-to-main, feature-branch-chain, or size-exception) plus a concrete branch plan.",
      "should_trigger": true,
      "files": []
    },
    {
      "id": 9,
      "prompt": "Brand new repo, just me. Run our git strategy setup — I'll only ever push to main, keep it dead simple.",
      "expected_output": "Strategy Setup (3.6) runs. Resolves solo-main (single contributor, only main, simplicity). Creates NO integration branch (materialization table: solo-main = main only). Asks NONE of Q1/Q2/Q3 (single-branch strategy gates them all out). Writes a MINIMAL runbook: marker <!-- git-flow-master:strategy:solo-main -->, branch table (main only), 'work lands on main via PR', commit rules. No invariant, no promotion, no hotfix block. No --force, no AI attribution.",
      "should_trigger": true,
      "files": []
    },
    {
      "id": 10,
      "prompt": "Set up our git strategy. Repo has main + staging already; staging is behind main. Use fast-forward promotion, merge commits for features, hotfix off prod with same-day back-merge.",
      "expected_output": "Strategy Setup runs. Resolves main-integration (main + staging). Materializes: staging already exists so it is NOT recreated; ancestry check (git log main..staging empty) shows main is ahead and staging is a pure ancestor, so ff-syncs staging up to main via 'git push origin origin/main:refs/heads/staging' — NEVER --force; confirms before the protected-branch push. Records all 5 markers (strategy:main-integration, integration-branch:staging, promote-method:ff-only, feature-merge:merge-commit, hotfix-policy:branch-off-prod-backmerge). Renders the GOLD main-integration runbook verbatim (invariant: main is ancestor of staging; ff-only release block; hotfix off main + back-merge). No AI attribution.",
      "should_trigger": true,
      "files": []
    },
    {
      "id": 11,
      "prompt": "We use gitflow here — develop plus release branches. Configure the git strategy and write the runbook.",
      "expected_output": "Strategy Setup runs. Resolves gitflow (develop present). Ensures develop exists (off main if missing); does NOT create release/* or hotfix/* at setup. Sets work-branch base to develop. Renders the gitflow runbook: feature/* off develop, release/* off develop -> main with back-merge to develop, hotfix/* off main with back-merge to develop. Invariant rendered as the develop/main back-merge discipline (NOT 'production is ancestor of integration'). Markers include strategy:gitflow + integration-branch:develop + decision markers. No --force, no AI attribution.",
      "should_trigger": true,
      "files": []
    },
    {
      "id": 12,
      "prompt": "Bootstrap branching for this repo. It has main and staging but they've each got commits the other doesn't.",
      "expected_output": "Strategy Setup runs and resolves main-integration. During materialization the ancestry check (git log main..staging AND git log staging..main) is non-empty in BOTH directions => branches diverged both ways => Strategy Setup STOPS the sync, does NOT --force, and hands off to conflict resolution (SKILL.md 3.5). The report states the divergence and points to conflict resolution rather than claiming a successful ff-sync. No history rewritten.",
      "should_trigger": true,
      "files": []
    },
    {
      "id": 13,
      "prompt": "Re-run git strategy setup. CLAUDE.md already has the strategy:main-integration, integration-branch:staging, promote-method:ff-only and feature-merge:merge-commit markers, but no hotfix-policy marker.",
      "expected_output": "Detection (Step 2 extension) reads the existing decision markers and treats Q1 (promote-method) and Q2 (feature-merge) as already answered => SKIPS them. Only Q3 (hotfix-policy, marker missing) is asked. Existing branches are not recreated. Idempotent: the only new write is the hotfix-policy marker plus the corresponding hotfix block in the runbook. No re-asking of answered questions, no --force.",
      "should_trigger": true,
      "files": []
    },
    {
      "id": 14,
      "prompt": "configura el flujo de git para este repo nuevo",
      "expected_output": "Strategy Setup runs (Spanish trigger 'configura el flujo de git'). Conversation mirrors Spanish; artifacts (markers, runbook, CLAUDE.md section) stay English. Step 1 repo state -> Step 2 detection -> if undetermined, present the 7 strategies and ask for one slug (detect-then-questionnaire), then run the applicable Q1/Q2/Q3 gated on the resolved strategy. Proposes branch creation (never auto-executes), never --force, no AI attribution.",
      "should_trigger": true,
      "files": []
    }
  ]
}
